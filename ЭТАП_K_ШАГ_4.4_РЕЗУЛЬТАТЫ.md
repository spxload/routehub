# 🔬 ЭТАП K, ШАГ 4.4 — РЕЗУЛЬТАТЫ ПОЛЕВОГО ПЕРЕБОРА (K4 и шире)

> Ветка `egern`. Спецификация — `ЭТАП_K_ШАГ_4.4_K4.md`. Предыдущие шаги —
> `ЭТАП_K_ШАГ_4.2.md`, `ЭТАП_K_ШАГ_4.3.md`.
> Всё ниже — измерено на устройстве k3, **Egern 2.20.0, iOS 18.7**, 2026-08-08/09.
> Стенд: Worker **e0.4.0**, скрипт ревизий k4-1 … k4-8.
> Раздел 2-бис дописан 2026-08-09 по выгрузке `/probe` (20 прогонов, k3).

---

## 1. K4 — ЗАКРЫТ ПОЛОЖИТЕЛЬНО

`schedule`-скрипт исполняется по cron **без MITM и без перехвата**; `ctx.http`
работает; запрос через группу маршрутизуется; отчёт `POST`'ом доходит до Worker'а.
Исходная формулировка K4 («скрипт на `http_request` без MITM») била мимо: нужный
механизм — `schedule`, к перехвату отношения не имеющий.

Следствие: телеметрия на Egern строится, и отрицательный K9 (нет штатной выгрузки
записей трафика) перестаёт быть блокирующим.

---

## 2. МАРШРУТИЗАЦИЯ ЗАПРОСОВ ИЗ СКРИПТА

| Способ | Результат |
|---|---|
| `ctx.http` с `policy` = имя группы | работает (204) |
| `ctx.http` с `policy` = `DIRECT` | работает, выход 5.227.10.120 (РФ) |
| `ctx.http` с `policy` = имя узла ИЗ ПОДПИСКИ | **404 от самого Egern** |
| `ctx.http` с `policy` = имя узла, ОБЪЯВЛЕННОГО в `proxies` | **АДРЕСУЕТ КОНКРЕТНЫЙ УЗЕЛ** — доказано в разделе 2-бис |
| `policyDescriptor` | ошибка 400 |
| `$httpClient` с `node` или `policy` | **параметры ИГНОРИРУЮТСЯ**, выход как у обычных правил |
| глобальный `fetch` | работает, но **ИДЁТ МИМО ТУННЕЛЯ** (выход 5.227.10.120) |

Все проксированные варианты k4-7 дали один выход `111.88.96.83`. Причина
установлена в разделе 2-бис: `$httpClient` (прослойка Surge) адресацию
игнорирует, а группа в тот момент выбрала тот же узел, что и явная политика.
**`ctx.http` адресацию соблюдает.**

---

## 2-БИС. ОТКРЫТЫЙ ВОПРОС №1 — ЗАКРЫТ ПОЛОЖИТЕЛЬНО (2026-08-09)

Ревизия **k4-8**, устройство k3, выгрузка `/probe` (20 прогонов).

### T2 — три явных узла дают ТРИ РАЗНЫХ выхода

| проба | политика | статус | IP | `loc` |
|---|---|---|---|---|
| n1 | `RH-Явный-1` | 200 | 111.88.96.83 | RU |
| n2 | `RH-Явный-2` | **HTTP 400** | — | — |
| n3 | `RH-Явный-3` | 200 | 85.93.10.78 | **DE** |
| group | `RH-Пул-Обычные` | 200 | 111.88.96.**82** | RU |
| direct | `DIRECT` | 200 | 5.227.10.120 | RU |

Групповой выход (`.82`) не совпал ни с одним явным (`.83`) — объяснение
«группа случайно выбрала тот же узел» исключено.

**T3** (повтор через 1,5 с): `RH-Явный-1` снова `111.88.96.83`, `RH-Явный-2` снова 400
⇒ распределение детерминировано, а не случайная раздача.

### T6 — поузловой замер скорости работает

По 256 КБ через два узла: `RH-Явный-1` — 220 мс / **9,5 Мбит/с**;
`RH-Явный-2` — 742 мс / **2,8 Мбит/с**. Разброс втрое ⇒ измеряется узел, а не
общий канал. **Персональный поузловой спидтест на Egern реализуем.**

### Что стоит за именами `RH-Явный-N`

`explicitNodes()` берёт первый ws-узел, затем первые tcp. Сверка с `sub_cache`
стенда (2026-08-09) показала: **все 17 ws-узлов подписки — обходные**, а они
отфильтрованы как платные ⇒ ветка «взять ws» фактически мертва, все три явных
узла — tcp.

| политика | узел подписки | транспорт | измеренный `loc` |
|---|---|---|---|
| `RH-Явный-1` | `🇷🇺 ⭐ 🚀 Россия [VPN] #1` | tcp | RU |
| `RH-Явный-2` | `🇷🇺 🚀 Россия [VPN] #1` | tcp | — (400) |
| `RH-Явный-3` | `🇩🇪 ⚡⭐ Германия [🌀 VPN]` | tcp | **DE** |

Имя `RH-Явный-3` обещало Германию — Cloudflare подтвердил `loc=DE`.
**Вывод 4 `СТАРТ.md` («страна — из имени») получил независимое подтверждение.**

**Адресуемость ws-узла из `proxies` НЕ проверена** и без расхода платного
трафика не проверяется. Ограничение переносится в решение о миграции.

### Аномалия `RH-Явный-2`

Узел стабильно отдаёт `HTTP 400, body: Error` на `https://www.cloudflare.com/cdn-cgi/trace`
(во всех прогонах, T2 и T3), **но в том же прогоне успешно прокачивает 256 КБ**
через `speed.cloudflare.com`. Узел живой и маршрутизируется; отказ привязан к
конкретному хосту. Причина не установлена — проверка вынесена в ревизию k4-9.

### Состав подписки изменился

На 2026-08-09 в `sub_cache` стенда — **68 узлов**: 51 tcp обычных + 17 ws обходных
(было 69: 52 + 17). Подтверждает вывод 16 `СТАРТ.md`: состав считать из `sub_cache`.

---

## 3. НЕДОКУМЕНТИРОВАННЫЙ СЛОЙ СОВМЕСТИМОСТИ — ЭТО SURGE

`$environment` = `{ system: "iOS", surge-build: "3010", surge-version: "5.8.3", language: "ru-KZ" }`.

| Объект | Состав | Состояние |
|---|---|---|
| `$httpClient` | get/post/put/patch/delete/head/options/request | работает, **маршрутные параметры игнорирует** |
| `$httpClient.request` | — | **РОНЯЕТ ПРОГОН.** НЕ ВЫЗЫВАТЬ |
| `$persistentStore` | read, write | работает; **хранилище ОБЩЕЕ с `ctx.storage`** (проверено крест-накрест) |
| `$utils` | geoip, ipasn, ipaso, ungzip | работают: `8.8.8.8` → US / 15169 / GOOGLE |
| `$network` | wifi(ssid,bssid), cellular-data, v4, v6, dns | то же, что `ctx.device` |
| `$script` | name, startTime, type=`cron` | работает |
| `$notification` | post | работает |
| `$done`, `$cronexp` | — | присутствуют |

**ОТСУТСТВУЮТ:** `$httpAPI`, `$config`, `$surge`, `$loon`, `$argument`, `$task`, `$prefs`,
`$option`, `$input`, `$rocket`, `$axios`, `$app`, `$exit`, `$log`, `$resource`, `$msg`,
`$openURL`, `$copy`, `$browser`, `$trigger`, `$intercept`, `$wifi`, `$storage`, `$mock`.

### ВЫВОД, ЗАКРЫВАЮЩИЙ ТЕМУ
**Переключение выбора политики из скрипта на Egern НЕВОЗМОЖНО НИКАКИМ ПУТЁМ**:
нет ни `$config.setSelectPolicy` (Loon), ни `$httpAPI` (Surge), ни штатного аналога
в `ctx`. Для RouteHub это терпимо: выбор делает `fallback`/`smart`, не скрипт
(вывод 1 в `СТАРТ.md`).

---

## 4. СРЕДА ИСПОЛНЕНИЯ — ПОЛНЫЙ WEBKIT

`navigator.userAgent` = `Mozilla/5.0 (iPhone; CPU iPhone OS 18_7 ...) AppleWebKit/605.1.15`.
В `globalThis` — весь DOM-набор: `window`, `document`, `location`, `WebSocket`,
`XMLHttpRequest`, `localStorage`, `sessionStorage`, `indexedDB`, `openDatabase`,
`EventSource`, `Worker`, `SharedWorker`, `RTCPeerConnection`, `ReadableStream`, `Blob`,
`FormData`, `structuredClone`, `performance`, `WebAssembly`, `Intl`.
Также присутствуют **неописанные `Egern`, `__context`, `_tickTimers`** — разбор
на шаге T1 ревизии k4-8.

Проверено практически:
- **WebSocket работает** (`wss://ws.postman-echo.com/raw` → эхо). Основа для дашборда
  реального времени: инициатива у телефона, что снимает ограничение вывода 7
  (Worker сам в телефон не стучится).
- `localStorage` персистентен между прогонами (третье хранилище помимо `ctx.storage`).
- `ctx.storage` держит значение **1 МБ** (32 КБ, 256 КБ, 1 МБ — все прошли).
  Перечисления ключей НЕТ — имена надо знать заранее.
- `crypto` — только `getRandomValues`. **Нет `crypto.subtle` и `randomUUID`** ⇒ SHA-256
  и HMAC на устройстве штатно не считаются.
- `WebAssembly` полноценный; `setTimeout` точен (1207 мс на 1200).
- Бенчмарк: миллион итераций — 7 мс.
- 5 параллельных запросов через группу — 109–211 мс (против ~95 мс на один).
- `ctx.compress`: gzip 576 → 36 байт, brotli → 26; `$utils.ungzip` разжимает наш gzip.
- Локального HTTP-API у Egern НЕТ (пробованы 9090, 8080, 6152, 6170, 7890, 1082, 8888).

---

## 5. ГЕОЛОКАЦИЯ — ОБЕ БАЗЫ НЕНАДЁЖНЫ

Для выходного IP `111.88.96.83`: Cloudflare говорит `loc=RU`, `colo=DME`;
`ctx.lookupIP` — `PK` с пустыми `asn`/`organization`; `$utils.geoip` — тоже `PK`, `asn: null`.
При этом для `8.8.8.8` `$utils` даёт полные данные. Значит база просто не знает
диапазонов провайдера.

**Вывод 4 `СТАРТ.md` подтверждён окончательно: страна узла — ТОЛЬКО из имени**
(флаг/слово), ни одна GeoIP-база на устройстве для этого не годится.

---

## 6. СВОЙСТВА ПЛАТФОРМЫ, ВЫЯВЛЕННЫЕ ПОПУТНО

1. **Egern нормализует cron**: `*/5 * * * *` превращается в `0 */5 * * * *`.
2. **Падение внутри прослойки НЕ ловится `try/catch`** и убивает весь прогон
   (`UNHANDLED PROMISE REJECTION ... @ egern-js`). Отсюда схема перебора «один шаг
   за прогон, курсор в хранилище, сдвиг ДО выполнения» — единственный способ
   перебирать нестабильный API без потери остального.
3. **`ctx.widgetFamily` в `schedule` пуст**, `ctx.request`/`ctx.response` тоже — они
   заполняются только в своих типах скриптов.
4. **Экран запуска скриптов в Egern малоинформативен** (наблюдение Дианы),
   в отличие от Loon. Замена — `generic`-скрипт + `widgets` (раздел 8).
5. **Сборка Worker'а падала на трёх коммитах** из-за мусора в хвосте файла.
   Лечение — **хвостовой страж**: файл заканчивается строчным комментарием
   без перевода строки, и любой дописанный хвост попадает в комментарий.
6. **В тексте скрипта, живущего в шаблонной строке, нельзя обратные слэши**:
   `\d` превращается в `d` и тихо ломает регулярки. Все классы символов — явные.
7. **`sessionStorage` НЕ переживает прогон** (`prev_was: null` при `len: 1`),
   в отличие от персистентного `localStorage`. Состояние держать в `ctx.storage`
   либо `localStorage`.
8. **Cron `* * * * *` ломает курсорную схему**: шаг T3 длился 10,86 с, следующий
   прогон стартовал раньше его завершения, снял курсор и затёр промежуточную
   запись. Интервал cron обязан превышать длительность самого долгого шага.
9. **После `done: true` скрипт продолжает срабатывать вхолостую** (`total_ms` 1–4 мс)
   и слать `POST` — при `* * * * *` это 1440 запросов в сутки из общей квоты
   Workers на аккаунт (вывод 10 `СТАРТ.md`).

---

## 7. ДОКУМЕНТАЦИЯ — ЧТО ВЫЯСНИЛОСЬ БЕЗ СТЕНДА

Источники: `/docs/configuration/policy_groups`, `/proxies`, `/scriptings`,
`/javascript-api`, `/widgets`, `/faq`.

1. **`urls` и `filter` — ОБЩИЕ поля пяти базовых типов групп**, не только `external`.
   Значит подписку можно вешать прямо на `smart` или `fallback`; `external` не нужен.
   Это снимает ограничение, из-за которого K5 уехал на 4.4.
2. **`smart`**: EWMA по задержке, джиттеру и надёжности, гистерезис с минимальным
   временем удержания, адаптивная частота проб. Настройка — только `priorities`
   (коэффициенты по regex на имя: <1 поднимает, >1 опускает, 0 — всегда первым).
   Это наша модель рейтинга, реализованная внутри приложения.
3. **`conditional`**: правила `ssid`, `bssid`, `cellular` с glob-масками и `default_policy`.
   Разделение Wi-Fi/сотовая без единого скрипта — в Loon такого нет.
   Домашние данные уже известны: SSID `fh_862d68_5G`, BSSID `c4:64:b6:86:2d:6c`.
4. Общие поля групп: `flatten` (разворачивать узлы вложенных групп), `hidden`,
   `icon` (SF Symbols), `block_quic` на уровне группы и узла, **`prev_hop`** — цепочки прокси.
5. **`proxies`**: узлы объявляются прямо в профиле. Vless: `name`, `server`, `port`,
   `user_id`, `flow`, `tfo`, `udp_relay`, `transport`, `block_quic`, `prev_hop`, `ip_version`.
   `transport`: `tls{sni}`, `reality{public_key, short_id}`, `ws{path, headers{Host}}`.
   Имя узла обязано быть глобально уникальным; узлы подписки дедуплицируются по имени.
   `fingerprint` (`fp` в ссылке) в документации НЕ описан — не переносим.
6. **Скрипты**: пять типов — `http_request`, `http_response`, `schedule`, `generic`,
   `network`. Общие поля: `name`, `script_url`, `env`, `update_interval` (86400),
   `timeout` (10 с по умолчанию, **максимум 600**), `disabled`.
   Форма: `export default async function (ctx) { ... }`.
7. **Виджеты**: `generic`-скрипт возвращает JSON-DSL, секция `widgets` связывает
   виджет со скриптом по имени.

---

## 8. ЧТО ДЕЛАТЬ ДАЛЬШЕ (по приоритету)

1. **Ревизия k4-8 добрана полностью** (7 шагов, `rh_scan8`, все `done`).
   Итоги — раздел 2-бис. Следующая ревизия **k4-9**: причина 400 у `RH-Явный-2`.
2. **Вернуть cron на `*/5`** — на устройстве сейчас `* * * * *`.
   Обоснование усилилось: пункты 8–9 раздела 6.
3. **Собрать свой экран**: `generic`-скрипт `RH-Экран` + секция `widgets`.
   Данные берёт из `ctx.storage`, куда пишет `schedule`-скрипт. Сеть при отрисовке
   не нужна ⇒ экран работает и под whitelist, когда Worker недоступен.
4. **`http_request`-скрипт** на `http://cp.cloudflare.com/generate_204` — проверка
   перехвата plain-HTTP без MITM; счётчик читается шагом T7 (`rh_mitm_hits`).
5. **Перевод пула на `smart` + `priorities`** (и с ними K5), `conditional` по SSID.
6. **`/ws` на стенде** (Cloudflare Workers умеют WebSocket) — основа реального времени.
7. Остаются незакрытыми K1 и K3 (ждут реального whitelist) и K6 (звонки).

---

## 9. СОСТОЯНИЕ СТЕНДА НА КОНЕЦ СЕССИИ

- Worker **e0.4.0**, скрипт **k4-8** (коммит `650208a`).
- Профиль: 7 групп (`RH-Пул-Обычные`, `RH-Обход`, `RH-Проба-Обычная`,
  `RH-RU`, `RH-Главный`, `RH-Проба-DIRECT`, `RH-Явные`), 3 узла в `proxies`,
  6 `rule_set`, `default → RH-Главный`, один `schedule`-скрипт.
- Эндпоинты: `/health`, `/admin/keys?admin=`, `/t/<token>/nodes`, `/profile` (+`&safe=1`),
  `/script/k4.js`, `GET и POST /probe` (+`&n=` — сколько последних прогонов отдать).
- **Боевой контур за всю сессию не затронут.** Ветка `egern` в `main` не сливалась.

---

## 10. ПРЕДВАРИТЕЛЬНЫЙ БАЛАНС ПО МИГРАЦИИ (блок 5)

**За:** модель «слабый DIRECT» переносится (K2); списки `C-draft-39` работают без
конвертации (K7); `smart` с EWMA и `priorities` заменяет собой часть логики
сервера; `conditional` закрывает Этап E без скриптов; `schedule`-скрипты работают
без MITM; среда богаче Loon (WebSocket, localStorage, WebAssembly, `prev_hop`);
**поузловой спидтест реализуем** — `ctx.http` адресует узел из `proxies` (2-бис).

**Против:** нет выгрузки записей трафика (K9); нет управления политиками из
скрипта; геобазы неполны; прослойка совместимости частично нерабочая и умеет
ронять прогоны; поузловой спидтест требует объявления всех обычных узлов в
`proxies` (дублирование подписки в профиле), а адресуемость ws-узлов не проверена.

**Решение не принимается** до пунктов 3–5 раздела 8.
