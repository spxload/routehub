# 🚦 СТАРТ — RouteHub (читать ПЕРВЫМ в каждом новом чате)

> Точка входа: на каком мы этапе, где файлы, что решено. Главный документ —
> `ПЛАН_РЕАЛИЗАЦИИ_v3.md`. Этот файл — навигатор поверх него.
> Обновлён 2026-08-09 (C-draft-**39** / worker **v1.9.2** / панель **p2** /
> стенд Egern **e0.4.0**, скрипт стенда **k4-8**).
> **Блоки 1, 3, 2 и 1-бис ВЫПОЛНЕНЫ.** Сверх них закрыты хвост №1 (классификация
> узлов) и QR-код в панели (панель p2).
> **Блок 4 (стенд Egern): шаги 4.1, 4.2, 4.3 ЗАКРЫТЫ; шаг 4.4 — идёт, K4 закрыт.**
> ВНИМАНИЕ: всё про возможности Egern — в `ЭТАП_K_ШАГ_4.4_РЕЗУЛЬТАТЫ.md`
> (ветка `egern`). По стенду читать его ПЕРВЫМ.

---

## ТЕКУЩИЙ СТАТУС

- **Этапы A, B, C, D — ЗАКРЫТЫ.**
- **Этап E — собран, работает; полевая проверка в фоне** (не блокирует).
- **Этап F (whitelist РКН) — ГЛАВНЫЙ ВОПРОС ЗАКРЫТ** полевым тестом 2026-06-12.
  Подтверждено: **Loon ПРОБИВАЕТ член `DIRECT` в `fallback`**. Модель «слабый
  DIRECT по умолчанию» — см. `ЭТАП_F_ЗАМЕТКИ.md`.
- **Звонки (☎) — собраны** (C-draft-19..20), полевая проверка на k1 — открыта.
- **Локальный дашборд — РАБОТАЕТ** (`http://rh.box`). Доработка **ЗАМОРОЖЕНА**
  до решения по Egern.
- **МИГРАЦИЯ KV → D1 — ВЫПОЛНЕНА** 2026-08-03 (v1.8.0), уборка завершена
  2026-08-06 (v1.9.0). **Откат на KV невозможен.**
- **k1** (Диана) активен, **k2** активен. **k3 — ключ стенда Egern** (отдельная база).
- **РЕГИОНАЛЬНЫЙ ПРИОРИТЕТ — СДЕЛАНО** 2026-08-03 (C-draft-36 + v1.8.1).
- **АДМИН-ПАНЕЛЬ — СДЕЛАНА** 2026-08-06; сессия на 30 суток (v1.9.1);
  **QR-код ссылки — 2026-08-07** (панель p2).
- **ХВОСТ №1 (классификация узлов) — ЗАКРЫТ 2026-08-07** (v1.9.2 + C-draft-39,
  PR #2, merge `6d0314d6`); фильтры сверены на устройстве.
- **СТЕНД EGERN — шаги 4.1, 4.2, 4.3 закрыты; 4.4 идёт.** Разделы ниже.
- **ЧИСТКА ЗАВЕРШЕНА 2026-08-07:** KV-namespace удалён, `GIST_TOKEN` и гист удалены.

### Стенд Egern — шаг 4.1 (2026-08-07)
- Ветка **`egern`** (в `main` не сливается), Worker **`routehub-egern`**, база
  **`routehub-egern-db`** (id `55ebdd96-f6d4-453a-891a-4662ff5366dd`, EEUR).
- Секреты стенда: `SUBSCRIPTION_URL`, `SUB_HWID`, `ADMIN_KEY` (совпадает с боевым).
- **ГРАБЛИ.** Workers Builds не даёт выбрать ветку при создании проекта:
  первая сборка шла из `main` и задеплоила боевой код с биндингом на боевую базу.
  Лечение: Settings → Build → Branch control → production branch = `egern`.
  Галочку **«Builds for non-production branches» держать СНЯТОЙ.**

### Стенд Egern — шаг 4.2 (ЗАКРЫТ 2026-08-08)
Профиль принят устройством обычным вариантом (не `&safe=1`): 5 групп,
**52 узла** в `RH-Пул-Обычные`, **17** в `RH-Обход`, предупреждений нет.
Подробности — `ЭТАП_K_ШАГ_4.2.md`, раздел 7.

### Стенд Egern — шаг 4.3 (ЗАКРЫТ 2026-08-08)
- **K2 — положительно.** Диагностические группы дали 36 мс против таймаута
  на недостижимом 192.0.2.1 ⇒ к члену `DIRECT` применяется **групповой**
  `latency_test_url`. Модель «слабый DIRECT» на Egern переносится.
- **K7 — положительно.** Шесть списков загрузились, политики совпали с ожидаемыми,
  включая Shadowrocket-формат `domains_banking.list`.
- **K9 — отрицательно.** Выгрузки записей трафика нет нигде.
- K1 и K3 ждут реального whitelist. Подробно — `ЭТАП_K_ШАГ_4.3.md`.

### Стенд Egern — шаг 4.4 (ИДЁТ, K4 ЗАКРЫТ 2026-08-09)
Worker **e0.4.0**, скрипт **k4-8**. Профиль: 7 групп, 3 узла в `proxies`
(`RH-Явный-1..3`), 6 `rule_set`, один `schedule`-скрипт.
**ВСЁ по возможностям Egern — в `ЭТАП_K_ШАГ_4.4_РЕЗУЛЬТАТЫ.md` (ветка `egern`).**
Кратко: K4 закрыт (schedule-скрипт работает без MITM, телеметрия доходит);
найдена недокументированная Surge-прослойка; среда — полный WebKit с рабочим
 WebSocket. **Главный открытый вопрос: адресуется ли узел из `proxies`** — шаги
 T2/T3 ревизии k4-8, от него зависит поузловой спидтест при миграции.

### Что работает (актуальные версии)
- **Боевой Worker** `https://routehub.proton4iker.workers.dev` (**v1.9.2**, панель **p2**,
  авто-деплой при push в `main`). Эндпоинты устройства: `GET /config?key=kN`,
  `/nodes`, `/refresh`, `/dashboard`, `/mylist`, `/status`, `/whoami`, `/icon.svg`;
  `POST /speed`, `/rkn`, `/addrule`, `/delrule`. Админ-зона: `GET /admin`,
  `POST /admin/login`, `/admin/logout`, `GET /admin/state`, `/admin/keys`,
  `POST /admin/device`, `/admin/settings`, `/admin/action`, `/admin/mylist`.
- **Стенд** `https://routehub-egern.proton4iker.workers.dev` (**e0.4.0**, авто-деплой
  при push в `egern`). Эндпоинты: `/health`, `/admin/keys?admin=<ADMIN_KEY>`,
  `/t/<token>/nodes?key=kN`, `/profile` (+`&safe=1`), `/script/k4.js`,
  `GET и POST /probe` (+`&n=` — сколько последних прогонов отдать).
- **ВХОД В ПАНЕЛЬ — СЕССИЯ НА 30 СУТОК (v1.9.1).** cookie `rh_adm` = `<exp>.<подпись>`,
  HMAC-SHA256 по `rh-admin-v1|<exp>` ключом `ADMIN_KEY`. На сервере ничего не хранится.
  Атрибуты: `HttpOnly`, `Secure`, `SameSite=Strict`, `Path=/admin`.
- **ТОКЕНЫ — ФАЗА 2 (с 2026-08-06).** Флаг `token_required` в D1 (`settings`), `true`.
- **Хранилище — Cloudflare D1.** Боевая: binding `RH_DB`, `routehub-db`,
  id `b03b5540-80aa-43e6-9dce-92bd8e221507`, EEUR. Таблица `kv (key, value, updated_at)`.
  Ключи: `sub_cache`, `devices`, `settings`, `metrics:<kN>`, `rkn:<kN>`,
  `rkn_hist:<kN>`, `mylist:<kN>`. На стенде также `probe:<kN>`.
- **`FRESH_MS` = 60 мин**, верхнюю границу держит cron (раз в 2 ч).
- Секреты CF (тип Secret): `SUBSCRIPTION_URL`, `SUB_HWID`, `ADMIN_KEY`.
  `CONFIG_URL` = raw routehub.conf.
- **conf = C-draft-39** — модель «слабый DIRECT»:
  - строка `Lastdep` — **заглушка** `https://routehub.invalid/nodes,<параметры>`;
    хвост параметров после первой запятой берётся именно отсюда — не терять;
  - VPN-фильтры (12 шт.) — `^(?!.*Обход).*VPN].*🛜/📱`;
  - обычный трафик: `RH-АВТО-W/-C = fallback` (`EU → AM → RU → Прочие → Игры → Обход`);
  - whitelist-домены forg-lib → **жёсткий DIRECT** (анти-VPN);
  - GEOIP-RU, DeepSeek, Apple push, банки ЦБ, category-ru → **RH-RU**;
  - FINAL → **RH-Главный**; ИИ → RH-AI; youtube/instagram/telegram/refilter → RH-АВТО;
    звонки → RH-Звонки;
  - личные исключения вверху `[Rule]`: сейчас `samokat.ru → DIRECT`;
  - все fallback: `max-timeout=2000, interval=120`;
  - `[MITM] enable=false`, `RouteHub-FailLog.plugin` `enabled=false`.
- **Скрипты (активны):** `routehub-speedtest.js` v0.6.1 (cron 20 мин),
  `routehub-netwatch.js` v0.5.0, `routehub-viewer.js` v0.6.3,
  `routehub-dashcache.js` v0.1.0 (cron 15 мин), `routehub-rkn.js` v0.2.0 (cron 3 мин),
  `routehub-dash.js` v0.1.0.

### Состав подписки (2026-08-07)
**69 узлов**: 43 × `[VPN]`, 4 × `[🌀 VPN]`, 5 × `[Игры]`, 17 × `[Обход - <оператор>]`.
Транспорт: 52 tcp + 17 ws, **gRPC нет**. Считано из `sub_cache`.

**Значки провайдера меняются часто, логика на них НЕ завязана** — только на слово
в скобочном теге. 🌀 — прямая локация, под whitelist не работает.

### Тесты Worker'а
`tests/routehub-worker.test.js` + `tests/mock-d1.js`, запуск `node --test "tests/*.test.js"`.
На 2026-08-07 — **56/56** (Node v22). CI намеренно нет. `routehub-admin.html` тестами
не покрыт. **Стенд тестами не покрыт**: генератор профиля и текст скрипта
проверяются прогоном YAML-парсером и импортом как ES-модуля.

### Важно про доставку конфига
- raw GitHub кэширует файлы ~5 мин. Признак — версия в шапке.
- При пасте больших файлов ВСЕГДА сверять после коммита (md5/размер/diff).
- Loon дёргает `/config` раз в 15–20 мин.

---

## БЛОКИ РАБОТ

**Блоки 1, 3, «сессия панели», 2, 1-бис — ВЫПОЛНЕНЫ 2026-08-06.**
**Хвост №1 и QR-код — ВЫПОЛНЕНЫ 2026-08-07.**

**Блок 4 — стенд Egern:**
- шаг **4.1 — ВЫПОЛНЕН** (инфраструктура);
- шаг **4.2 — ВЫПОЛНЕН** (минимальный профиль принят);
- шаг **4.3 — ВЫПОЛНЕН** (K2 да, K7 да, K9 нет; K1/K3 ждут whitelist);
- шаг **4.4 — ИДЁТ.** K4 закрыт. Осталось:
  1. добрать ревизию **k4-8** (7 шагов, ключ состояния `rh_scan8`) — главное T2/T3;
  2. вернуть cron скрипта на `*/5` (сейчас на устройстве `* * * * *`);
  3. `generic`-скрипт + `widgets` — свой экран взамен скудного штатного;
  4. `http_request`-скрипт — перехват plain-HTTP без MITM;
  5. `smart` + `priorities` (и с ними K5), `conditional` по SSID;
  6. `/ws` на стенде — реальное время;
  7. звонки (K6), иконки, панель стенда.

**Блок 5 — решение «мигрируем / остаёмся».** Предварительный баланс — раздел 10
`ЭТАП_K_ШАГ_4.4_РЕЗУЛЬТАТЫ.md`. Не принимается до закрытия T2/T3.

Заморожено до решения по Egern: доработка дашборда, скилл `routehub`.

---

## КАК СО МНОЙ РАБОТАТЬ (стиль)

- Академичный русский, без «воды» и вводных фраз. Только проверенные факты;
  нет данных — «не могу подтвердить». Источники не выдумывать.
- Простота и предсказуемость важнее функций. Решения до кода; пушбэк с аргументами.
- **Правило «один этап = один чат» СНЯТО.** Результат фиксируется в репозитории
  после каждой завершённой единицы работы, а не в конце чата.
- **Решения фиксировать в репозитории ДО написания кода.**
- **Без согласия Дианы живой конфиг/код не менять.**
- Claude коммитит файлы в репо целиком; Диана скачивает/заменяет. Деплой — авто.
- `СТАРТ.md` и `ПЛАН_РЕАЛИЗАЦИИ_v3.md` Диана перекладывает в «Файлы проекта» вручную
  (туда у Claude доступа нет; готовый файл Claude кладёт в репозиторий).
- Удаление файлов, Secrets/базы Cloudflare — только Диана.

---

## ФАЙЛЫ ПРОЕКТА

### A. В «Файлах проекта» (грузятся всегда)
- `СТАРТ.md` — этот файл.
- `ПЛАН_РЕАЛИЗАЦИИ_v3.md` — этапы, факты, журнал решений, каталог списков,
  сверка с докой Loon. (Серверная часть устарела — теперь Worker+D1.)

### B. В репозитории — читать инструментом ПО НАДОБНОСТИ
> get_file_contents, owner=`spxload`, repo=`routehub`. **Ветку указывать.**

| Файл | Ветка | Когда читать |
|---|---|---|
| `ЭТАП_K_ШАГ_4.4_РЕЗУЛЬТАТЫ.md` | `egern` | **ВСЁ про возможности Egern — читать ПЕРВЫМ по стенду** |
| `ЭТАП_K_ШАГ_4.4_K4.md` | `egern` | спецификация проверки K4 |
| `ЭТАП_K_ШАГ_4.3.md` | `egern` | K2, K7, K9 — протоколы и итоги |
| `ЭТАП_K_ШАГ_4.2.md` | `egern` | решения и границы объёма стенда |
| `ЭТАП_K_EGERN.md` | `main` | исходное исследование, чек-лист K1–K10 (часть устарела) |
| `ЭТАП_F_ЗАМЕТКИ.md` | `main` | модель «слабый DIRECT», тест whitelist |
| `ЭТАП_E_ПРОГРЕСС.md` | `main` | чек-лист полевой проверки E |
| `ДОКУМЕНТАЦИЯ_LOON_RU.md` | `main` | синтаксис Loon 3.3.9 |
| `КАТАЛОГ_СПИСКОВ.md` | `main` | списки правил |

### C. Код в репозитории
**Ветка `main` (боевое):** `routehub-worker.js` **v1.9.2**, `routehub-admin.html` (**p2**),
`routehub.conf` **C-draft-39**, `wrangler.toml`, `tests/*`, `routehub-speedtest.js` v0.6.1,
`routehub-netwatch.js` v0.5.0, `routehub-viewer.js` v0.6.3, `routehub-rkn.js` v0.2.0,
`routehub-dash.js` / `.html` v0.1.0, `routehub-dashcache.js` v0.1.0, `routehub-faillog.js`,
`RouteHub-Dash.plugin`, `RouteHub-FailLog.plugin` (выключен).

**Ветка `egern` (стенд):** `routehub-egern-worker.js` **e0.4.0**,
`routehub-egern-k4.js` (скрипт **k4-8**), `routehub-egern-proxies.js` (разбор vless→proxies),
свой `wrangler.toml`, документы шагов 4.2–4.4. **В `main` не сливать.**

---

## ГЛАВНЫЕ ВЫВОДЫ (зафиксировано)

1. **Выбор узла делает fallback**, не скрипт. Порядок узлов задаёт Worker.
2. **Loon ПРОБИВАЕТ DIRECT в fallback** → модель «слабый DIRECT».
3. **РФ-whitelist домены → жёсткий DIRECT** (обход вреден: анти-VPN).
   РФ-не-whitelist (tbank, мелкие банки, whoosh, Apple) → RH-RU.
4. **СТРАНА УЗЛА — ИЗ ИМЕНИ** (флаг/слово), НЕ из GeoIP.
5. **Whitelist детектит netwatch + routehub-rkn**; под whitelist Worker и панель
   достижимы ТОЛЬКО через обход.
6. **Приоритет правил Loon (3.9):** доменные раньше IP; локальные > подписка.
7. **Дашборд = «пульт»:** Worker сам в телефон «постучать» не может.
   **(На Egern снимается: WebSocket работает, см. вывод 45.)**
8. **Loon НЕ умеет логировать незагрузившиеся домены.**
9. **При пасте больших файлов сверять после коммита** (md5/diff).
10. **Лимиты Cloudflare (free):** D1 — 100 000 строк/сутки; Workers — 100 000
    запросов/сутки. НА АККАУНТ, стенд ест ту же квоту.
11. **`D1.batch()` НЕ экономит квоту** — только атомарность.
12. **Порядок миграции хранилища критичен.** У второго Worker'а — своя база, всегда.
13. **Порядок узлов в подписке — ОДИН на все фильтры** (ограничение Loon).
14. **Экранирование при коммите — РЕАЛЬНЫЙ риск** (`\[VPN\]` → `\\[VPN\\]`).
    `.conf` — простой текст, слэши НЕ удваивать. Всегда `diff` после push.
15. **Задеплоенный код можно и НУЖНО сверять** — `workers_get_worker_code`
    либо запрос к живому эндпоинту.
16. **Состав подписки меняется — считать из `sub_cache`**, не из памяти.
17. **zashboard и подобные панели НЕ подключаются** ни к Loon, ни к Egern.
18. **Плагины/скиллы контекст НЕ экономят, а тратят.**
19. **Нормализация данных под SQL была бы вредна** (1 запись → ~90).
20. **Kernel MCP (браузер) полезен** там, где статическая выдача устарела.
21. **Wrangler бандлит модули по `[[rules]]`** — только статически импортированные.
22. **Крупные файлы безопаснее коммитить через ветку + PR.**
23. **Настройки, способные отрезать устройство, держать в D1**, не в константе.
24. **Точечная правка JSON в D1** — `json_remove`; перед записью прогнать `SELECT`
    с `json_valid()`.
25. **Состав подписки читается из D1 без выгрузки секретов** (рекурсивный CTE,
    часть после `#`). Имена percent-encoded НЕПОСЛЕДОВАТЕЛЬНО.
26. **Статус `conflict` — рассинхрон `nonce`.** Лечение — «Отвязать».
27. **Сессия без серверного состояния:** HMAC-cookie + `ADMIN_KEY` как ключ подписи.
28. **Классификацию узлов НЕЛЬЗЯ вешать на значки провайдера.** Критерий — слово
    в скобочном теге. Проверять ВСЕ места критерия.
29. **`]` в regex вне класса символов литерально** — экранировать не нужно.
30. **Кодировщик QR ошибается ТИХО.** Проверка — помодульное совпадение И обратное
    декодирование независимым декодером.
31. **Правка панели не требует подъёма версии Worker'а** — есть `PANEL_REV`.
    **Аналогично на стенде: `K4_REV` у скрипта ведётся отдельно.**
32. **`var` в двух ветках одного обработчика** читается как ошибка.
33. **Workers Builds не даёт выбрать ветку при создании проекта.** Проверять итог
    не по логу сборки, а запросом к эндпоинту.
34. **Egern валидирует профиль по типам и падает на ПЕРВОМ несоответствии**
    → новые конструкции вводить по одной за импорт. Пример: `hijack_dns` — список.
35. **Выжимка документации — не замена документации.**
36. **Панель нельзя переключать между контурами кросс-доменным вызовом**
    (`SameSite=Strict`). Переключатель — обычная ссылка.
37. **`SUBSCRIPTION_URL` и `SUB_HWID` существуют в единственном НЕЧИТАЕМОМ
    экземпляре.** Достать из кабинета Lastdep и сохранить у себя.
38. **Фильтр группы в Egern — regex по имени узла**: `^(?!.*Обход)` → 52,
    `\[Обход` → 17 при 69 в подписке. **`urls` и `filter` — общие поля пяти
    базовых типов групп**, не только `external`.
39. **Egern принимает `DIRECT` членом `fallback` и тестирует его ГРУППОВЫМ
    `latency_test_url`** (36 мс против таймаута на 192.0.2.1). Модель переносится.
40. **`rule_set` принимает списки формата Shadowrocket** — конвертация не нужна.
41. **Выгрузки записей трафика в Egern НЕТ** нигде (K9 отрицателен).
42. **`schedule`-скрипты работают без MITM**, `ctx.http` с `policy` группы
    маршрутизуется, телеметрия доходит до Worker'а. **K4 ЗАКРЫТ.**
43. **Внутри Egern — недокументированная прослойка SURGE 5.8.3**: `$httpClient`,
    `$persistentStore`, `$utils`, `$network`, `$script`, `$notification`, `$done`, `$cronexp`.
    **`$persistentStore` и `ctx.storage` — одно хранилище.** НО: `$httpClient`
    **игнорирует `node` и `policy`**, а `$httpClient.request` **роняет прогон**.
44. **Переключение политики из скрипта на Egern НЕВОЗМОЖНО**: нет `$config`,
    нет `$httpAPI`, нет аналога в `ctx`. Для RouteHub терпимо (вывод 1).
45. **Среда скриптов — полный WebKit** (iOS 18.7): **WebSocket работает**,
    `localStorage` персистентен, есть `WebAssembly`, `indexedDB`, `XMLHttpRequest`.
    **`crypto.subtle` ОТСУТСТВУЕТ** — SHA-256/HMAC на устройстве не считаются.
    `ctx.storage` держит 1 МБ на ключ, перечисления ключей нет.
46. **Глобальный `fetch` идёт МИМО туннеля** — политикам не подчиняется.
47. **Падение внутри прослойки Egern не ловится `try/catch`** и убивает весь
    прогон. Нестабильный API перебирать по шагам с курсором в хранилище,
    сдвигая курсор ДО выполнения.
48. **Egern нормализует cron**: `*/5 * * * *` → `0 */5 * * * *`.
49. **При коммите возможен мусор в хвосте файла** (случалось трижды, сборка
    падала). Защита — **хвостовой страж**: файл заканчивается строчным
    комментарием без перевода строки. После push — всегда проверять хвост и
    импортировать модуль.

---

## ОТКРЫТЫЕ ХВОСТЫ (по убыванию важности)

1. **Адресуется ли узел из `proxies` через `ctx.http`** — шаги T2/T3 ревизии k4-8.
   От этого зависит поузловой спидтест при миграции.
2. **Достать `SUBSCRIPTION_URL` и `SUB_HWID` у провайдера** (вывод 37) — единая
   точка отказа всего контура, к Egern отношения не имеет.
3. **Вернуть cron скрипта стенда на `*/5`** — сейчас `* * * * *`.
4. **Полевая проверка QR** на k1 (кнопка «QR», в шапке `панель p2`).
5. **`RH-Главный` объявлена как `select`, а не `fallback`** в `routehub.conf`.
   Решение Дианы 2026-08-07: оставить, вернуться позже.
6. **Кнопка выбора клиента в панели** — ссылка на `/admin` другого контура.
7. **Личные исключения `[Rule]`** — Диана правит сама.
8. **Полевая проверка звонков** на k1; **Этап J (TikTok)**; **Этап G**.
9. **Скилл `routehub`** — заморожен до решения по Egern.

---

## ЛИЧНЫЕ ИСКЛЮЧЕНИЯ (растущий список — блок вверху `[Rule]`)
Домены из whitelist-списков forg-lib, которые без обхода под whitelist НЕ работают.
**Ведёт Диана вручную.**
- `whoosh.bike` — проверено: в whitelist, без обхода не работает.
- `samokat.ru` — сейчас стоит `DIRECT`.

---

## ГЛАВНЫЕ ИСТОЧНИКИ
- Loon docs: `https://nsloon.app/docs/` (перевод — `ДОКУМЕНТАЦИЯ_LOON_RU.md`).
- **Egern docs — читать напрямую, не по выжимкам:**
  `https://egernapp.com/docs/configuration/policy_groups`, `/proxies`, `/rules`,
  `/scriptings`, `/widgets`, `/dns`, `/modules`, `/example`,
  `https://egernapp.com/docs/javascript-api`, `/docs/faq`, `/docs/url-scheme`.
- Cloudflare Wrangler: `https://developers.cloudflare.com/workers/wrangler/configuration/`.
- РФ-DIRECT/whitelist: `forg-lib-lov/roscomvpn-shadowrocket`. Прочие:
  blackmatrix7/ios_rule_script, viewer12/OverseasAI.list, misha-tgshv.

---

## С ЧЕГО НАЧАТЬ В ЭТОМ ЧАТЕ
1. Прочитать этот файл.
2. Текущая работа — **блок 4, шаг 4.4**. По Egern СНАЧАЛА читать
   `ЭТАП_K_ШАГ_4.4_РЕЗУЛЬТАТЫ.md` в ветке `egern` — там всё измеренное.
   Остальные документы шагов — только если нужны подробности.
3. Живой конфиг/код менять ТОЛЬКО с согласия Дианы; после push — сверка
   diff/md5 и запрос к живому эндпоинту.
4. Несколько блоков в одном чате допустимы, но каждый фиксируется сразу.
