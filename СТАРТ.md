# 🚦 СТАРТ — RouteHub (читать ПЕРВЫМ в каждом новом чате)

> Точка входа: на каком мы этапе, где файлы, что решено. Главный документ —
> `ПЛАН_РЕАЛИЗАЦИИ_v3.md`. Этот файл — навигатор поверх него.
> Обновлён 2026-08-12 (conf C-draft-**40** / боевой worker **v1.9.3** / панель **p2** /
> стенд Egern **e0.6.0**, скрипт стенда **k4-16** / пробы Stash **ST5**, Loon **L8**).
>
> **ГЛАВНОЕ ЗА ПОСЛЕДНЮЮ СЕССИЮ:**
> 1. **Блок 6 ЗАКРЫТ.** Тест-адрес исправлен во ВСЕХ десяти местах:
>    девять в `routehub.conf` (C-draft-40) и одно в `routehub-worker.js` (v1.9.3,
>    генератор AI-групп). Сверено по задеплоенному бандлу.
> 2. **НАЙДЕНО УПРАВЛЕНИЕ В STASH.** Через локальный контроллер работает
>    переключение группы, смена режима, журнал соединений, WebSocket.
> 3. **Loon и Egern проверены той же методикой** — контроллера нет ни у кого.
> 4. Два наших утверждения оказались неверными (см. «ИСПРАВЛЕНО»).

---

## 🔴 ТРЕБУЕТ РЕШЕНИЯ ДИАНЫ

1. **Направление развития** — три варианта (подробно ниже):
   А — остаться на Loon и снять костыли; Б — перейти на Stash;
   В — гибрид (Loon боевой + Stash как стенд вместо Egern).
2. **Три сотовые группы сидят на платном обходе** (выгрузка L8, ниже).
   Ждём сутки после правки тест-адреса — вернутся ли сами.
3. **Достать `SUBSCRIPTION_URL` и `SUB_HWID`** у провайдера — без них Stash
   не получит узлов, и дальше пустого профиля проверки не уйдут.

---

## ТЕКУЩИЙ СТАТУС

- **Этапы A, B, C, D — ЗАКРЫТЫ.** E — собран. **Блок 6 — ЗАКРЫТ.**
- **Боевой Worker** `https://routehub.proton4iker.workers.dev` — **v1.9.3**, панель p2.
  Авто-деплой при push в `main`. Проверять через `workers_get_worker_code`
  (служебного `/health` у боевого Worker НЕТ, есть только `/admin`).
- **Стенд Egern** `https://routehub-egern.proton4iker.workers.dev` (e0.6.0, k4-16),
  есть `/health`. **Исследование Egern ЗАВЕРШЕНО ОКОНЧАТЕЛЬНО.**
- **Стенд Stash** — `routehub-stash-probe.yaml` (ветка `main`), скрипт вшит в конфиг.
  Узлов в нём нет, боевой контур не затрагивает.
- **Хранилище — D1**, откат на KV невозможен. **k1 и k2 активны**, k3 — стенд.

---

## 🆕 ИТОГИ ПРОБ ПО ЧЕТЫРЁМ КЛИЕНТАМ (2026-08-12)

Методика одинаковая для трёх: сплошная опись имён, опись членов объектов,
поиск локального контроллера по портам и путям, попытка записи.

| | Loon 3.5.0 | **Stash 3.4.1** | Egern 2.20.0 | Shadowrocket |
|---|---|---|---|---|
| Локальный контроллер | нет (9 портов) | **есть, запись 204** | нет (9 портов) | не проверено |
| `$environment` | ОТСУТСТВУЕТ | 7 ключей, в т.ч. адрес+токен | 4 поля, пустышка | ? |
| Переключение политики из кода | свои группы | **любые, через API** | НЕТ | `$httpAPI`? |
| Плагины/скрипты из кода | **ДА** | нет | нет | нет |
| Журнал соединений | нет | **да (~19 КБ)** | нет | нет |
| Замер задержки ядром | нет | **да** | нет | нет |
| WebSocket | не соединяется | **открывается** | работает | нет |
| Глобальных имён | 971 | 1062 | 959 | 92 |
| Миллион итераций | 12 мс | 5 мс | 7 мс | 40 мс |

Подробности — `ЭТАП_K_STASH.md` (ветка `main`).

### Контроллер Stash — карта (проверено 29 путей)

Адрес `http://127.0.0.1:9090`, порт прокси ядра 7890. Ключ из
`$environment['controller-authorization']` передаётся в `Authorization` КАК ЕСТЬ
(уже содержит `Bearer `). Без ключа — 401 на всё.

| Путь | Что даёт |
|---|---|
| `GET /` | версия, архитектура, deviceId, порты |
| `GET /proxies` и `/proxies/{имя}` | группы: `all`, `now`, `type` |
| **`PUT /proxies/{имя}`** | **выбор члена группы → 204** |
| `GET /proxies/{имя}/delay` | штатный замер (без сети — 503, не 404) |
| `GET /configs`, **`PATCH /configs`** | режим ядра, смена → 204 |
| `GET /rules`, `/connections`, `/providers/proxies` | правила, соединения, наборы |
| `ws://…/traffic?token=` | поток `{"up":0,"down":0}` |

НЕ СУЩЕСТВУЮТ (404): `/version`, `/status`, `/api*`, `/v1*`, `/policies`,
`/groups`, `/scripts`, `/tiles`, `/ui`, `/dns/query`.

---

## 🆕 ИСПРАВЛЕНО (было записано неверно)

1. **У Loon ЕСТЬ цепочки узлов (Proxy Chain)** — документация описывает прямо.
   Скорость по самому медленному звену, трафик тратится на КАЖДОМ узле.
2. **У Loon ЕСТЬ правила по типу сети** — три способа: ssid-группа, скрипт,
   `ssid-trigger` (значения `direct`/`proxy`/`rule`, отдельно `cellular` и `default`).
   Наши дубли групп `-W`/`-C` — наше решение, а не вынужденная мера.

---

## 🆕 ЖИВАЯ ВЫГРУЗКА `policy_select` (L8, k1)

| Группа | Выбрано |
|---|---|
| RH-AI-C | 🇩🇪 Германия **[Обход - МТС]** |
| RH-АВТО-C | 🇩🇪 Германия **[Обход - Мегафон]** |
| RH-Звонки-C | 🇩🇪 Германия **[Обход - МТС]** |
| RH-AI-W | 🇩🇪 Германия [VPN] |
| RH-АВТО-W | 🇪🇪 Эстония [VPN] |
| RH-Звонки-W | 🇫🇮 Финляндия [Игры] |
| RH-Все | 🇷🇺 Россия [VPN] #1 |
| RH-Главный → RH-АВТО, RH-RU → RH-Обход | |

Все три сотовые группы — на платном обходе, хотя по замерам 11.08 обычные
узлы дают 89–99 мс против 388–505 у обхода.

---

## ТРИ ВАРИАНТА РАЗВИТИЯ

**А — остаться на Loon, снять костыли.** Worker присылает скрипту имена
узлов, скрипт назначает их через `setSelectPolicy`. Убирает управление
через порядок узлов в подписке — самый хрупкий механизм системы.
Цена: один скрипт и часть Worker. Дни.

**Б — перейти на Stash.** Получаем управление, журнал соединений, замер
задержки ядром, живой дашборд, работу скриптов под whitelist без внешней
загрузки. Теряем: `[Argument]` (галочки в UI), управление плагинами
из кода. Цена: Worker учится отдавать Clash YAML, скрипты переписываются.

**В — гибрид (рекомендация).** Loon остаётся боевым; Stash занимает место
стенда вместо Egern (через его журнал видно то, что в Loon невидимо).
Параллельно делается ВАРИАНТ А — его работа нужна в ОБОИХ сценариях.

---

## КАК СО МНОЙ РАБОТАТЬ (стиль)

- Академичный русский, без «воды». Только проверенные факты; нет данных —
  «не могу подтвердить». Источники не выдумывать.
- Ошибки признавать сразу и править записи в документах (см. «ИСПРАВЛЕНО»).
- Одиночное измерение — не вывод. Нужна серия и медиана без первого замера.
- **Без согласия Дианы живой конфиг/код не менять.**
- `СТАРТ.md` и `ПЛАН_РЕАЛИЗАЦИИ_v3.md` Диана перекладывает в «Файлы проекта» вручную.
- **`routehub-worker.js` вырос до 77 КБ — инструментом его БОЛЬШЕ НЕ ПЕРЕДАТЬ**
  (не помещается в один ответ). Мелкие правки — вручную через GitHub;
  крупные — только после разбиения на модули.

---

## ФАЙЛЫ ПРОЕКТА

### A. В «Файлах проекта» (грузятся всегда)
- `СТАРТ.md` — этот файл.
- `ПЛАН_РЕАЛИЗАЦИИ_v3.md` — этапы, журнал решений, каталог списков.
  (Серверная часть устарела — теперь Worker+D1. Раздел F про обходные узлы —
  опровергнут замерами 11.08.)

### B. В репозитории — читать инструментом ПО НАДОБНОСТИ
> `get_file_contents`, owner=`spxload`, repo=`routehub`. **Ветку указывать.**

| Файл | Ветка | Когда читать |
|---|---|---|
| `ЭТАП_K_STASH.md` | `main` | **итоги проб ST1–ST5, карта контроллера** |
| `СРАВНЕНИЕ_КЛИЕНТОВ_И_WHITELIST.md` | `main` | замеры под whitelist |
| `ЭТАП_K_EGERN_СПРАВОЧНИК.md` | `egern` | вся документация Egern |
| `ЭТАП_K_EGERN_ЛАЗЕЙКИ.md` | `egern` | 8 закрытых направлений |
| `ЭТАП_F_ЗАМЕТКИ.md` | `main` | модель «слабый DIRECT» |
| `ДОКУМЕНТАЦИЯ_LOON_RU.md` | `main` | синтаксис Loon (**3.3.9 — устарела**) |
| `КАТАЛОГ_СПИСКОВ.md` | `main` | списки правил |

### C. Код
**Ветка `main` (боевое):** `routehub-worker.js` **v1.9.3**, `routehub-admin.html` (p2),
`routehub.conf` **C-draft-40**, `tests/*`, `routehub-speedtest.js` v0.6.1,
`routehub-netwatch.js` v0.5.0, `routehub-viewer.js` v0.6.3, `routehub-rkn.js` v0.2.0,
`routehub-dash.js`/`.html`, `routehub-dashcache.js`, `RouteHub-Dash.plugin`.

**Пробы (только чтение):** `routehub-probe-loon.js` (L6b),
`routehub-probe-loon-api.js` (**L7** — поиск контроллера),
`routehub-probe-loon-config.js` (**L8** — getConfig, $loon, тайминги),
`routehub-probe-stash.js` (ST1), `routehub-stash-probe.yaml` (**ST5**, скрипт вшит),
`RouteHub-Probe.stoverride`, `routehub-probe-shadowrocket.js` (SR3).

**Ветка `egern` (стенд):** `routehub-egern-worker.js` e0.6.0,
`routehub-egern-k4.js` (**k4-16**). **В `main` не сливать.**

---

## ГЛАВНЫЕ ВЫВОДЫ (зафиксировано)

1. **Выбор узла делает fallback**, не скрипт. Порядок узлов задаёт Worker.
2. **Loon ПРОБИВАЕТ DIRECT в fallback** → модель «слабый DIRECT».
3. **РФ-whitelist домены → жёсткий DIRECT.**
4. **СТРАНА УЗЛА — ИЗ ИМЕНИ**, не из GeoIP.
5. **Под whitelist Worker достижим только через обход.**
6. **Приоритет правил Loon:** доменные раньше IP; локальные > подписка.
7. **Дашборд = «пульт»**, Worker сам в телефон не стучится.
8. **Логировать незагрузившиеся домены не умеет ни один клиент** —
   **ПОД СОМНЕНИЕМ:** `/connections` в Stash отдаёт журнал (19 КБ), метаданные
   ещё не разобраны.
9. **При пасте больших файлов сверять после коммита** (md5/diff).
10. **Лимиты Cloudflare (free):** D1 100 000 строк/сутки, Workers 100 000 запросов.
13. **Порядок узлов в подписке — ОДИН на все фильтры.**
16. **Состав подписки читать из `sub_cache`**, не из памяти.
28. **Классификация узлов — по слову в скобочном теге**, не по значку.
44. **Управление политиками из скрипта на Egern НЕВОЗМОЖНО** — подтверждено
    ДВАЖДЫ: сплошной описью 959 имён и поиском контроллера (k4-16).
52. **`auto_update` в Egern СОБЫТИЙНЫЙ, не по таймеру.**
54. **Перебор ГЛОБАЛОВ ПО СПИСКУ ИМЁН — негодная методика.**
55. **В Loon `$httpClient` БЕЗ `node` идёт ПО ПРАВИЛАМ** — для 127.0.0.1 задавать
    `node: 'DIRECT'` явно.
57. **Доставка отчётов:** Loon — буфер обмена через `$notification` с
    `clipboard`; Stash — хранилище + консоль + плитка; Egern — POST на приёмник.
58. **🆕 Stash: управление есть — через локальный контроллер**, адрес и ключ —
    в `$environment`. Запись подтверждена кодом 204.
59. **🆕 У Loon контроллера НЕТ** (9 портов, мгновенный отказ), `$environment`
    отсутствует. **У Egern — тоже нет**, `$environment` = 4 поля (surge-version 5.8.3).
60. **🆕 `all_buildin_nodes` в Loon — ЭТО НЕ УЗЛЫ**, а 12 встроенных политик
    (DIRECT, REJECT и варианты). **Список узлов скрипту НЕДОСТУПЕН** —
    подтверждено, не опровергнуто.
61. **🆕 `$loon` — СТРОКА** «iPhone14,7 26.5.2 3.5.0(975)», не объект.
62. **🆕 `$config` в Loon — ДЕВЯТЬ методов** (есть и `getSubPolicies`, и
    `getSubPolicys`); `$persistentStore` умеет `remove`; `$utils` — geoip, ipasn,
    ipaso, ungzip, ungzipTest; `$network` ОТСУТСТВУЕТ.
63. **🆕 Таймеры в Loon точны** — медиана дрейфа 5 мс на шести интервалах.
    В Stash наблюдалось 120 с при бюджете 33 — **ОДИН замер, не вывод**, нужна серия.
64. **🆕 В Stash ТРИ способа адресовать узел** для запросов скрипта: заголовок
    `X-Stash-Selected-Proxy`, строка `$httpClient.globalProxy`, свёрнутая панель.
    Системный трафик переключается ТОЛЬКО через контроллер.
65. **🆕 В Stash скрипт можно вшить в конфиг** полем `payload` — единственный
    рабочий способ под whitelist. URL-схемы запуска скрипта НЕТ (есть
    install-config, install-override, install-icon-set, start, stop, toggle).
    Ручной запуск — ПЛИТКА (`$script.type` = `tile`); `$argument` в плитке НЕТ.
66. **🆕 Типы групп Stash:** select, url-test, fallback, load-balance
    (consistent-hashing / round-robin), ssid, relay (устарел → `dialer-proxy`).
    **smart-группы НЕТ** (есть у Surge и Egern). consistent-hashing даёт sticky
    без скриптов — штатное решение принципа №3.
67. **🆕 Боевой Worker вырос до 77 КБ** — инструментом не передаётся.
    Разбиение на модули — отдельная задача.

---

## ОТКРЫТЫЕ ХВОСТЫ

1. **Не проверено в Stash (по убыванию важности):**
   - **`SCRIPT`-правила** (`SCRIPT,<имя>,POLICY`) — маршрутизация, вычисляемая
     скриптом на каждое соединение. Может заменить половину логики групп;
   - метаданные `/connections` — виден ли домен (вывод 8);
   - доступен ли контроллер С ДРУГОГО устройства сети (это был бы канал
     управления НАПРЯМУЮ с Worker);
   - замер задержки на ЖИВОМ узле; серия таймингов;
   - `rule-providers`, on-demand connection, DNS; Ярлыки iOS и виджеты.
2. **Не проверено в Loon:** типы скриптов кроме `generic` (в `cron` и
   `http-request` среда может отличаться); `load-balance` с PCC (sticky без
   скриптов); ssid-группы и `ssid-trigger`; Proxy Chain; `resource-parser`;
   URL-схемы.
3. **Достать `SUBSCRIPTION_URL` и `SUB_HWID`** — единая точка отказа; без них
   нет ни миграции, ни полноценного стенда Stash.
4. **Shadowrocket при активном туннеле** — `$httpAPI`, `useServer`. Выбыл по
   другой причине: под whitelist НЕ грузит тела скриптов.
5. Полевая проверка QR и звонков на k1; Этап J (TikTok); Этап G.
6. **`RH-Главный` объявлена `select`, а не `fallback`** — решение отложено.

---

## ЛИЧНЫЕ ИСКЛЮЧЕНИЯ (блок вверху `[Rule]`, ведёт Диана)
- `whoosh.bike` — в whitelist, без обхода не работает.
- `samokat.ru` — сейчас `DIRECT`.

---

## ГЛАВНЫЕ ИСТОЧНИКИ
- Loon: `https://nsloon.app/docs/`
- Stash: `https://stash.wiki/en` (разделы Manage Scripts, Syntax & Interface,
  Scheduled Tasks, Override, Tile, Proxy Groups, Rule Types, URL Schema)
- Egern: `https://egernapp.com/docs/`
- РФ-DIRECT/whitelist: `forg-lib-lov/roscomvpn-shadowrocket`

---

## С ЧЕГО НАЧАТЬ В ЭТОМ ЧАТЕ
1. Прочитать этот файл.
2. Если работа по клиентам — читать `ЭТАП_K_STASH.md` (ветка `main`).
3. **Живой конфиг/код менять ТОЛЬКО с согласия Дианы**; после push — сверка.
4. Пробы кладутся в репозиторий и никогда не пишут в боевую маршрутизацию.
