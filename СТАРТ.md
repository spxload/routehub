# 🚦 СТАРТ — RouteHub (читать ПЕРВЫМ в каждом новом чате)

> Этот файл — точка входа. Прочитав его, ты знаешь: на каком мы
> этапе, какие файлы где лежат, что уже решено, куда смотреть.
> Главный документ проекта — `ПЛАН_РЕАЛИЗАЦИИ_v3.md`. Этот файл —
> навигатор поверх него.

---

## ТЕКУЩИЙ СТАТУС

- **Сейчас идёт:** Этап B (серверная часть) — близок к завершению.
- **Сделано:** Этап A (проверки на устройстве) — закрыт.
- **Дальше по порядку:** C → D → H → E → F → I → J → G.
- **Открытый вопрос Этапа B:** внедрять ли точные маркеры эталона
  lmc999 (ChatGPT через compliance/cookie_requirements, Claude
  через редирект, Gemini через маркер 45631641) — Диана не успела
  выбрать объём. Спросить при возврате к B.

---

## КАК СО МНОЙ РАБОТАТЬ (стиль)

- Академичный русский, без «воды» и вводных фраз.
- Только проверенные факты. Нет данных — «не могу подтвердить».
- Источники не выдумывать. Уточнения — списком.
- Главный принцип: ПРОСТОТА И ПРЕДСКАЗУЕМОСТЬ важнее функций.
- Экономия токенов: один этап = один чат. Чат раздувается —
  завершаем этап, переходим в новый.

---

## ФАЙЛЫ ПРОЕКТА — ЧТО ГДЕ

### В «Файлах проекта» (видны каждому чату)
- `СТАРТ.md` — этот файл, навигатор и статус.
- `ПЛАН_РЕАЛИЗАЦИИ_v3.md` — ГЛАВНЫЙ документ. Всё: этапы, факты,
  архитектура, журнал решений, каталог списков (Прил. 1), сверка
  с документацией Loon (Прил. 2).
- `ДОКУМЕНТАЦИЯ_LOON_RU.md` — документация Loon на русском
  (версия 3.3.9). Для любых вопросов по синтаксису.
- `ИССЛЕДОВАНИЕ_GITHUB.md` — выводы исследования открытых решений
  по 10 направлениям (что брать / на рассмотрение / не брать).
- `КАТАЛОГ_СПИСКОВ.md` — каталог проверенных списков правил.
- `ЭТАП_A_РЕЗУЛЬТАТЫ.md`, `ЭТАП_B_РЕШЕНИЯ.md` — зафиксированные
  результаты пройденных этапов.

### В репозитории GitHub `spxload/routehub` (видит только сервер)
> ВАЖНО: новый чат НЕ видит файлы репозитория автоматически.
> Если нужен код из репо — попросить открыть через GitHub-инструмент.
- `routehub-server.py` — тестировщик узлов + рейтинг (Этап B).
- `routehub-geo.py` — консолидация гео (голосование + MaxMind).
- `routehub-config.json` — конфиг сервера.
- `diag_ai.py` — диагностика AI на узлах.
- `.github/workflows/check.yml` — запуск раз в 2 ч.
  ⚠️ Workflow правит ТОЛЬКО Диана вручную (у Claude нет прав).
- `routehub-ratings.json`, `routehub-history.json` — результаты.

---

## ГЛАВНЫЕ ВЫВОДЫ (зафиксировано, не пересматривать без причины)

1. **Сервер думает, телефон исполняет.** Тяжёлая проверка (AI,
   гео, тип IP, скорость) — на сервере раз в 2 ч → JSON-рейтинг.
   Loon читает рейтинг и переключает группы. Подтверждено
   исследованием — так делают все сильные проекты.

2. **AI-доступность без логина = только страна + тип IP +
   стабильность.** Реальный доступ под аккаунтом сервер НЕ видит
   (только телефон). Готового переключателя по ChatGPT/Claude нет
   нигде — пишем сами по паттерну «тест → setSelectPolicy».

3. **Проблема Gemini = тип IP.** Google жёстко банит датацентр/VPN
   даже в разрешённой стране. Определяется на сервере через ASN
   (X4BNet + is-vpn). Gemini хорош на residential, плох на
   datacenter.

4. **MaxMind — через зеркало P3TERX/GeoLite.mmdb БЕЗ ключа**
   (регистрация на maxmind.com Диане недоступна). Уже внедрено.

5. **РКН — два типа блокировок:** внутренние (РКН режет наружу →
   Re-filter) и внешние (сервисы сами режут RU-IP → no-russia-
   hosts). Это РАЗНЫЕ списки, нужны оба.

6. **TikTok — БЕЗ MITM.** Semporia/TikTok-Unlock отвергнут (MITM,
   устарел, риск бана). Вместо него — маршрутизация доменов TikTok
   на узел нужной страны без переписывания региона.

7. **Авторизованная проверка AI по узлам отвергнута окончательно**
   (impossible-travel-бан, небезопасно).

---

## ГЛАВНЫЕ ИСТОЧНИКИ (проверены)

- Документация Loon: `https://nsloon.app/docs/`
- Списки: blackmatrix7/ios_rule_script, viewer12/OverseasAI.list,
  1andrevich/Re-filter-lists, dartraiden/no-russia-hosts,
  misha-tgshv/shadowrocket-configuration-file.
- Гео/IP: P3TERX/GeoLite.mmdb, X4BNet/lists_vpn, josephrocca/is-vpn.
- Спидтест: faceair/clash-speedtest.
- Эталон AI-детекции: lmc999/RegionRestrictionCheck, xykt/IPQuality.
- Полные оценки — в `ИССЛЕДОВАНИЕ_GITHUB.md` и `КАТАЛОГ_СПИСКОВ.md`.

---

## ПРАВИЛО РАБОТЫ С ФАЙЛАМИ

Чаты НЕ видят переписку друг друга — только файлы проекта.
Решили что-то важное → ОБНОВИЛИ файл. Иначе следующий чат не
узнает. По завершении этапа: обновить этот `СТАРТ.md` (статус) +
план (отметить сделанное) + при необходимости создать
`ЭТАП_X_РЕЗУЛЬТАТЫ.md`.

---

## С ЧЕГО НАЧАТЬ В ЭТОМ ЧАТЕ

1. Прочитать этот файл (готово).
2. Открыть `ПЛАН_РЕАЛИЗАЦИИ_v3.md`, найти раздел текущего этапа.
3. Если нужен код — открыть из репозитория `spxload/routehub`
   через GitHub-инструмент.
4. Расписать шаги этапа, выполнять, по ходу обновлять файлы.
