# 🧪 ЭТАП K, ШАГ 4.3 — ПОЛЕВЫЕ ПРОВЕРКИ НА k3

> Ветка `egern`. Предыдущий шаг и его итог — `ЭТАП_K_ШАГ_4.2.md` (раздел 7).
> Порядок работ задан Дианой 2026-08-08: сначала правка профиля (K1/K2),
> затем K7, затем K9.
> **Итог шага — раздел 5. K2, K7, K9 закрыты; открыт только K1 (ждёт whitelist).**

---

## 0. ИСХОДНОЕ СОСТОЯНИЕ

- Worker стенда **e0.2.4**, база `routehub-egern-db`, ключ `k3`.
- `GET /health` 2026-08-08: `db: ok`, `kv_keys: 2`, `sub_nodes: 69`,
  `sub_age_min: 45`. Стенд читает свою базу; ключа `settings` нет — код к нему
  не обращается (фаза токенов у стенда зашита, переключать нечего).
- Cron у стенда не заводится, `sub_cache` обновляется **лениво**, запросом
  `/nodes` или `/profile`. Egern тянет `/nodes` раз в `update_interval` = 3600 с,
  этого достаточно.
- Профиль принят устройством 2026-08-08: 5 групп, 52 узла в обычном пуле,
  17 в обходе, предупреждений нет.

---

## 1. ПРАВКИ ПРОФИЛЯ ПОД ПРОВЕРКИ K1/K2

### 1.1 Причина
Проверки K1 и K2 сформулированы через член `DIRECT` внутри `fallback`
(K1 — пробивает ли Egern такой член под whitelist, K2 — применяется ли к нему
`latency_test_url`). В профиле e0.2.2 **ни одна группа члена `DIRECT` не
содержала**: `RH-RU` = [`RH-Проба-Обычная`, `RH-Обход`], `DIRECT` из класса B
убран намеренно. То есть проверки не выполнялись не из-за отсутствия whitelist,
а из-за отсутствия предмета проверки.

Вопрос принципиальный: модель «слабый DIRECT» (вывод 2 в `СТАРТ.md`) —
основание маршрутизации класса B в Loon. Переносима она на Egern или нет —
одна из развилок решения «мигрируем / остаёмся».

### 1.2 Две диагностические группы
```
e0.2.3:  fallback RH-Проба-DIRECT   = [DIRECT, RH-Обход]
                  latency_test_url = http://cp.cloudflare.com/generate_204
e0.2.4:  fallback RH-Проба-DIRECT-2 = [DIRECT, RH-Обход]
                  latency_test_url = http://192.0.2.1/generate_204
```
Обе **ни одним правилом не используются**: `rules` не менялись, трафик в них
не попадает, маршрутизация прежняя. Расход — пробы раз в 120 с.

`192.0.2.1` — TEST-NET-1 (RFC 5737), в интернете не маршрутизируется, поэтому
недостижим и напрямую, и через любой узел.

### 1.3 Логика различения
Профиль задаёт два адреса пробы: `direct_latency_test_url` верхнего уровня
(msftconnecttest.com, из РФ достижим всегда) и `latency_test_url` у группы.
Первая группа их не различает: `cp.cloudflare.com` и `msftconnecttest.com` —
CDN с близкими точками присутствия, из РФ оба дают десятки миллисекунд.
Вторая группа различает однозначно: отказ у члена `DIRECT` возможен только
если применён групповой адрес.

### 1.4 Проверено до импорта
Генератор прогнан YAML-парсером (`js-yaml`, Node 22) в обоих вариантах:
7 групп в объявленном порядке, обе диагностические = `["DIRECT","RH-Обход"]`,
7 правил, `default → RH-Главный`. В `&safe=1` `latency_test_url` снимается со
всех групп, поэтому K2 проверяется только на обычном варианте. Файлы после
push сверены по `raw`: `RE_BYPASS` не удвоился, версии `e0.2.3` и `e0.2.4`.

---

## 2. ПРОТОКОЛ K7 — СОВМЕСТИМОСТЬ СПИСКОВ С `rule_set`

Шесть наборов в профиле:

| № | Список | Политика |
|---|---|---|
| 1 | forg-lib `whitelist-domains.list` | `DIRECT` |
| 2 | forg-lib `hxehex-whitelist.list` | `DIRECT` |
| 3 | forg-lib `whitelist-ips.list` | `DIRECT` |
| 4 | forg-lib `category-ru.list` | `RH-RU` |
| 5 | forg-lib `apple.list` | `RH-RU` |
| 6 | misha-tgshv `domains_banking.list` | `RH-RU` |

Проверка в два слоя: загрузка всех шести (число правил, ошибки разбора) и
применение на живом трафике — whitelist-домен → `DIRECT`, банковский →
`RH-RU`, зарубежный → `RH-Главный`.

## 3. ПРОТОКОЛ K9 — ЭКСПОРТ ЗАПИСЕЙ ТРАФИКА

Экран «Хранилище» приложения, а при отсутствии выгрузки там — экран запросов
и Analytics (действия «поделиться», «экспортировать», «сохранить»).
Практический смысл — годится ли штатный экран как замена локальному дашборду
`http://rh.box` (заморожен, ТЗ в `ЭТАП_K_EGERN.md`, 6.11).

## 4. ЧТО ОСТАЁТСЯ ЗА ПРЕДЕЛАМИ 4.3

K1 — ждёт реального whitelist. K3 — по `СТАРТ.md` тоже привязан к whitelist,
но провоцируется и без него; процедура пишется отдельно по запросу.
K5 — после появления `smart` на шаге 4.4. K4 и K6 — шаг 4.4.

---

## 5. РЕЗУЛЬТАТЫ (2026-08-08, Egern 2.20.0, k3)

| Проверка | Итог |
|---|---|
| импорт e0.2.3 / e0.2.4 | **принят**; `DIRECT` допустим как член `policies` у `fallback` |
| K2 | **закрыт положительно** — см. 5.2 |
| K7 | **закрыт положительно** — см. 5.1 |
| K9 | **закрыт отрицательно** — см. 5.3 |
| K1 | открыт, ждёт реального whitelist; группа `RH-Проба-DIRECT` стоит на устройстве |

### 5.1 K7 — списки переносятся без конвертации
Шесть наборов загрузились, три пробы дали ожидаемые политики:
whitelist-домен → `DIRECT`, банковский → `RH-RU`, зарубежный → `RH-Главный`.
Существенно, что принят и `domains_banking.list` — список написан под
Shadowrocket. Значит набор списков боевого `C-draft-39` переносится на Egern
как есть; отдельного слоя конвертации на Worker'е не потребуется.

### 5.2 K2 — групповой `latency_test_url` применяется к члену `DIRECT`
Наблюдение на устройстве:

| Группа | Адрес пробы | `DIRECT` |
|---|---|---|
| `RH-Проба-DIRECT` | cp.cloudflare.com | **36 мс** |
| `RH-Проба-DIRECT-2` | 192.0.2.1 (TEST-NET-1) | **таймаут** |

Отказ во второй группе при работающей первой возможен только в одном случае:
Egern тестирует член `DIRECT` **адресом, заданным у группы**, а не
`direct_latency_test_url`. Иначе обе группы показали бы одинаковые ~36 мс.

Следствие: **модель «слабый DIRECT» на Egern переносится.** Под whitelist
зарубежный адрес пробы перестанет отвечать напрямую, член `DIRECT` выпадет,
и `fallback` уйдёт на следующий член — то самое поведение, на котором в Loon
построен класс B (вывод 2 в `СТАРТ.md`). Это снимает главное архитектурное
возражение против миграции.

Что отсюда **не** следует: что `DIRECT` надо возвращать в `RH-RU`. Текущая
схема класса B (обычный узел → обход, без `DIRECT`) принималась отдельным
решением; теперь у неё появилась альтернатива, и выбор между ними — вопрос
шага 4.4, а не следствие этой проверки.

Судьба диагностических групп: `RH-Проба-DIRECT` остаётся до закрытия K1 —
она и есть инструмент наблюдения под whitelist. `RH-Проба-DIRECT-2` свою
задачу исчерпала и на шаге 4.4 удаляется: её пробы заведомо и постоянно
падают, это шум в журнале и в статистике групп.

### 5.3 K9 — выгрузки записей трафика нет
Экран «Хранилище» перечисляет занятый объём по категориям (кэш, журналы,
прочее) — ни просмотра записей, ни выгрузки файла. В остальных местах
приложения выгрузка также не найдена.

Следствия:
1. Наблюдаемость на Egern ограничена экранами устройства. Данные о трафике
   наружу не отдаются — ни в Worker, ни в файл.
2. Схема «телефон считает, Worker хранит» на Egern без скриптов не строится.
   Всё, что RouteHub сейчас собирает через `POST /speed`, при миграции
   упирается в K4 (скрипт на `http_request` без MITM) — а он ещё не проверен.
3. Вопрос о разморозке локального дашборда возвращается в повестку решения
   по миграции: штатный экран Analytics заменяет просмотр, но не заменяет
   сбор и хранение.

---

## 6. ЧТО ДАЛЬШЕ

Шаг 4.3 закрыт в той части, которая не требует whitelist. Остаток:
- **K1** — наблюдение за `RH-Проба-DIRECT` при первом реальном whitelist;
- **K3** — по запросу, отдельной процедурой;
- **шаг 4.4** — `smart` и `priorities` (с ними K5), `conditional` по SSID, DNS,
  ИИ-группы, звонки (K6), спидтест и K4, панель стенда; там же удаляется
  `RH-Проба-DIRECT-2` и решается судьба `DIRECT` в `RH-RU`.
