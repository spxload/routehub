# RouteHub

Маршрутизация трафика в Loon на iPhone: держит AI-сервисы на стабильных узлах,
обходит блокировки, экономит платный трафик обходных узлов. Подписка одна,
узлы раздаёт Cloudflare Worker, выбор внутри группы делает сам Loon.

**Состояние на 2026-08-16:** Worker `v1.9.8`, конфиг `C-draft-41`,
спидтест `v0.6.3`. Проверить живой Worker: `curl https://<worker>/version`.

## С чего начать

| Файл | Зачем |
|---|---|
| `CHANGELOG.md` | что менялось в Worker'е и конфиге, с причинами |
| `ADR-01_СХЕМА_КОНТУРОВ.md` | почему один код, но несколько Worker-сервисов |
| `ТЕХДОЛГ.md` | что накопилось и в каком порядке разбирать |
| `ЗАМЕРЫ_И_ВЕСА.md` | почему веса балла не рычаг, а замеры — да |
| `СВЕРКА_LOON_3.5.md` | что подтвердилось в документации Loon, что разошлось |

## Как это устроено

```
подписка Lastdep
      │
      ▼
Cloudflare Worker (routehub-worker.js + src/*.js)
   ├── ядро src/*.js          считает баллы, хранит в D1, кэширует подписку
   └── src/clients/loon.js    рендерит конфиг под синтаксис Loon
      │
      ▼
routehub.conf на устройстве  →  Loon выбирает узел внутри fallback-группы
      ▲
      └── скрипты устройства: speedtest (метрики), netwatch (смена сети),
          viewer, dash, dashcache, L10 (ручная проба контекста)
```

Стенды разворачиваются из этой же ветки через `[env.*]` в `wrangler.toml`:
`npx wrangler deploy --env stash`. Ветка на клиента больше не заводится.

## Актуальность документов

Часть документов описывает решения, которые уже заменены. Они оставлены как
история — но читать их как описание текущего состояния нельзя.

| Документ | Статус |
|---|---|
| `ЭТАП_E_ПРОГРЕСС.md` | **устарел**: `RH-Прямой = select` заменён в C-draft-25 на fallback-группы |
| `ИНСТРУКЦИЯ_ПРОЕКТА.md` | **устарела**: 58 узлов, старая раскладка файлов, отменённые запреты |
| `ДЛЯ_ДИАНЫ_инструкция_и_промпты.md` | **устарела**: вторая инструкция, расходится с первой |
| `ЭТАП_D_RESEARCH_ПРОМПТ.md` | исполнен, ценность историческая |
| `МИГРАЦИЯ_НА_WORKERS.md` | выполнено 2026-06-08 |
| `ДОКУМЕНТАЦИЯ_LOON_RU.md` | перевод версии **3.3.9**, на устройстве 3.5.0 — сверка в `СВЕРКА_LOON_3.5.md` |
| `ЭТАП_K_EGERN.md` и ветка `egern` | исследование закрыто: управления политиками из скрипта у Egern нет |

Актуальные: `CHANGELOG.md`, `ADR-01_СХЕМА_КОНТУРОВ.md`, `ТЕХДОЛГ.md`,
`ЗАМЕРЫ_И_ВЕСА.md`, `СВЕРКА_LOON_3.5.md`, `ЭТАП_K_STASH.md`,
`ЭТАП_K_STASH_СТЕНД.md`, `СРАВНЕНИЕ_КЛИЕНТОВ_И_WHITELIST.md`,
`ЭТАП_F_ЗАМЕТКИ.md`, `КАТАЛОГ_СПИСКОВ.md`.

## Разработка

```bash
git clone --depth 1 https://github.com/spxload/routehub.git
node --test "tests/*.test.js"          # 63 теста, CI нет намеренно
npx wrangler deploy --env="" --dry-run # боевое окружение
npx wrangler deploy --env stash --dry-run
```

`git push` из облачной сессии не работает (прокси, баг claude-code #76248) —
правки идут через GitHub API, по одному файлу, с обязательной сверкой чистым
клоном после заливки.

Боевой контур меняется только с согласия владельца: `routehub.conf`, Worker,
скрипты устройства. Записи в D1 и отвязка ключей — тоже.
