# ЭТАП K — СТЕНД STASH

> Заведён 2026-08-16. Схема — по **ADR-01**: код общий с боевым (ветка `main`),
> отдельный Worker-сервис через окружение `[env.stash]`, отдельная база D1,
> свой ключ устройства. Боевой контур (Loon, k1/k2, `routehub-db`) не
> затрагивается ни рантаймом, ни данными.

## Что уже готово

| Сущность | Значение | Кем |
|---|---|---|
| Окружение | `[env.stash]` в `wrangler.toml` ветки `main` | Claude, 16.08 |
| База D1 | `routehub-stash-db`, `6ba57fd5-c707-410c-a2ea-428156d06f88`, EEUR | Claude, 16.08 |
| Имя воркера | `routehub-stash` | — |
| Cron | отключён явно (`crons = []`) — стенду фоновое обновление не нужно | Claude, 16.08 |
| Ключ устройства | `k3` (в реестре, статус `free`) | — |

Проверено `wrangler deploy --dry-run` 16.08: боевое окружение видит
`routehub-db`, стенд — `routehub-stash-db`; тесты на чистом клоне `main` 58/58.

Код Worker'а на стенде тот же, что у боевого (модули `src/*.js`, v1.9.5). Это
сразу даёт стенду `/nodes`, панель и реестр устройств. Раздача конфига Stash в
YAML появится модулем `src/stash.js` — после шагов 1–3, их результат может
половину задуманного отменить.

Ветка `stash` осталась историческим срезом первого захода; после первого
успешного билда из `main` её нужно удалить, чтобы код не расходился.

## Что должна сделать Диана

### Разово, чтобы стенд ожил

1. **Cloudflare → Workers → Create → импорт `spxload/routehub`, ветка `main`.**
   Имя воркера — `routehub-stash`.
2. **Deploy command:** `npx wrangler deploy --env stash` (без флага
   развернётся боевой конфиг — это важно не перепутать).
3. **Секреты:** `SUBSCRIPTION_URL`, `SUB_HWID`, `ADMIN_KEY`. По ADR-01 их стоит
   сразу завести в **Secrets Store** и привязать к обоим воркерам — тогда
   следующему контуру вписывать нечего.
4. **Заодно у боевого воркера** сменить deploy command на
   `npx wrangler deploy --env=""`: в конфиге появились окружения, и верхний
   уровень лучше выбирать явно.
5. Проверить: `/admin` стенда открывается, в базе стенда появляется `devices`.

### Шаги проверки Stash — редакция 17.08, ВСЁ С ТЕЛЕФОНА

Прежняя редакция начиналась с `curl` к контроллеру телефона с ноутбука.
Ноутбука в проверках нет и не будет: всё, что ниже, делается на самом
iPhone, включая проверку доступности контроллера по локальной сети (шаг 6).
Порядок — от дешёвого к дорогому. Ни один шаг не выброшен.

**Шаг 1. Экран «Connections» в самом Stash.** Ничего включать не нужно.
Приложение показывает активные соединения и умеет доставать имя хоста
снифферингом: TLS Client Hello (SNI), QUIC/HTTP3 и HTTP-запросы внутри TCP
(Method, Host, URL) — release notes iOS. Рядом есть экраны **HTTP Inspection**
и **DNS Inspection**.
*Что закрывает:* вывод 8 — журнал незагрузившихся доменов — **без MITM и без
API вообще**. Если имена видны, задача, ради которой обсуждался MITM, решается
штатным экраном.

**Шаг 2. zashboard в Safari телефона** на `http://127.0.0.1:9090`, ключ —
`Authorization: Bearer <ключ>`. Контроллер на этом адресе уже отвечал в пробах
ST3–ST5 (замер 12.08, см. ниже), то есть шаг проверяет не доступность
контроллера, а годность самой панели.
*Что закрывает:* если заведётся — своей панели для Stash не пишем.
*Оговорка:* ключ API берётся в приложении, «More Settings». Он же
подставляется автоматически в «Tools → Remote Controller» между устройствами
одного iCloud.

**Шаг 3. `/connections` через панель.** Эндпоинт существует и отдаёт ~19 КБ
(замер 12.08). Открытый вопрос ровно один: видно ли там имя хоста, а не только
IP. Если шаг 1 уже показал имена в приложении, этот шаг становится
необязательным — он нужен, только если журнал хочется собирать автоматически.

**Шаг 4. `ssid-policy`** вместо пары групп `-W` / `-C`.
**Уточнение к прежней записи: отдельного типа группы `ssid` в Stash нет.**
Есть параметр `ssid-policy` у группы типа `select`, с зарезервированными
значениями `default` и `cellular`:

```yaml
- name: ssid-group
  type: select
  proxies: [ss1, ss2, ss3, DIRECT]
  ssid-policy:
    office: ss1
    home: ss2
    cellular: ss3
    default: DIRECT
```

Если ни `default`, ни `cellular` не заданы, переключение не срабатывает вовсе.
*Что даёт:* конфиг схлопывается вдвое, уходит `routehub-netwatch.js` и
дублирование узлов в `/nodes`. Нужен `src/clients/stash.js`, собирающий YAML.

**Шаг 5. `consistent-hashing`** для AI. Это не тип группы, а параметр
`strategy` у группы `load-balance`; документация прямо рекомендует его, чтобы
не менять IP часто и не будить защиту сервиса.
*Что даёт:* per-site sticky штатными средствами, без страновых тиеров.

**Шаг 6. Слушает ли контроллер локальную сеть — тоже с телефона.**
Ноутбук для этого не нужен: телефон обращается к самому себе по своему
LAN-адресу, а не по `127.0.0.1`. Порядок:

1. Настройки iOS → Wi-Fi → сеть → посмотреть **IP-адрес** телефона
   (например `192.168.1.42`).
2. В Safari открыть `http://192.168.1.42:9090/` — **без ключа**.
3. Читаем ответ:
   - **401** — контроллер слушает `0.0.0.0`, то есть доступен по локальной
     сети. Ключ просто не передан, это ожидаемо. Управление с других
     устройств возможно.
   - **«Не удаётся установить соединение»** — слушает только `127.0.0.1`,
     снаружи недоступен. Тогда включить в Stash **Allow LAN** и повторить.
   - **404 «page not found»** — соединение есть, путь другой; это тоже ответ
     «слушает» (стандартная страница Go-роутера, замер ST4).

Ключ передать из Safari нельзя — заголовок не задашь. Но для этого шага он и
не нужен: различаются именно отказ соединения и ответ сервера. Полноценные
запросы с ключом делает zashboard (шаг 2).

---

### Сверка: документация против наших замеров (17.08)

Вики `stash.wiki` прочитана целиком (42 страницы). Но **решающее в этом
проекте измерено пробами ST1–ST5**, а не вычитано: половина того, на чём
держится план, в документации отсутствует. Ниже обе колонки, и там, где они
расходятся, приоритет у замера.

| Факт | Документация | Наш замер | Итог |
|---|---|---|---|
| Управление выбором группы из скрипта: `PUT /proxies/{имя}` | нет | **204, ST3–ST5** | **есть**, и это главный вывод по Stash |
| `$environment['controller-url']`, `['controller-authorization']` | нет | **есть, ST2** | скрипт получает адрес и токен без настройки |
| Адрес контроллера `http://127.0.0.1:9090`, порт ядра 7890 | нет | **ST3** | есть |
| Авторизация: значение передаётся в `Authorization` как есть, уже с `Bearer ` | «Bearer ${secret}», ключ в «More Settings» | **ST3** | совпадает |
| `/connections` | нет | **200, ~19 КБ, ST4** | есть; **разобрано не было** — открытый вопрос ST7.1 |
| `/configs` PATCH → смена режима ядра | нет | **204, ST4** | есть |
| `ws://…/traffic?token=` | нет | **открывается, ST4** | есть |
| `/proxies/{имя}/delay` | нет | **503 без сети, ST4** | эндпоинт есть, на живом узле не мерян |
| Нет `/version`, `/status`, `/policies`, `/groups`, `/scripts`, `/ui` | — | **404, ST4** | отсутствие `/version` однажды приняли за «контроллера нет» |
| `$httpClient.globalProxy` | нет | **есть, строка, ST2** | недокументированный третий способ адресации узла |
| Выбор узла заголовком `X-Stash-Selected-Proxy` | есть | — | есть |
| **`$config` в среде** | — | **ОТСУТСТВУЕТ, ST2**: ровно шесть `$`-объектов | **SSID из скрипта не прочитать** — ключевое ограничение для ADR-02 |
| `$persistentStore.read / .write` | есть | есть, ST2 | есть |
| Cron-скрипты, только JS, требуют поднятого туннеля | есть | ST5 | есть |
| **`setTimeout` растягивается**: бюджет 33 с → 120 с факта | нет | **ST5** | логика с опорой на точное время ненадёжна |
| `$argument` в плитке | нет упоминания | **отсутствует, ST5** | ручной запуск — только плитка, без аргумента |
| Скрипт можно вшить в конфиг полем `payload` | есть | ST5 | под whitelist единственный рабочий способ |
| Экран «Connections» с именами хостов через сниффинг SNI / QUIC / HTTP | есть | не смотрели | проверяется шагом 1 |
| Экраны HTTP Inspection, DNS Inspection, Network Quality Test | есть | не смотрели | — |
| Встроенная веб-панель (`external-ui`) | нет | `/ui` → 404, ST4 | **нет** |
| `ssid-policy` у группы `select`, значения `default` и `cellular` | есть | не проверяли | проверяется шагом 4 |
| `consistent-hashing` — `strategy` группы `load-balance` | есть | не проверяли | — |
| Типы групп: `url-test`, `fallback`, `load-balance`, `select`, `relay` | есть | `Selector` в ответе `/proxies`, ST3 | согласуется |
| Override: массивы вставляются **в начало**, правка элементов не поддерживается | есть | ST5 (`.stoverride` применялся) | учитывать при сборке |
| Экспорт логов с устройства | нет | — | не нашли |

**Поправка к прежней записи об `SCRIPT`-правилах.** Синтаксис
`SCRIPT,<shortcut>,POLICY` существует, но `<shortcut>` — это **выражение на
Python** из секции `script.shortcuts`, возвращающее bool, а не JavaScript.
Доступны `network`, `host`, `dst_ip`, `dst_port`, `src_ip`, `src_port` и
функции `resolve_ip()`, `in_cidr()`, `geoip()`, `ipasn()`, `match_provider()`,
`match_geosite()`. Маршрутизировать по нашему композитному баллу через
`SCRIPT` нельзя. Прежняя формулировка «заменит половину логики групп» верна
только для маршрутизации по IP, ASN и geosite.

**Поправка о «нет управления политиками».** Это вывод по **Egern**, не по
Stash. В Stash управление есть, через локальный контроллер, и подтверждено
кодом 204. Если формулировка встретится применительно к Stash — она неверна.

### Костыли, которые снимаются (ADR-02)

Разбор в `docs/ADR-02_ГРУППЫ_STASH.md`. Коротко:

- **Пара `-W`/`-C` и скрипт-переключатель** — снимается `ssid-policy` у
  родительской `select`-группы. `routehub-netwatch.js` в контур Stash не
  переносится: его задача «флип групп по сети» исчезает как класс.
- **Дублирование узлов в `/nodes`** — снимается тем, что в Clash-YAML секция
  `proxies:` (описания узлов) отделена от `proxy-groups:` (порядок членов).
  Один и тот же узел стоит в группе Wi-Fi и в группе сотовой в разном
  порядке, а описан один раз. Цена: метрику больше нельзя вписывать в имя,
  её место — панель.

## Чего в Stash нет

- **smart-группы.** Замена — `consistent-hashing` плюс наш композитный балл,
  который в отличие от Surge учитывает ещё и скорость.
- **Нативного спидтеста.** Переносим свой скрипт.
- **Включения MITM из API.**
- Ручной запуск скрипта — только плитка (`$script.type = tile`), `$argument`
  в плитке нет.
- Маршрутизации JavaScript-скриптом: `SCRIPT`-правило принимает Python-выражение,
  не JS (см. поправку выше).

## Что известно про контроллер (ЭТАП_K_STASH.md, замер 12.08)

`http://127.0.0.1:9090`, порт ядра 7890. Без ключа — 401 везде.
Живые пути: `GET /`, `/proxies`, `/proxies/{имя}`, **`PUT /proxies/{имя}` → 204**,
`/proxies/{имя}/delay`, `GET /configs`, **`PATCH /configs` → 204**, `/rules`,
`/connections` (~19 КБ), `/providers/proxies`, `ws://…/traffic?token=`.
404: `/version`, `/status`, `/api*`, `/v1*`, `/policies`, `/groups`, `/scripts`,
`/tiles`, `/ui`, `/dns/query`.

## Почему база всё-таки отдельная

Реестр устройств хранит `nonce`. Общая база перепривязала бы боевые ключи k1/k2
(вывод 12). Экономии на общей базе нет: квоты D1 считаются на аккаунт, а не на
базу (5 млн чтений строк в сутки, 100 000 записей, 5 ГБ суммарно). Значит
объединение баз давало бы только риск — отклонено в ADR-01.

## Открытое по стенду

1. Видно ли имя хоста в `/connections` (ST7.1). Экран приложения имена
   показывает сниффингом — вопрос в том, отдаёт ли их API.
2. Замер задержки ядром (`/proxies/{имя}/delay`) — сравнить с нашим
   спидтестом: если совпадает, часть замеров можно отдать клиенту.
3. Нужен ли стенду свой конфиг-скрипт вообще, если zashboard закроет панель.
4. Слушает ли контроллер локальную сеть — шаг 6, с телефона.
5. Перенос подписки: Lastdep отдаёт формат Loon, Stash ест Clash-YAML.
   Преобразование ложится на `src/clients/stash.js`.
6. Проверки под ADR-02: срабатывает ли `ssid-policy` при уходе на сотовую;
   переваривает ли Stash один узел в двух группах в разном порядке; как часто
   он перечитывает конфиг и рвёт ли при этом соединения.
