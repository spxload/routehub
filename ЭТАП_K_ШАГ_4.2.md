# 🧱 ЭТАП K, ШАГ 4.2 — МИНИМАЛЬНЫЙ ПРОФИЛЬ СТЕНДА

> Решения зафиксированы 2026-08-07, до написания кода. Файл живёт в ветке
> `egern`. Основной документ этапа — `ЭТАП_K_EGERN.md` (ветка `main`);
> здесь только то, что уточняет или ОТМЕНЯЕТ его положения для шага 4.2.
> Состояние стенда после шага 4.1 — в `СТАРТ.md`.

---

## 0. ЧТО УЖЕ ЕСТЬ (шаг 4.1, выполнен 2026-08-07)

- Ветка `egern`, Worker **`routehub-egern`**, база **`routehub-egern-db`**
  (id `55ebdd96-f6d4-453a-891a-4662ff5366dd`, EEUR, таблица `kv` той же схемы,
  что в боевой; пустая).
- Заглушка `routehub-egern-worker.js` **e0.1.0**, единственный `GET /health`.
- Секреты стенда заведены Дианой типом Secret: `SUBSCRIPTION_URL`, `SUB_HWID`,
  `ADMIN_KEY`. **`ADMIN_KEY` совпадает с боевым** — сознательно; следствия
  см. п. 5.
- Боевой контур разведён: на стенде `/status`, `/admin`, `/config` → 404.

---

## 1. ТРИ ПОПРАВКИ К `ЭТАП_K_EGERN.md`

### 1.1 Фильтры групп — по слову в скобочном теге, НЕ по значку
В разделе 6.2 скелет написан по старому признаку: `filter: "🙏"` для обхода и
`^(?!.*🙏)` для обычных. **Значка 🙏 в подписке нет**: обходные узлы называются
`[Обход - <оператор>]`. Верный признак (вывод 28 в `СТАРТ.md`):

| Группа | Фильтр |
|---|---|
| обычные (`smart`) | `^(?!.*Обход)` |
| обход (`auto_test`) | `\[Обход` |

Цена ошибки, если перенести скелет буквально: `RH-Обход` окажется пустым, а
платные узлы попадут внутрь `smart` — сработает риск №1 раздела 10, причём
молча и с расходом платного трафика на пробы.

### 1.2 Состав подписки в файле устарел
Было «72 строки вместо 126». На 2026-08-07 — **69 узлов**: 43 `[VPN]`,
4 `[🌀 VPN]`, 5 `[Игры]`, 17 `[Обход - …]`. Считать из `sub_cache`, не из памяти.

Существеннее: `[🌀 VPN]` — прямые зарубежные локации, **под whitelist не
работают**. В `smart` они годятся, в `RH-Обход` — нет, в класс B пускать нельзя.

### 1.3 `conditional` в шаг 4.2 не входит
Он требует домашних SSID (открытый вопрос 2 раздела 9) и на проверки K1–K3, K5
не влияет. В минимальном профиле — одна `smart`-группа `RH-АВТО`. Разделение
сетей — шаг 4.4.

---

## 2. ОБЪЁМ ШАГА 4.2

### 2.1 Эндпоинты стенда
| Метод, путь | Назначение |
|---|---|
| `GET /health` | остаётся от 4.1: версия, состояние базы, число ключей |
| `GET /t/<token>/nodes?key=k3` | список узлов, **имена без разметки** |
| `GET /t/<token>/profile?key=k3` | YAML-профиль Egern |

Токен передаётся префиксом пути, как в боевом Worker'е. Фаза токенов у стенда
сразу жёсткая: запрос без токена — 403.

**Имена узлов не размечаются** (журнал решений этапа K): ни балла, ни скорости,
ни значков сети. Ранжирование ушло в `priorities`, а на шаге 4.2 `priorities`
ещё нет — узлы отдаются в порядке подписки.

### 2.2 Ключи в стендовой D1
`sub_cache`, `devices` (только `k3`), `settings`. Модель прежняя: один ключ =
один JSON-блок. Схема таблицы уже создана на 4.1.

### 2.3 Группы минимального профиля
```
smart      RH-АВТО            filter ^(?!.*Обход)
auto_test  RH-Обход           filter \[Обход, interval 3600
fallback   RH-Проба-Обычная   [RH-АВТО]
fallback   RH-RU              [RH-Проба-Обычная, RH-Обход]   ← класс B
fallback   RH-Главный         [RH-АВТО, RH-Обход]            ← класс C / FINAL
```
`interval 3600` у обхода — не оптимизация, а защита платного трафика: у `smart`
частота проб не настраивается, поэтому обход в `smart` не входит никогда.

### 2.4 Правила
- класс A (анти-VPN: forg-lib whitelist, банки ЦБ) → **жёсткий `DIRECT`**;
- класс B (прочее РФ) → `RH-RU`;
- FINAL → `RH-Главный`;
- звонки на этом шаге отдельной группы не получают, идут в `RH-Главный`
  (K6 проверяется после появления `RH-Звонки`, шаг 4.4).

### 2.5 ВНЕ объёма 4.2
ИИ-группы и sticky; `priorities` от Worker'а; спидтест и `POST /speed`;
вердикты «работает / не работает»; модули и персонализация; свой DNS; иконки;
админ-панель стенда. Всё это — 4.4 и дальше.

Обоснование границы: перечисленное упирается в открытые вопросы раздела 9
(DNS-серверы, SSID, формат имён, иконки) и не требуется ни для одной из
проверок K1, K2, K3, K5, K9. То есть восемь пунктов чек-листа закрываются
минимальным профилем, а решения по спорному откладываются до данных.

---

## 3. ЧТО ПРОВЕРЯЕТСЯ ПОСЛЕ 4.2

| # | Проверка | Чем закрывается |
|---|---|---|
| K1 | пробивает ли `fallback` член `DIRECT` под whitelist | `RH-RU`, ждёт реального whitelist |
| K2 | применяется ли `latency_test_url` к члену `DIRECT` | `RH-RU` |
| K3 | реагирует ли `fallback` на реальные отказы | `RH-Главный` |
| K5 | поведение `smart` за сутки | `RH-АВТО` |
| K9 | экспорт записей трафика | экран «Хранилище» приложения |
| K7 | совместимость списков с `rule_set` | правила класса A |

K4 (скрипт на `http_request` без MITM) и K6 (звонки) — после 4.4.
K8 и K10 закрыты 2026-08-07 без стенда.

---

## 4. ТРЕБОВАНИЯ К ГЕНЕРАТОРУ

- Имена узлов в regex **экранировать** (`^…$`, эмодзи, скобки `[VPN]`) —
  требование раздела 6.3.
- YAML собирать программно, а не склейкой строк «на глаз»: результат обязан
  парситься. Проверка перед коммитом — прогон сгенерированного профиля
  YAML-парсером и сверка состава групп со списком п. 2.3.
- Ничего не создавать в UI Egern руками (журнал решений этапа K): профиль
  генерирует Worker, ручные сущности дают расхождение.
- Логические правила: до 2.20 неподдерживаемое подправило ловило **весь**
  трафик. На стенде 2.20.0, но конструкции держать простыми.

---

## 5. СЛЕДСТВИЯ ОБЩЕГО `ADMIN_KEY`

Cookie сессии привязана к origin и имеет `SameSite=Strict`, поэтому у боевой
панели и панели стенда **разные сессии**: одинаковый ключ означает лишь
одинаковый пароль, входить надо на каждом контуре отдельно. Смена ключа
обнуляет сессии сразу на обоих.

Переключатель контуров внутри одной панели **не делаем как кросс-доменный
вызов**: `SameSite=Strict` cookie не отдаст, а единственная альтернатива —
хранить ключ в странице, что отменяет модель v1.9.1 (вывод 27). Решение:
кнопка-**ссылка** на `/admin` другого контура, без единого кросс-доменного
запроса. Делается вместе со следующей правкой панели, чтобы не тратить
перезаливку 34 КБ и подъём `PANEL_REV` на одну строку.

---

## 6. ОТКРЫТЫЕ ВОПРОСЫ, КОТОРЫЕ 4.2 НЕ ТРОГАЕТ

Из раздела 9 `ЭТАП_K_EGERN.md`: DNS-серверы, домашние SSID, разблокирующий DNS
для ИИ, формат имён узлов, иконки групп. Ответы нужны к 4.4, не раньше.

Отдельно, к Egern отношения не имеет: `SUBSCRIPTION_URL` и `SUB_HWID`
существуют в единственном нечитаемом экземпляре — секретами Cloudflare.
Восстановить их можно только у провайдера. Диане стоит достать их из личного
кабинета Lastdep и сохранить у себя вне зависимости от решения по Egern.
