# Передача дел: переезд в Claude Code (24.09.2026)

## Цель

RouteHub переезжает из Cowork в облачные сессии Claude Code; репозиторий —
единственное место правды. Готово, когда первая сессия Code выполнила
`studio/tasks/T-workflows.md`, а память проекта перенесена в
`studio/memory/` (см. «Память проекта» ниже).

## Сделано 23–24.09 (коммиты в `main`)

- `258f4b8` — `CLAUDE.md` и `MAP.md`.
- `f4c4d96` — роли `.claude/agents/`, `studio/`, бэклог, брифы.
- `5ae573e` — все роли на `model: opus`.
- `505851d`, `00fad17` — правила работы с Opus 5.5, схема «ветка → PR →
  Merge», скиллы `handoff`, `diagnosing-bugs`, `grill-me`, хук `guard-prod`
  (тестировщик: принято).
- `4cb1508`, `1bfd62e` — монитор контекста `context-watch` и скилл
  `context-audit`.
- `2e93230` — роль `ideator`, скиллы `prove-blocked`, `ideas`, тест формата
  ролей.
- Коммит с этим файлом — раздел «Идеи и проверка невозможного» в
  `CLAUDE.md`, строки `MAP.md`, инструмент `Skill` исполнителям.

## Проверено

`node --test tests/*.test.js` на чистом клоне `main`: 218 из 218, падений
нет (после коммита с этим файлом).

## Осталось — по порядку

1. `studio/tasks/T-workflows.md` — первая сессия Code.
2. `studio/tasks/T-private-repo.md`.
3. Тестировщик первой волны 22.09 — пункты бэклога 14, 41, 43, 44, 52:
   совпадает ли сделанное с запросом, тесты, мутации. Коммиты: 14 —
   `8e4076f`; 52 — `03ef02b`, `8e4076f`, `7deac66` в `main`; 41 и 43 —
   `1cdf7cf`, `493743d` в `stash-client`; 44 — проба L12 `e8b7f82` в `main`.
4. `CHANGELOG.md` — записи, не попавшие туда из-за размера файла: в `main` —
   проба L12 (`e8b7f82`), раздел «Скрипты устройства»; в `stash-client` —
   `stash-collect v0.2.2` (`1cdf7cf`), тот же раздел. Составить по сообщениям
   коммитов.
5. `stash-client`: признак обхода в `scripts/routehub-speedtest.js` ещё
   узкий (`[Обход`), в `main` широкий с v0.7.0 — синхронизировать.
6. `src/admin.js` и `src/api.js` больше 15 КБ — разделить (правило проекта).
7. Дальше — бэклог `studio/tasks/BACKLOG.md`.

## Ждёт Диану

**1. `handoff` сам или по команде** — снимать ли `disable-model-invocation:
true` со скилла `handoff`? Без этого монитор контекста только предлагает
команду `/handoff`, а не передаёт дела сам.
   По умолчанию: не снимать, пока монитор не проверен в живой сессии.

**2. Автосжатие** — выключать ли `autoCompactEnabled: false`?
   По умолчанию: НЕ выключать — автосжатие остаётся страховкой, монитор
   передаёт дела раньше.

**3. Плагины в claude.ai** — включить `code-review`, выключить
`example-skills` и `claude-api`.
   По умолчанию: да, все три.

**4. На телефоне** — прогоны L12 (без whitelist и под whitelist), флаг EWMA
в админ-панели, очистка нонса k2.
   По умолчанию: сделать при ближайшем удобном случае; до прогонов L12
   пункт 44 бэклога считается непроверенным на устройстве.

## Открытые вопросы

**1.** Доступны ли Диане «Проекты» Claude Code (бета)?
   По умолчанию: не рассчитывать на них, пока не проверено.

**2.** Виден ли запрос хука `ask` (`guard-prod`) в мобильном приложении?
   По умолчанию: считать, что не виден, пока не проверено по шагу 1 брифа
   `T-workflows`.

## Открыть первыми

- `CLAUDE.md`
- `MAP.md`
- `studio/tasks/BACKLOG.md`
- `studio/tasks/T-workflows.md`

## Скиллы

- `context-audit` — первым запросом сессии (правило `CLAUDE.md`).
- `prove-blocked` — перед любым выводом «не работает / невозможно».
- `grill-me` — если по брифу остались вопросы к Диане.
- `ideas` — в конце закрытой задачи.

## Память проекта

`ПАМЯТЬ_ПРОЕКТА.md` и `СТАРТ.md` пока лежат в Файлах проекта claude.ai.
Переносятся в `studio/memory/` ТОЛЬКО после перевода репозитория в
приватный, последним заходом в Cowork (решено 24.09).
