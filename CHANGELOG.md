# CHANGELOG — RouteHub

История версий Worker'а, главного конфига Loon и профиля Stash. Вынесена
из шапок файлов 2026-08-15: в `routehub-worker.js` она занимала 18 КБ
(четверть файла), в `routehub.conf` — 11 КБ из 29 КБ.

Здесь — действующие версии. Всё, что старше, перенесено дословно в
`CHANGELOG_ARCHIVE.md` (техдолг 10): каждая правка через GitHub API тянет
файл целиком, а 28 КБ на одну строку изменений — дорого. Граница: для
Worker'а — начиная с v1.9.4, для конфига — начиная с C-draft-41; всё
предыдущее в архиве, ничего не удалено и не пересказано.

Порядок — от новых к старым, как было в шапках.

---
## Worker

### v1.11.0 (2026-09-24, ветка stash-client) — ФАЙЛЫ РЕПОЗИТОРИЯ ИЗ СБОРКИ (перенос T-private-repo)

Перенос Worker v1.12.0 из `main` (коммиты `70d6006..70aacb3`, PR #8) руками:
код ветки разошёлся с `main` (`src/api/*.js`, клиентские слои Stash), файлы
не копировались. Номер — следующий минор ВЕТКИ: v1.11.0 `main` (отдача в
`/speed`) здесь по-прежнему нет, одинаковый номер в двух ветках означает
разное содержание.

Причина та же: `spxload/routehub` станет приватным, а стенд
`routehub-stash` и override-файлы Stash ходили на `raw.githubusercontent.com`
и jsDelivr — после перевода 404.

- `src/files.js` — манифест файлов ЭТОЙ ветки: `routehub.conf`, 8
  `scripts/*.js`, 18 `probes/*.js`, 2 `plugins/*.plugin`, 11
  `plugins/*.stoverride` (40). Сторож `tests/files-manifest.test.js`: файл на
  диске = в манифесте, байт в байт; правила Text `wrangler.toml` и хук тестов
  покрывают одно и то же.
- `src/repo.js` — маршрут `GET|HEAD /t/<токен>/repo/<путь>`, гейт как в
  `main`: токен из пути обязателен всегда, ищется среди `reg[*].token`, иначе
  403; строгая регулярка по сырому пути + манифест, иначе 404; в D1 не пишет,
  в сеть не ходит. Переписчик принимает ref `main` И `stash-client` (raw и
  jsDelivr): override стенда ссылаются на обе ветки (ST13–ST17,
  Stash-Probes, Stash-Collect — `stash-client`; ST6, ST6-cdn, Probe, Stash,
  FailLog, `[Plugin]` конфига — `main`). Отдаётся копия ЭТОЙ ветки; для ссылок
  на `main` это допустимо — все их адресаты на 24.09 совпадают с `main` байт в
  байт (обоснование — в шапке `REPO_LINK_RE`).
- Ссылки установки Stash (`https://link.stash.ws/install-override/<адрес без
  схемы>`) в override тоже ведут на прокси стенда: замечание тестировщика
  24.09 — иначе после перевода репозитория в приватный override не на чем
  переустановить. Установка: открыть отдаваемый `/t/<токен>/repo/plugins/<файл>`
  и взять из его шапки ссылку `link.stash.ws`.
- `src/api/config.js` — шаблон Loon из сборки вместо `fetch(CONFIG_URL)`,
  `scriptBase` — `<origin>/t/<токен>/repo/`, переписчик после `renderConfig`
  (только для клиентов с шаблоном); `Cache-Control: no-store` у обоих
  клиентов. Профиль Stash (`usesTemplate = false`) по логике не менялся;
  тест сторожит, что в нём нет ссылок на `spxload`.
- `wrangler.toml` — правила Text расширены (оба окружения), `CONFIG_URL`
  убран из `[vars]` и `[env.stash.vars]` (код ветки его не читает).
- Тесты: хук `tests/text-loader.mjs` вместо копии
  `.rh-worker-under-test.mjs` (строка убрана из `.gitignore`); новые
  `tests/repo.test.js`, `tests/files-manifest.test.js`, сторож синтаксиса
  скриптов `tests/scripts-syntax.test.js` из `main` (18b756e, с проверкой
  шаблона dash — к v0.7.0 применима); `tests/config.test.js` — по образцу
  `main` плюс два теста профиля Stash. 311 → 365.

`routehub.conf` и `plugins/*` не менялись (пара `routehub.conf`
main↔stash-client совпадает). Ссылки установщика `link.stash.ws/…` в
комментариях override по-прежнему ведут на raw GitHub — после перевода
репозитория override ставится по ссылке `<origin стенда>/t/<токен>/repo/plugins/<файл>`.

### v1.10.1 + перенос v1.11.1 (2026-09-21, ветка stash-client) — ШИРОКИЙ ПРИЗНАК ОБХОДА

Та же правка, что Worker v1.11.1 в `main` (коммит `73af3f4`), перенесена
точечно: ветки расходятся, файлы из `main` не копировались. Версия Worker'а
в ветке (`WORKER_VER` v1.10.1) и профиля (`S-draft-7`) не менялись: v1.11.0
здесь нет, а выдача профиля при нынешних именах подписки прежняя.

**Что было.** `tagOf` считал узел обходным по подстроке `'[Обход'`. Имя вида
`🇩🇪 Германия [🌀 Обход VPN] #1` (значок внутри скобки — провайдер так уже
делал с VPN) получало тег `vpn`, без слова VPN — тег `other`.

**Почему для Stash срочнее, чем для Loon.** У Loon правило 1 держат ещё и
регулярки конфига `^(?!.*Обход)`. У Stash страховки нет: раскладку по группам
решает только `tagOf` (`clients/stash-order.js`). Проверено по коду и прогоном
до правки: нераспознанный обход 🇩🇪 с тегом `vpn` встаёт в тир DE — ранг 0 в
`RH-AI` (выше Нидерландов и всех прочих стран) и регион EU, ранг 0, в
`RH-АВТО`; с тегом `other` выпадает из профиля совсем, и `RH-Обход`
вырождается в `DIRECT`. Срез подписки 21.09 16:00 UTC: 8 обходных узлов, все
вида `… Германия [Обход] #N` — узкий признак их ловит, риск спящий.

**Что сделано.** Константа `BYPASS_WORD = 'Обход'` (`src/const.js`) — слово в
любом месте имени. Её читают `tagOf` (`src/util.js`) и четыре регулярки
AI-фильтров Loon (`src/clients/loon.js`); вывод `aiBlocks` побайтно прежний
(сверено до и после). Других проверок признака подстрокой в `src/` нет.
Сборщик `routehub-stash-collect.js` v0.2.1 и проба ST15 уже
ищут слово `Обход` в любом месте — с Worker'ом теперь согласованы.

**Тесты.** Новый `tests/bypass.test.js`: смысл набора из `main` плюс правило
1 для Stash — обход 🇩🇪 стоит строго после всех обычных узлов в `RH-AI-*`,
`RH-АВТО-*`, `RH-Звонки-*`, оба обходных узла (с «VPN]» и без) — члены
`RH-Обход`, обход как единственный узел 🇩🇪 тира DE не создаёт; сторож
совпадения признака со сборщиком и ST15. 305 из 305. Мутация «вернуть
`'[Обход'`» — 7 падений.

**Не сделано (скрипт устройства, отдельное согласие).** `routehub-speedtest.js`
в этой ветке — старый, с узким признаком `'[Обход'` в двух местах; в `main`
он расширен в v0.7.0.

### v1.10.1 (2026-08-17) — ВОЗРАСТ ЗАМЕРА ВИДЕН ГЛАЗАМИ

v1.10.0 научила Worker отличать настоящий замер от переотправленного кэша,
но увидеть это можно было только в сыром JSON `/dashboard` и только
разглядывая весь список узлов. Техдолг 12.

`/admin/state` отдаёт `metrics_age` — сводку по каждому слоту: сколько узлов
всего, у скольких есть отметка времени, у скольких нет, возраст самого
старого и самого свежего замера. Строка «сотовая: 0 из 50 с отметкой»
читается как «этот слот не мерялся ни разу» — без списка и без D1.

Админ-панель (p3) получила отдельную карточку «Замеры узлов»: две строки
вида «Wi-Fi 48 из 48 с отметкой, самый старый 37 мин» и «сотовая 0 из 50 с
отметкой — не мерялся, приходит из кэша». Проверено в браузере на ширине
390 px: карточка на своём месте, ошибок в консоли нет.

Парная правка панели устройства — `routehub-dash.js` v0.7.0: под каждым узлом
строка «замер 12 мин назад», а когда возраст скорости и пинга расходятся —
«скорость 3 ч назад · пинг 12 мин назад». Если у всех узлов набора отметки
нет, панель прямо пишет, что набор приехал из кэша и, похоже, не мерялся.

Ревью перед заливкой нашло четыре вещи, все исправлены до push:

1. Панель врала бы при рассинхроне версий: Worker до v1.10.0 полей возраста
   не отдаёт вовсе, а код не отличал «поля нет» от «отметки нет» — под всеми
   узлами появилось бы «время замера неизвестно» и ложная тревога о
   незамеренном наборе. Добавлена проверка `hasAge`.
2. В режиме кэша панель занижала возраст ровно там, где он важен: число
   считает Worker в момент ответа, а `rh_dash` хранит его замороженным.
   Loon лежал три часа — панель писала «12 мин назад». Добавлена поправка на
   возраст кэша.
3. `metricsAge` считала мёртвые узлы, тогда как дашборд и каскад их
   отбрасывают: два числа на одном экране переставали быть сравнимыми.
   Мёртвые вынесены в отдельный счётчик.
4. Возраст мог выйти отрицательным (`clampTs` пропускает отметку на пять
   минут вперёд) — зажат нулём.

Тестов 86.

### v1.10.0 (2026-08-17) — ВРЕМЯ ЗАМЕРА НА КАЖДЫЙ СЛОТ

До этой версии у метрик была одна отметка времени на всю запись
`metrics:<ключ>`, а слотов два: `w` (Wi-Fi) и `c` (сотовая). Устройство при
каждом свипе переотправляет **оба** своих кэша независимо от текущей сети —
так устроена функция `send` в `routehub-speedtest.js`. Значит «пришло» не
равно «измерено»: слот, который телефон не мерил ни разу, приезжает снова и
снова, `updated_at` движется, а значения заморожены.

Случай, который это вскрыл: у ключей k1 и k2 сотовый блок совпал побайтово
у всех 50 узлов при полностью различающемся Wi-Fi (техдолг 11). По базе
отличить замороженный кэш от настоящего замера было нечем.

Теперь у каждого слота своя отметка:

- **`ts`** — время полного замера скорости (`down`, `bl`). Именно он весит
  0.40 в композитном балле, поэтому возраст балла считается по нему.
- **`tsp`** — время последнего пинга (`rtt`, `jit`). Лёгкий пинг-свип
  обновляет их, не трогая `down`, поэтому отметки расходятся, и валить их в
  одну было бы враньём.

Источник отметки — **само устройство** (парная правка `routehub-speedtest.js`
v0.6.4: `buildArr` отдаёт `ts` и `tsp`, которые в кэше лежали и раньше, но
наружу не уходили). Часы телефона недоверенные, поэтому значение зажимается
окном «сейчас − 30 суток … сейчас + 5 минут»; всё за его пределами
отбрасывается.

Для устройств со старым скриптом остался запасной путь: признак настоящего
замера — **изменение значений**. Совпало до последнего поля, значит слот
переотправлен из кэша. Если у прежней записи отметки не было вовсе
(запись старше v1.10.0), возраст остаётся `null` — честнее, чем «только
что»: иначе замороженный слот показал бы нулевой возраст ровно в тот момент,
ради которого всё и делалось.

`/dashboard` отдаёт по каждому узлу `age_min` и `ping_age_min`. Запись метрик
на 50 узлах выросла с 6,3 до 8,2 КБ (+30 %), лимитов D1 это не задевает.

Тестов стало 84: первая отметка, переотправка, изменение поля, миграция без
отметки, приоритет устройства, отбраковка времени из будущего и из прошлого
века, два возраста в дашборде.

Правка прошла обязательное ревью перед заливкой. Ревью нашло ошибку в первой
редакции: у записи без `ts` условие рушилось раньше проверки значений и слот
штамповался текущим временем — то есть правка не решала задачу, ради которой
делалась. Исправлено, случай покрыт тестом.

### v1.9.9 (2026-08-16) — ПУТЬ СКРИПТА С ПАПКОЙ

Подготовка к раскладке репозитория по папкам. Подстановка базового адреса в
`script-path` понимала только имя файла в корне; regex расширен до
`(?:[\w.-]+/)*routehub-…`, то есть работает и с `routehub-speedtest.js`,
и с `scripts/routehub-speedtest.js`.

Сделано ДО переезда файлов намеренно: между коммитом с перемещением и
обновлением конфига на устройстве нет окна, в котором Worker отдал бы
неправильную ссылку. Правка в `src/clients/loon.js`, плюс тест на обе
раскладки.

### v1.9.8 (2026-08-16) — ПРОВЕРКА ДЕПЛОЯ И ЧЕСТНЫЙ ДЖИТТЕР

`GET /version` отдаёт `{"worker":"vX.Y.Z"}` — до этого убедиться, что
Workers Builds собрал именно последний коммит, можно было только косвенно.
Ответ не требует авторизации и не читает D1.

В дашборде `jit` больше не превращается в `0` при `null`: сбойный замер
(v1.9.7) показывался как идеальный джиттер, что читалось ровно наоборот.
Теперь `null` доходит до панели как `null`.

Парная правка — `routehub-speedtest.js` v0.6.3: тестовый адрес переведён на
`connectivitycheck.gstatic.com/generate_204`, тот же, по которому Loon
проверяет узлы в группах. Раньше скрипт и группы мерили разные цели, и
верхушка нашего списка не обязана была совпадать с выбором Loon. Сравнение
медиан `rtt` до и после — через сутки после выката.

### v1.9.7 (2026-08-16) — ОТСЕЧЕНИЕ СБОЙНЫХ ЗАМЕРОВ

Разбор среза `metrics:k1` (`ЗАМЕРЫ_И_ВЕСА.md`) показал: `jit` и `bl` дают
45-50 % различий между узлами, при этом в данных встречались `jit = 23 726 мс`
и `bl = 8039 мс`. Одиночная испорченная проба отбрасывала быстрый узел на
10-23 позиции вниз — а порядок узлов и есть всё, чем Worker влияет на выбор.

`metricOf` теперь отдаёт `null` вместо значений выше предела разумного
(`JIT_BAD = 1000`, `BL_BAD = 2000`). В `scoreOf` такой компонент нейтрален,
а не худший из возможных; `voiceOk` при `jit = null` по-прежнему отказывает —
обещать голос по сбойному замеру нельзя. Реальный плохой джиттер в сотни
миллисекунд порог не превышает и наказывает узел как раньше.

Проверка на срезе 16.08: 🇦🇹 Австрия (`jit` 1028 при `down` 43) поднимается с
40-го места на 11-е, польская локация с `jit` 23 726 — с 50-го на 40-е.
Набор Wi-Fi не изменился вовсе: там абсурдных значений не было.

Парная правка — `routehub-speedtest.js` v0.6.2: устойчивый джиттер на
устройстве. Worker чинит последствия, скрипт — причину. Старые записи в D1
перезаписываются при следующем свипе, то есть эффект появляется в течение
20 минут после деплоя, а не мгновенно.

**Веса композитного балла НЕ трогали.** Проверка показала, что они не рычаг:
любые разумные наборы дают ранговую корреляцию ρ ≥ 0.985.

Заодно тесты разложены по файлам (`harness.js` + `routehub-worker.test.js`,
`clients-loon.test.js`, `metrics.test.js`) и версия перестала хардкодиться в
тестах: правка одного набора больше не требует перезаливки 34 КБ через API,
а релиз — правки тестов. Тестов 62.

### v1.9.6 (2026-08-16) — КЛИЕНТСКИЙ СЛОЙ

По ADR-01 код делится не только на модули, но и на два уровня: ядро `src/*.js`
считает и хранит, `src/clients/*.js` рендерит под конкретный клиент. Первый
клиентский модуль — `clients/loon.js`, туда перенесены три вещи, которые знали
про синтаксис Loon и лежали не на своём месте:

* `aiBlocks` — из `ai.js`. В `ai.js` остался расчёт тиеров (`buildAiTiers`),
  то есть то, что от клиента не зависит.
* `subParamsFromConf` — из `util.js`. Разбор строки `[Remote Proxy]` — формат
  Loon, в утилитах общего назначения ему делать нечего.
* Подстановки плейсхолдеров из `handleConfig` — теперь функция `renderConfig`.
  `api.js` собирает контекст (ключ, база с токеном, флаги устройства,
  посчитанные блоки) и отдаёт его клиентскому слою.

**Логика не менялась.** Проверено побайтовым сравнением: старая цепочка
`replace` и новый `renderConfig` на боевом `routehub.conf` дают идентичный
результат (18 749 байт).

Что это даёт: `clients/stash.js` встанет рядом и будет собирать YAML, не
трогая ядро. Раньше для этого пришлось бы ветвить `api.js` и `ai.js`.

### v1.9.5 (2026-08-15) — РАЗДЕЛЕНИЕ НА МОДУЛИ

Файл вырос до 76 КБ и перестал передаваться инструментами целиком: `git push`
из облачной сессии блокирует прокси (баг claude-code #76248), а передача
текстом рисковала исказить суррогатные пары эмодзи (вывод 9 проекта — на
проверке не подтвердился, все 49 констант прошли round-trip побайтово).
Разложен на восемь модулей в `src/`, каждый меньше 15 КБ и правится по
отдельности. Точка входа — только роутинг и `scheduled`, 6.9 КБ.

Граф импортов ацикличен: `const` ← `util` ← `store` ← `sub` ← {`ai`, `api`,
`dash`, `admin`}. `ADMIN_HTML` импортируется точкой входа и передаётся в
`handleAdminPage` параметром — правило Text в `wrangler.toml` действует только
на статический импорт.

**Логика не менялась**, тесты те же 58 и все зелёные. Временный файл теста
переехал в корень репозитория, иначе не разрешаются относительные импорты.

### v1.9.4 (2026-08-15) — РЕГИОНАЛЬНЫЕ ОСТАТКИ AIeu / AIam

В AI-тиеры попадали только страны с двумя и более узлами, поэтому одноузловые
европейские (на дату: AT, BE, CH, CZ, DK, ES, FR, GB, IE, NO) падали в общий
`AIrest` — ниже тира 🇹🇷 Турции с её двумя узлами. Наблюдение 15.08: `RH-AI-C`
выбрала Турцию, тогда как `RH-АВТО-C` со своим региональным каскадом взяла
Великобританию. Для ИИ гео важнее числа узлов. Новый порядок каскада:
DE → тиры Европы → AIeu → тиры Америки → AIam → тиры прочих → AIrest → Обход.
СНГ исключён везде, как и раньше.

---

## Скрипты устройства

### routehub-stash-collect.js v0.2.3 (2026-09-25) — ПОД ОБЁРТКУ S-draft-8

После S-draft-8 поле `now` пула `RH-АВТО-W` / `-C` — обычно имя ручной
группы `RH-АВТО-W-Ручной`, а не узла. Прежняя проверка второго рубежа
правила 1 («не обходной ли выбран» по имени в `now`) видела бы группу и
всегда отвечала «рабочий». Теперь решение принимается по концу цепочки
`chainOf` до узла: обходной узел в конце, группа в конце (пустое `now`,
петля, предел глубины — в том числе запись без поля `type`, но с `now`/`all`),
конец, которого нет в `/proxies` (отсутствие записи — не «рабочий»), или
пустая цепочка — «не рабочий», самопроверки и замера нет. Ручная группа
на обходном узле (по составу профиля невозможно, но ответ контроллера нам не
подконтролен) — тревога первой строкой отчёта, дописывается к тревоге
`RH-Главный` / `RH-RU`, а не затирает её. Уход самого fallback на обход —
штатный последний резерв, тревоги нет (как и раньше). Ручная группа в пул
замеров не входит: отсекается по суффиксу `MANUAL` (второй слой — её и так
отсекают `looksLikeNode` и `isGroup`). Строка отчёта о пуле называет ручную
группу («… выбран рабочий узел (через RH-АВТО-W-Ручной)»). Сеть по-прежнему
читается по хвосту `-W` / `-C` у родителей. Ни одного нового запроса: к
контроллеру только `GET /proxies` и `GET /connections` (правила 1 и 2).

Тесты: `stash-collect` 33 → 41 (песочница по умолчанию отдаёт профиль с
обёрткой; `wrap: false` — прежний профиль). Всего по наборам проекта на
ветке с этой правкой 441 → 457, все зелёные.
Мутационная проверка (генератор и сборщик): 21 мутация, 20 роняют тесты —
обход в ручной группе; ручная не в fallback; ручная последней; пустой
`select` при пустом наборе; ручная не `select`; форма (Б) без ручной в
fallback; порядок ручной обращён; fallback раньше ручной; суффикс рендерера;
суффикс сборщика; конец цепочки вне `/proxies` считается рабочим; обход в
конце цепочки не проверяется; решение по имени
первого звена; конец-группа не проверяется; `groupLike` → `isGroup`;
тревога без признака ручной; тревога снята; тревога затирает прежнюю; все
фильтры пула сняты; строка «через» снята. Выжила одна, эквивалентная:
снятое отсечение по `MANUAL` (ручную группу отсекают `looksLikeNode` и
`isGroup`; при снятии всех трёх тесты падают).

### routehub-stash-collect.js v0.2.1 (2026-09-21) — СТОРОЖ 90 С И ЖУРНАЛ ВЫБОРА ГРУПП

СТОРОЖ 25 → 90 С, ПУТЬ ДО `$done` ОГРАНИЧЕН. Арифметика v0.2.0 («25 с номинала
втрое — 75 с, скрипт уже закончил») держалась на растяжении `setTimeout` (ST5),
а растяжения может и не быть: тогда 25 с номинала — это 25 с настоящих, и
сторож обрывал живой прогон посреди замера. Обрыв по своему таймеру выглядит
как отказ узла — дефект ST14, пойманный проектом уже четыре раза. Пересчёт по
коду показал второе: худший честный путь v0.2.0 не был ограничен ничем. Узел
запускался, пока до конца бюджета оставалось `NODE_COST_MS` (до 30-й
секунды), а сам стоил до `5 × PING_SEC + DOWN_SEC` = 75 с; с выгрузкой
`POST_SEC` 15 — до 120 с. Сторож в 90 с такой путь резал бы так же.

Поэтому тайм-аут каждого запроса к контроллеру и через узел теперь
подрезается под остаток бюджета (`capSec`: целые секунды, округление вниз), а
за бюджетом запрос не отправляется вовсе. Всё, кроме выгрузки, закрывается к
`BUDGET_MS`, и путь до `$done` не длиннее `BUDGET_MS + POST_SEC` = 60 + 15 =
75 с настоящих. Сторож 90 с номинала — не меньше 90 с настоящих (таймеры
Stash растягиваются, но не укорачиваются): запас 15 с. Бюджет снижен с 70 до
60 с ради этого запаса; узлы запускаются в первые 20 с прогона вместо 30. В
фоне сторож растягивается до 270–360 с и уступает тайм-ауту манифеста; тот
остаётся 180 с (больше 90 и больше 75 — поднимать не понадобилось), поправлен
только устаревший комментарий о бюджете в
`plugins/RouteHub-Stash-Collect.stoverride`.

ЖУРНАЛ ВЫБОРА СЛУЖЕБНЫХ ГРУПП. S-draft-7 (14.09) перевёл `RH-Главный` из
ручного `select` в `fallback`, и у решения записан риск: если ядро ошибочно
сочтёт DIRECT мёртвым, весь прочий иностранный трафик молча уйдёт на узлы,
включая платные обходные. Увидеть это можно только по полю `now`. Сборщик
теперь читает его у `RH-Главный`, `RH-RU`, `RH-Обход`, `RH-AI`, `RH-АВТО`,
`RH-Звонки` из того же ответа `GET /proxies` — ни одного нового запроса, ни
байта через узлы (правила 1 и 2) — и раскрывает цепочку до узла
(«RH-Главный → RH-АВТО → RH-АВТО-W → узел»).

Итоговый отчёт прогона выводится одной записью в журнал сценариев, и первая
его строка — вердикт: `выбор штатный: RH-Главный и RH-RU на DIRECT` либо
`⚠ ТРЕВОГА ВЫБОРА: …`. Тревога поднимается, если `RH-Главный` или `RH-RU` не
на DIRECT, если группы нет в `/proxies` или её `now` пуст; случай
`RH-RU → RH-Обход → обходной узел` назван прямо: «РФ-трафик идёт по ПЛАТНОМУ
обходу». Тот же вердикт и карта `now` пишутся в хранимый журнал
`rh_stash_log` (поля `sel` и `g`). Уведомления на тревогу нет: прежнее
правило «уведомлять только об отказе» не пересматривалось.

Тесты: `tests/stash-collect.test.js` 14 → 27, `stash-collect-coupling` 7 → 8,
в наборах проекта 280 → 294. Новое: восемь проверок журнала выбора; «к
контроллеру только GET» с учётом поля `method` в опциях и записью вызовов
`put/patch/delete` до исключения (прежняя проверка через исключение
проглатывалась `try/catch` скрипта); «тревога не добавляет ни одного
запроса»; худший честный путь на виртуальных часах без растяжения (запросы
отвечают за миллисекунду до тайм-аута или отказывают по нему, выгрузка
обязательна) — `$done` на 75 с, сторож молчит; сцепка имён журнала с
рендерером профиля. Мутационная проверка: десять мутаций из десяти роняют
тесты — сторож 25 с, снятый журнал, `PUT /proxies/RH-Главный`, GET с
`method: 'PUT'`, пропуск тревоги по `RH-Главный`, тревога не первой строкой,
снятая тревога платного обхода, снятая подрезка тайм-аутов, бюджет 70 с,
лишний GET из журнала.

### routehub-stash-collect.js v0.2.0 (2026-09-02) — СБОРЩИК МЕТРИК ДЛЯ STASH

Первая рабочая редакция (v0.1.0 наружу не выходила: её разобрало ревью, см.
ниже). Закрывает главный пробел стенда — профиль Stash отдавался, но
сортировки по композитному баллу на нём не было: балл считает Worker из
метрик, а присылать их было некому.

Не перенос `routehub-speedtest.js`, а другой скрипт: у Loon сборщик стоит на
трёх опорах `$config` / `$httpClient({node})`, и ни одна из них у Stash не
работает в прежнем виде (ADR-04, §1). Состав узлов и текущая сеть берутся из
`GET /proxies` контроллера — состав по членам группы `RH-АВТО-W`/`-C`, сеть по
хвосту `-W`/`-C` у члена, на который смотрит родитель с `ssid-policy`. Это
чтение решения, уже принятого ядром, а не своё определение.

ЧТО МЕРЯЕТ ЯДРО И ЧТО СКРИПТ. Группы `-W`/`-C` — `fallback` с умолчанием
`interval` 600 с, у рабочих узлов стоят `benchmark-url`/`benchmark-timeout`
(S-draft-3), поэтому ядро непрерывно проверяет каждый узел само. Из этого
берутся **живость** и **очередь кандидатов** — но НЕ метрики балла. Причина в
сопоставимости: замер ядра это HEAD раз в десять минут, разброс таких
замеров — разброс между десятиминутными интервалами, а пороги проекта
(`FLOOR_JIT` 10, `VOICE_JIT` 30, `FLOOR_BL` 20) откалиброваны на пяти подряд
идущих пробах Loon. Вдобавок разброс не из чего считать: истории у Stash
3.4.1 нет, запись узла в `/proxies` несёт одно текущее `delay` (ST9). Числа выглядели бы теми же, а значили бы другое. Поэтому все четыре
метрики снимает сам скрипт, тем же методом, что и Loon: `rtt` — минимум из
пяти проб `generate_204` через узел, `med` — их медиана, `jit` — усечённый
размах, `bl` — прирост задержки во время закачки над `rtt`, `down` — 4 МБ
через узел.

СКОРОСТЬ ИЗ `/connections` — ТОЛЬКО НИЖНЯЯ ГРАНИЦА. `download.max` в байтах в
секунду (ST11) даётся даром, но покрывает один-два узла из полусотни и
говорит о том, что качало приложение, а не о возможностях узла. Поэтому
пассив поднимает `down` у уже измеренного узла и никогда не опускает; и он
не отменяет очередной активный замер — срок годности считается по отдельной
отметке `ats`, иначе одно просмотренное видео замораживало бы узел на сутки.

УЗЕЛ БЕЗ АКТИВНОГО ЗАМЕРА НЕ ОТПРАВЛЯЕТСЯ ВОВСЕ. Контракт `/speed` не умеет
сказать «метрика неизвестна»: `metricOf` превращает отсутствующее поле в 0, а
нулевые `rtt` и `jit` на шкале `scoreOf` — это МАКСИМАЛЬНЫЙ балл. «Отправить,
чего намерили» означало бы поднять неизмеренный узел наверх списка.

САМОПРОВЕРКА ПИНОВКИ. Заголовок `X-Stash-Selected-Proxy` документирован, но в
проекте не подтверждён ни одним замером: ST1 доходила до его проверки только
при заданном аргументе `node`, а он не задавался. Активная фаза не
запускается, пока сборщик сам не убедится — сравнением выходного адреса через
узел A, через узел B и без заголовка, — и вердикт «работает» принимается
только после ДВУХ удачных проверок подряд, каждая со своей парой узлов:
разойтись адреса могут и случайно, если между запросами `fallback` сменил узел.

ПРАВИЛО 1 — ДВА РУБЕЖА. Первый: имя с пометкой обхода отбрасывается при сборе
пула, до любого вызова. Второй, на случай если пиновка молча перестанет
работать (тогда запрос уходит по текущей политике, а последний запас
`fallback` — как раз обходной узел): и самопроверка, и активная фаза
запускаются, только если пул прямо сейчас смотрит на необходной узел.

ЧТО ИСПРАВИЛО РЕВЬЮ ПЕРЕД ВЫПУСКОМ (кроме двух пунктов выше):
* **Ключ кэша — базовое имя.** Имя узла в профиле несёт метрики
  («… · 21↓68 / 7↓96»), и они меняются при каждой перевыдаче конфига. Кэш по
  полному имени обнулялся бы после каждого замера: узлы вечно «просрочены»,
  трафик вчетверо против расчётного, мёртвый узел никогда не помечен мёртвым.
  У Loon это сделано с самого начала (`baseName`).
* **`bl` — прирост, а не абсолют.** Абсолютные 80–300 мс на шкале, рассчитанной
  на прирост, означали бы, что для звонков не годится ни один узел, а
  измеренный узел проигрывает по баллу неизмеренному.
* **Тайм-ауты в секундах.** У Stash поле `timeout` — секунды (ST1 и ST9
  отвечают при 3, 5, 8), у Loon — миллисекунды; перенос значений оттуда давал
  закачке тайм-аут в семь часов.
* **Выход по чужой блокировке снимает сторожа.** Иначе сторож пропущенного
  прогона через полминуты снимал замок работающего, и два прогона качали
  параллельно, затирая замеры друг друга.
* **Кэш пишется после каждого узла**, а не один раз в конце: сторож,
  сработавший не вовремя, не отменяет оплаченной трафиком работы. Номинал
  сторожа заведомо больше бюджета после растяжения таймеров (ST5).
* **Неудачная запись `nonce` отменяет выгрузку.** Иначе следующий прогон
  сгенерировал бы другой, Worker перевёл бы ключ в `conflict` и отвечал 409,
  пока Диана не отвяжет ключ руками.
* **Отказ виден уведомлением.** FATAL («контроллер не ответил», «сеть не
  определена») больше не уходит только в журнал сценариев.
* **Аргумент не пишется в журнал** — в нём может быть токен ссылки.

Тест `tests/stash-collect.test.js` (14 проверок) проверяет не живучесть, а
поведение, и прогоняет скрипт НЕСКОЛЬКО РАЗ ПО ОДНОМУ ХРАНИЛИЩУ, с именами
боевой формы — на чистых именах и одиночном прогоне ни один из дефектов выше
не виден.

Второй набор, `tests/stash-collect-coupling.test.js` (7 проверок), закрывает
то, чего песочница не видит принципиально: сборщик держит ЗАШИТЫЕ ИМЕНА
(`RH-АВТО-W`, три родительские группы, пометка обхода, разделитель метрик), а
приходят они из `src/clients/stash*.js`, и связь между файлами не выражена
ничем, кроме совпадения строк. Переименуй группу в рендерере — сборщик не
упадёт: он не найдёт пул, объявит «сеть не определена» и будет молча ничего
не собирать. Поэтому имена не переписаны от руки, а ВЫНИМАЮТСЯ из исходника
сборщика регулярным выражением и сверяются с тем, что рендерер реально кладёт
в профиль; набор ломается при переименовании с любой стороны.

Он же зафиксировал переход, из-за которого дефект с ключом кэша и был
опасен: **пока замеров нет, имена узлов в профиле ЧИСТЫЕ** (`nodeLabel`
возвращает базовое имя, если обеих метрик нет), и стенд сейчас именно в этом
состоянии. Первая же выгрузка сборщика добавит хвост метрик ко ВСЕМ именам
разом — то есть ровно в тот момент, когда кэш впервые становится нужен, он
целиком и обнулялся бы. Тест требует, чтобы базовые имена до и после перехода
совпадали.

Расписание — `plugins/RouteHub-Stash-Collect.stoverride`, каждые 20 минут
(5, 25, 45), разведено с пробами ST8…ST12.

ЧЕГО ПОКА НЕТ: лёгкого пинг-свипа (аналог speedtest v0.6.0). `rtt` стареет
вместе с `down`, до суток.

### routehub-dash.js v0.7.0 (2026-08-17) — ВОЗРАСТ ЗАМЕРА В КАРТОЧКЕ УЗЛА

Под именем узла появилась строка возраста. Замеров два — полный (`down`,
`bl`) и лёгкий пинг (`rtt`, `jit`), и они расходятся, поэтому при заметном
расхождении показываются оба. Отдельная подсказка, когда у всего набора нет
ни одной отметки: это признак того, что слот приходит переотправкой кэша и
спидтест в этой сети не отрабатывает. Парно с Worker v1.10.1.

### routehub-speedtest.js v0.6.4 (2026-08-17) — ВРЕМЯ ЗАМЕРА НАРУЖУ

`buildArr` отдаёт в запросе `ts` и `tsp` — они лежали в кэше с самого начала
(`ts` пишется при полном замере, `tsp` при пинг-свипе), но на сервер не
уходили, и тот мог судить о свежести только по факту получения. Две строки
снимают догадку целиком. Парная правка к Worker v1.10.0; на старом Worker'е
лишние поля просто игнорируются.

### routehub-speedtest.js v0.6.3 (2026-08-16) — ЕДИНЫЙ ТЕСТ-АДРЕС

Пинг переведён с прежней цели на `http://connectivitycheck.gstatic.com/generate_204`
— тот же адрес, по которому Loon проверяет узлы внутри групп. Раньше скрипт и
группы мерили разные цели, поэтому верхушка нашего списка не обязана была
совпадать с выбором Loon. Парная правка к Worker v1.9.8.

Следствие: медианы `rtt` в базе после выката несравнимы с прежними напрямую —
сравнение до/после делается на срезах, снятых уже на новом адресе.

### routehub-speedtest.js v0.6.2 (2026-08-16) — УСТОЙЧИВЫЙ ДЖИТТЕР

`RTT_SAMPLES` 3 → 5, а `jit` считается **усечённым размахом**: из
отсортированных проб отбрасываются минимум и максимум. Раньше метрикой был
полный размах `max − min` по трём пробам — одна проба, попавшая в таймаут или
в паузу планировщика iOS, определяла результат целиком.

На синтетике: пробы `[50, 52, 55, 58, 3000]` дают 6 мс вместо 2950. При числе
проб меньше пяти отбрасывать нечего, поведение прежнее.

Правка парная с Worker v1.9.7: скрипт чинит причину, Worker страхует от
остатков и от чужих данных.

---

## Пробы

### ST19 (2026-09-24) — ЗАКРЕПЛЕНИЕ ВО ВРЕМЕНИ, ОБЁРТКА, КОМАНДА ПО ССЫЛКЕ

**РЕЗУЛЬТАТ НА УСТРОЙСТВЕ (25.09, 05:45–11:25, Stash 3.4.1, ~70 прогонов
cron; перехватчик — 11:31, 4 нажатия в Safari).**
- Закрепление fallback (здоровый член и мёртвый муляж) и url-test встало, но
  к следующему удачному чтению (через 25 мин) сброшено на автовыбор. Причина
  не установлена: в этом окне сеть сменилась Wi-Fi → сотовая, а контроллер
  ~20 мин не отвечал (возможен перезапуск ядра). Опыт одиночный.
- load-balance: PUT → 400 `must be one of Selector / URLTest / Fallback`.
- Выбор в select держится часами (≥ 5 ч).
- **Обёртка работает**: fallback над ручным select пропускает его при мёртвом
  выборе (≤ 25 мин) и возвращается к нему при живом (≤ 20 мин). Разрешение
  по времени грубое — ограничено частотой удачных чтений, не Stash.
- Тип сети читается по `now` группы RH-AI (RH-AI-W / RH-AI-C); в
  `$environment` сведений о сети нет. Ручной выбор в группе с ssid-policy —
  вывода нет (совпал со значением политики).
- Контроллер недоступен (EOF / тайм-аут прогрева; повтор через 1 с не
  спасает) примерно в 30 % прогонов, окна до 17–20 мин.
- Карта рычагов: `GET /connections/{id}` → 200 (DELETE по id не проверен);
  `/group`, `/group/{имя}/delay`, `/dns/query` — маршрутов нет;
  `/proxies/DIRECT/delay` работает; маршруты `/providers/proxies/{имя}`
  (GET, PUT, healthcheck) есть, `/providers/proxies` пуст; `/logs` — поток,
  за 3 с данных нет. `$httpClient`: get/post/put/patch/delete/head/options.
- **Перехватчик ссылки работает**: переход на `http://rh-cmd.example.net/st19?n=…`
  перехвачен request-скриптом, PUT 204, выбор RH-Т19-Команда сменился и
  сверен чтением — 4 из 4, ~1 с на нажатие; в request-скрипте есть
  `$request` и `$httpClient`. Страница по https может командовать Stash
  обычными ссылками без токена на телефоне и без MITM. Для боевой панели
  нужны белый список групп «-Ручной», узлы без обхода и защита от чужих
  ссылок (одноразовый токен / подтверждение).
  Открывает ли уведомление ссылку из опции `url` — не проверено.

Предложение (ждёт решения Дианы, вопрос 1 передачи дел 25.09): управлять
узлами стенда через обёртку (fallback над ручным select), а не через
закрепление fallback.

Продолжение ST18. Открытый вопрос: вечное ли закрепление fallback. Если да,
fallback, которому скрипт выбрал узел, перестаёт сам уходить с упавшего.
Идеи — ideator 24.09, синтаксис HTTP-скриптов — researcher 24.09
(stash.wiki/en/script/rewrite-requests, /en/http-engine/force-http-engine).

- `probes/routehub-probe-stash19.js` — cron раз в 5 мин, фазы в
  `$persistentStore` (RH_ST19): старт → наблюдение (12 мин) → возврат →
  итог. Закрепляет здоровый не первый член (держится ли), мёртвый муляж
  (уходит ли fallback сам), ручной select внутри fallback-обёртки (пропускает
  ли её с мёртвым выбором и возвращается ли); PUT в url-test и
  load-balance; группа с ssid-policy — датчик сети и держится ли ручной выбор.
- Карта рычагов контроллера без побочных эффектов: 405 на GET — маршрут есть
  (например, DELETE соединения по id), текстовый 404 — нет, JSON-404 —
  маршрут есть, имени нет. Пишущий запрос — только с несуществующим именем.
  Из `/connections` — число и имена полей, из `$environment` — имена ключей.
- Карта и закрепления идут по одному пункту в пределах бюджета, остаток — в
  следующем прогоне. Прогревочный GET / с одним повтором (EOF из ST18).
- `probes/routehub-probe-stash19-cmd.js` — перехват `http://rh-cmd.example.net/st19`
  (`force-http-engine` на один хост, без MITM): страница-«панель» с кнопками
  переключает только RH-Т19-Команда по белому списку. Уведомление cron-части
  несёт эту ссылку (опция `url` в вики не описана — опыт).
- `plugins/RouteHub-Stash-ST19.stoverride` — муляж socks5 на 192.0.2.1
  (TEST-NET), 9 тестовых групп RH-Т19-*, одно правило DIRECT для хоста
  перехватчика.
- Ревью 24.09: неудачное чтение не считается ни сменой, ни подтверждением
  выбора; «не закрепился» — только по удачному чтению; из `/logs` — только
  длина (в журнале хосты); вывод по обёртке — только если до закрепления она
  стояла на Ручном, возврат проверяется по коду; сторож перехватчика позже
  трёх запросов (CTRL_SEC 2 с); после «итог» цикл не перезапускается.
- Тестировщик 24.09: граница «до/после возврата» — по фазе снимка, а не по
  секундам (снимок прогона возврата округлялся к той же секунде); если
  обёртка не уходила с Ручного, возврат — «не применимо»; из карты убран
  `GET /cache/fakeip/flush` (при обработчике без проверки метода сбросил бы
  fake-ip на рабочем телефоне). Длительности наблюдения, переход к возврату
  и «итог» считаются только по удачным чтениям: снимок с ошибкой не
  продлевает «держится» и не даёт «не возвращается».
- `tests/probe-stash19.test.js` — 40 тестов: подставной контроллер с тремя
  моделями fallback (закрепление вечное / снимается проверкой здоровья /
  с мёртвого уходит) и моделью «ядро не смотрит внутрь select», цепочки
  прогонов раз в 5 и раз в 1 мин на подставных часах, точечные сбои и обрывы
  на границе бюджета. Мутации — свои, ревьюера и тестировщика: все
  неэквивалентные убиты; выжившие эквивалентны (запрет записи — страховка;
  план не пересчитывается для уже закреплённых групп; граница фаз по
  секундам после вердикта «не применимо»).

### ST18 (2026-09-24) — ПЕРЕКЛЮЧЕНИЕ УЗЛА ИЗ СКРИПТА, ТЕСТОВЫЕ ГРУППЫ

**РЕЗУЛЬТАТ НА УСТРОЙСТВЕ (24.09, 20:30–20:45 МСК, Stash 3.4.1, 4 прогона).**
Три полных прогона совпали: select переключается (`now` меняется; PUT 38–122 мс),
кириллица проходит, несуществующее имя — 400 `Selector update error: proxy
not exist` без сбоя выбора, откат работает. **Fallback закрепляется `PUT`'ом**
(`now` → RH-Тест-Прямо). `DELETE` — 405, закрепление не снимает; поля
`fixed` в ответе нет, поэтому «закреплён на DIRECT» и «автовыбор DIRECT» после
возврата не различить — открытый вопрос для ST19. Прогон 20:35 — EOF
контроллера на первом запросе, ничего не записано; причина не установлена.

Решение Дианы 24.09: управление узлами на стенде Stash разрешено (скрипт и
панель меняют выбор через контроллер, обход — только последним резервом).
Первый шаг — проба на тестовых группах.

ST3–ST5 получили на `PUT /proxies/{группа}` ответ 204, но в группе был один
член, то есть выбор был холостым. ST18 проверяет по-настоящему: меняется ли
`now` при выборе другого члена, проходит ли имя кириллицей, что отвечает
контроллер на несуществующее имя, возвращается ли исходный выбор и можно ли
закрепить член fallback-группы (узлы стенда живут в RH-X-W / RH-X-C) и
снимается ли закрепление `DELETE /proxies/{группа}` (так у mihomo; у Stash
не проверено). Группу RH-AI (select + ssid-policy) только читает.

- `plugins/RouteHub-Stash-ST18.stoverride` добавляет `RH-Тест-Выбор`
  (select: DIRECT, REJECT, RH-Тест-Резерв), `RH-Тест-Резерв` (fallback:
  DIRECT, RH-Тест-Прямо) и `RH-Тест-Прямо` (select: DIRECT) — заведомо
  здоровую цель закрепления: с REJECT отказ «закрепить нельзя» был бы
  неотличим от «нездоровый член пропущен» (ревью). Правил на них нет, узлов
  в них нет — трафика нет, правила 1 и 2 не задеты. Принимает ли Stash `proxy-groups` в override, документация
  не говорит; не примет — проба скажет и ничего не запишет.
- Запись разрешена в коде только в `RH-Тест-Выбор` и `RH-Тест-Резерв`;
  откат выполняется всегда, в том числе когда чтение после записи не
  удалось. Шаг начинается, только если бюджета хватает на всю его цепочку с
  откатом, — худший честный путь не длиннее бюджета 45 с; сторож 75 с,
  `timeout` cron 300 с (растяжение таймеров в фоне). Вердикт «работает» —
  только по смене `now`, не по коду 204; отказ первого чтения с кодом не 404
  называется отказом контроллера, а не «override не применился».
- После срабатывания сторожа новых записей нет: запоздалый ответ
  контроллера ведёт только к откату (найдено тестировщиком — мутация защиты
  `FINISHED`).
- `tests/probe-stash18.test.js` — 28 тестов на подставном контроллере
  Clash-вида с подставными часами: бюджет проверяется поведением (время
  ответа 0,5–6 с), провал отката, запоздалый ответ в шести фазах (в том
  числе после закрепления fallback — откат обязан пройти), кириллица в
  закодированном адресе. Мутации: свои 18 + 6 на защиту после сторожа,
  тестировщика 20 + 12. Выжившие: снят запрет записи в чужие группы (код в
  них не пишет, запрет — страховка), стоп s2 без отката (выбор ещё не
  сдвинут) — эквивалентны; `STEP_MS` без запаса 1 с и `>=` в `room` —
  граничные, приняты.

### ST15 v2 (2026-09-06) — ПЕРЕПИСАНА ПО ОДИННАДЦАТИ ПРОГОНАМ

Первый выход на устройство, 06.09 08:40–10:21, Wi-Fi, RH-AI-W, 53 члена.
Из 11 прогонов 8 дали «чисты все проверенные узлы»; ценность в остальных трёх
и в том, что показала сама выгрузка.

**Что нашли данные.**
- **`grok.com` отдал 403 «This service is not available in your region»** на
  двух узлах из четырёх при `loc=DE` у обоих, тогда как два других узла (тоже
  DE и EE) получили 200 в том же прогоне. Значит дело в РЕПУТАЦИИ конкретного
  выходного адреса, а не в стране. Формулировка не совпала ни с одним нашим
  маркером — исход был помечен «не опознан». Маркер добавлен.
- **`claude.ai` отдал 200 и 434 666 байт HTML вместо 281 байта `robots.txt`**
  («<!DOCTYPE html><!-- Last Published»), на одном узле это заняло 19 288 мс.
  Отпечаток тела, заведённый ревью накануне, сработал ровно как задумано: без
  него это был бы штатный «ОТВЕТИЛ». Теперь такой ответ — отдельный исход
  **ПОДМЕНА**, а не успех.
- **Узел с флагом 🇩🇪 показал выход RU** (111.88.96.x вместо обычного
  85.93.10.x), и в том же прогоне Gemini с Grok до него не дошли. Две версии —
  сменился выход узла либо отвалилась пиновка и запросы ушли мимо узла — и
  проба теперь умеет различать вторую (см. ниже).
- **Тайм-аут 6 с срезал ТРИ сервиса одного узла ровно на 6016 мс**, тогда как
  два других на том же узле ответили за 400 мс. Причина — наша же
  параллельность: шесть одновременных рукопожатий через один узел. Это снова
  дефект ST14 в своей форме: обрыв по собственному тайм-ауту выглядел как
  «узел не пускает сервис». `SVC_SEC` поднят до 10 с, бюджет и сторож
  пересчитаны, `timeout` задания cron — до 120 с.

**Что нашла сама выгрузка, а не отказы.**
- **Одиннадцать прогонов подряд проверили одни и те же четыре узла** — верх
  каскада — из 53. На вопрос «многие узлы нейронки не работали» четыре узла не
  отвечают ни при каком числе прогонов. Заведено окно со сдвигом: смещение
  живёт в `$persistentStore`, за прогон сдвигается на четыре и заворачивается
  по кругу, весь пул обходится примерно за 13 прогонов.
- **`обходных_пропущено: 0` во всех одиннадцати прогонах** — не потому, что
  обхода в каскаде нет (он там есть, рангом последним), а потому, что цикл
  обрывался, набрав четвёрку, и до обхода не доходил. Счётчик, считающий не
  то, что на нём написано, хуже отсутствующего: он выглядел доказательством,
  что правило 1 соблюдено. Отбор идёт по всему списку.

**Что нашло ревью правки** (третий круг по этой пробе).
- Проверка правдоподобия тела была применена и к 3xx — обычный редирект CDN с
  телом объявлялся подменой, а прогон, где так ответил один адрес на всех
  узлах, звал чинить исправный туннель. Дефект ST14 в третий раз. 3xx —
  ответ, тело у него не судят.
- Маркеры страны и бот-защиты смотрелись только у 403, поэтому 200 с капчей и
  200 со страновой заглушкой стали неотличимы — то самое различение, ради
  которого проба и написана. Теперь маркеры смотрятся у любого отказа.
- Сторож 60 с рубил бы прогон, идущий по предохранителям (5 + 4 × 14 = 61 с),
  за секунду до конца, и усечение не попало бы даже в «не успели». Сторож 75 с,
  резерв на узел считается по предохранителю, растянутому втрое, а не вдвое.
- **Адрес страны стоял на нейтральном хосте `www.cloudflare.com`** — и это была
  дыра в правиле 1: такой хост не ловится правилами AI, доезжает до
  `MATCH,RH-Главный`, а тот через `RH-АВТО` заканчивается обходными узлами.
  При отвалившейся пиновке один из шести запросов уходил бы по платному
  трафику мимо рубежа, который смотрит на RH-AI. Перенесён на
  `chatgpt.com/cdn-cgi/trace`: край Cloudflare отвечает до приложения, так что
  страну он отдаёт по-прежнему. Тест теперь сверяет КАЖДЫЙ адрес пробы со
  списком `AI_SUFFIX` из боевых правил.
- Признак группы был белым списком типов, открытым снизу; заменён на наличие
  состава и текущего выбора — у узла их нет.

**Новое в отчёте:** страна выхода сверяется с флагом в имени (флаг говорит о
стране СЕРВЕРА), и расхождение выносится в текст; совпадение выходного адреса
у трёх и более узлов поднимает подозрение «пиновка не действует» — приставкой
к вердикту, а не вместо него, потому что подозрение бывает ложным: окно берёт
смежный кусок каскада, то есть узлы одной страны и часто одного поставщика.

**Тест — 52 проверки**, девять мутаций кода из девяти роняют его.

### ST15 (2026-09-05) — ПУСКАЮТ ЛИ УЗЛЫ ИИ-СЕРВИСЫ

**Вопрос.** Жалоба 03.09: «многие вчера и не работали на узлах нейронки».
Группа RH-AI собирается каскадом ПО СТРАНАМ (`src/ai.js`): порядок считается
от числа узлов в стране и от региона, а не от того, пускает ли сервис этот
выход. Второе проект не мерял ни разу — только предполагал по географии.

**Что делает.** Берёт четыре узла действующей группы RH-AI, пинует запрос
заголовком `X-Stash-Selected-Proxy` и спрашивает `/robots.txt` у пяти
сервисов (ChatGPT, Claude, Gemini, Grok, Perplexity) плюс `/cdn-cgi/trace`
за страной выхода. Порядка 15 КБ за прогон.

**Что различает.** Четыре исхода вместо одного «не работает»: НЕ ДОШЛИ
(статус 0 — про узел), ЗАПРЕТ (403/451, с отдельной пометкой «страна» или
«бот-защита» — про выходной адрес), ВРЕМЕННО (429/5xx — ни о чём, в счёт
чистоты узла не идёт), ОТВЕТИЛ. Страна выхода важна отдельно: флаг в имени
узла говорит о стране СЕРВЕРА, а выходной адрес бывает другим.

**Чего не доказывает, и это записано в самой пробе.** «ОТВЕТИЛ» — край
пустил один безымянный GET; что откроется диалог, отсюда не следует.
«ЗАПРЕТ» от бот-защиты не равен «сервис на узле не заработает» — поэтому
страновой отказ и бот-защита помечаются раздельно по телу ответа.

**Правило 1 держат три рубежа**, а не один: обход отбрасывается по имени;
вложенные группы и служебные члены (`looksLikeNode` + `isGroup`) не
опрашиваются, потому что пиновка на имя ГРУППЫ отдала бы запрос тому, кого
группа выбрала сейчас; и прогон НЕ НАЧИНАЕТСЯ, если действующая группа
смотрит на обходной узел ЛИБО не сообщила выбор вовсе. Последний рубеж
закрыт по умолчанию: пустое `now` — это «проверить нечем», а не «можно».

**Сеть читается, а не угадывается.** На устройстве `-W` и `-C` существуют
одновременно, и какая действует, решает ядро по `ssid-policy`. Схема «W, а
если нет — C» проверяла бы на сотовой не ту группу, не тот набор узлов и не
ту политику во втором рубеже. Сеть определяется по хвосту выбора
родительских групп — метод сборщика.

**Две редакции за день, обе правки — по ревью, не по замеру.** Первая
содержала четыре блокирующих дефекта, и два из них проект уже проходил:
предохранитель узла стоял РАНЬШЕ собственного клиентского тайм-аута (4667 мс
против 8 с) и в переднем плане резал живые запросы — дословный дефект ST14
«молчала проба, а не узел»; знаменатель считался по ПРИШЕДШИМ ответам, и
узел, у которого пропал ровно тот сервис, ради которого проба и написана,
получал «4 из 4» и уходил в чистые. Плюс отчёт ключевался коротким именем
(дубликаты затирали друг друга, трафик тратился впустую) и не было второго
рубежа правила 1.

**Тест — 30 проверок, и три из них смотрят не на поведение, а на текст
файла.** Причина: песочница не моделирует клиентский тайм-аут — подставной
`$httpClient` отвечает через миллисекунду независимо от `SVC_SEC`. Значит
возврат дефекта «предохранитель раньше тайм-аута» поведенческим тестом не
ловится ВООБЩЕ, его держал бы только комментарий. Соотношения
`NODE_WATCH_MS > SVC_SEC`, `GUARD_MS >` худшего честного прогона,
`NODE_COST_MS >= 2 x NODE_WATCH_MS` проверяются регулярками по исходнику.
Проверено мутациями: пять правок кода из шести роняют тест.

### ST14 v3 (2026-09-04) — ДВЕ ОПОРНЫЕ ТОЧКИ И ФАКТ ПРО `?timeout=`

**Найден факт о Stash, которого проект не знал.** Параметр `?timeout=` у
`GET /proxies/{имя}/delay` **ядром игнорируется** — действует
`benchmark-timeout` самого узла. Доказательство из прогонов 19:50–20:05:
обычный узел с `benchmark-timeout: 3` отказывал ровно на 3034, 3039, 3039 и
3040 мс — четыре прогона подряд с разбросом в 6 мс, это тайм-аут, а не сеть;
обходные узлы с `benchmark-timeout: 10` (S-draft-6) в те же минуты упирались
уже в клиентский предел пробы. **Побочно это подтверждает, что S-draft-6
доехал до устройства и применился.** Следствие: временем замера управляет
профиль, а не проба.

**Вторая опорная точка.** Проба брала ОДИН рабочий узел для сравнения, и им
оказалась Польша, отказывавшая четыре раза подряд. Вердикт объявлял «СЕТЬ:
молчат и рабочий, и обходные» — при том что в те же минуты
`Германия [Обход] #2` отвечала за 505 мс. Одна опорная точка делает вердикт
заложником одного плохого узла. Теперь рабочих узлов два, а «СЕТЬ»
объявляется, только если не ответил ни один из них.

### ST14 v2 (2026-09-04) — ПРОБА МЕРИЛА СОБСТВЕННОЕ ТЕРПЕНИЕ

Одиннадцать прогонов 04.09 (18:26–19:21) дали данные и вместе с ними —
дефект самой пробы.

**Дефект.** Клиентский тайм-аут запроса к контроллеру был равен серверному:
`CTRL_SEC = 5` при `/delay?timeout=5000`. Проба рубила свой же запрос ровно
на 5008–5017 мс, в выгрузке это выглядело как `статус: 0` и
«context deadline exceeded», а вердикт объявлял «ОБХОД МОЛЧИТ». Молчала
проба. Тот же узел `webhook.deploy-assure.ru:443` в соседних прогонах
отвечал за 144, 147, 159, 165, 168, 431, 542, 605 мс.
**Правило: клиентский тайм-аут обязан быть с запасом больше серверного —
иначе измеряется не узел, а собственное терпение.** Разведены:
`DELAY_MS` 10 000 мс ядру, `DELAY_CTRL_SEC` 20 с на ожидание ответа.

**Исходы больше не смешиваются.** Обрыв по нашему тайм-ауту получил
отдельную категорию `не_дождались` — раньше он попадал в «молчит» вместе с
настоящим отказом ядра, то есть живой узел записывался в мёртвые. Вердикт
разделяет «ОБХОД ЖИВ», «ОБХОД ОТКАЗАЛ» и «НЕОПРЕДЕЛЁННО: ни один не успел».

**Что при этом установлено по существу.**
* **Обход работает.** `Германия [Обход] #2` отвечает регулярно, задержка
  140–600 мс. Значит после S-draft-5 fallback имеет кого выбрать.
* **Два узла из трёх отказывают по-настоящему:** `Германия [Обход]` и
  `Эстония [Обход]` дают 503 «An error occurred in the delay test» быстро
  (187–2261 мс) — это ответ ядра, а не тайм-аут. Отказ на стороне узла.
* **Сеть в момент замеров нестабильна:** рабочий узел прыгал 79 → 1272 мс
  при обычных 70–90. Одиночные прогоны в таких условиях ничего не решают —
  и это ещё одно подтверждение правила «одиночное измерение не вывод».

### ST14 (2026-09-03) — ПОЧЕМУ ОБХОД В ТАЙМАУТЕ

`probes/routehub-probe-stash14.js` + `plugins/RouteHub-Stash-ST14.stoverride`.

После S-draft-5 обходные узлы наконец МЕРЯЮТСЯ — то есть `benchmark-disabled`
действительно был тем, что подавляло замер, — но все до одного дают таймаут,
тогда как в Loon те же узлы отвечают и работают.

**Почему дальше нельзя гадать.** У обходных узлов три особенности разом, и
все три отличают их от рабочих: **порт 443** (у всех 52 рабочих — 52006 или
8443), инфраструктура `deploy-assure.ru` и половина на `type=ws` (`ws` в
подписке больше нигде не встречается). Плюс наше подозрение на ключ `sni`:
в схеме vless у Stash его нет вовсе (`stash.wiki/en/proxy-protocols/proxy-types`
перечисляет `flow, skip-cert-verify, network, tls, client-fingerprint,
ws-opts, grpc-opts, h2-opts, reality-opts`), а Clash-совместимые ядра
называют это поле `servername`. **Но версия про `sni` сама себе противоречит:**
восемь reality-узлов, которым SNI необходим, измеряются и работают. Значит
либо `sni` всё-таки принимается, либо reality берёт имя откуда-то ещё.
Различить это «снаружи» нечем.

Ядро при замере знает точную причину отказа. `GET /proxies/{имя}/delay` при
неудаче возвращает тело с описанием — проба забирает его сырым и печатает.
Три исхода различаются сразу: ошибка про handshake/TLS указывает на наше
описание узла, «i/o timeout» на TCP — на недоступность сервера с этой сети,
что-то про `ws`/`flow` — на транспорт.

**Правило 1 — сознательное и минимальное исключение:** три HEAD-запроса на
`generate_204` через обходные узлы, порядка килобайта каждый, плюс один через
рабочий узел (без опорной точки «таймаут» ничего не значит). С S-draft-5 ядро
и так меряет обход раз в час.

### ST13 (2026-09-03) — КТО ОБСЛУЖИВАЕТ ОТДАЧУ

`probes/routehub-probe-stash13.js` + `plugins/RouteHub-Stash-ST13.stoverride`.
Вопрос: какое ПРАВИЛО и какой УЗЕЛ обслуживают загрузку в Telegram и в MAX,
когда она не проходит.

Жалоба 03.09: в Telegram видео смотрится, но своё в группу не загружается; в
MAX загрузка не идёт, пока не включить прямой туннель руками. Обе про отдачу,
и причин-кандидатов две, несовместимых. **Кандидат 1:** правила для MAX нет,
он попадает в `GEOIP,RU` → `RH-RU`, а это `fallback` с DIRECT первым и
обходным узлом вторым; если ядро Stash считает DIRECT недоступным, российский
трафик уедет на заграничный узел. **Кандидат 2:** Telegram направлен в
`RH-АВТО` намеренно и работает — кроме отдачи; тогда дело в качестве отдачи
узла, а её проект не меряет вообще (в балле четыре компонента, upload среди
них нет). Лечить, не различив причины, проект уже пробовал — так потерялся
Discord.

Проба показывает по каждому интересующему соединению сработавшее правило,
фактическую цепочку и отдельно — стоит ли в цепочке обходной узел; плюс режим
ядра и выбор служебных групп.

**Двенадцать снимков внутри одного прогона.** `/connections` отдаёт живые
соединения, загрузка видео длится десятки секунд, и одиночный снимок в неё
не попадёт. Снимки объединяются по `id`, минутный cron покрывает минуту
почти целиком.

**Имена хостов выгружаются ТОЛЬКО для Telegram, MAX и VK** — про них и задан
вопрос. Всё остальное считается, но не называется. Это отличие от ST11,
которая не выгружала хосты вовсе: там вопрос был про доли, здесь — про
конкретные сервисы. Граница проверяется тестом, а не декларируется:
`tests/probe-stash13.test.js` роняет прогон, если в отчёт попал хост вне
списка.

---

## Профиль Stash

### S-draft-8 (2026-09-25, ветка stash-client) — ОБЁРТКА ДЛЯ РУЧНОГО ВЫБОРА УЗЛА

ЗАЧЕМ. Диана выбирает узел для RH-AI / RH-АВТО / RH-Звонки вручную, но
ручной выбор не должен отключать автоматический уход с мёртвого узла, а обход
должен оставаться последним резервом. Проба ST19 (25.09, Stash 3.4.1; раздел
«Пробы» → ST19): закрепление члена fallback через `PUT /proxies` к следующему
чтению сбрасывается, выбор в `select` держится ≥ 5 ч, обёртка (fallback с
ручным `select` первым членом) пропускает мёртвый выбор и возвращается к
ожившему. Решение Дианы 25.09 — делать через обёртку.

СОСТАВ ГРУПП. На каждую функцию X и сеть — новая группа `RH-X-W-Ручной` /
`RH-X-C-Ручной`, тип `select`: те же узлы и в том же порядке, что в
`RH-X-W` / `RH-X-C`, но без обходных (`tag === 'bypass'`, слово «Обход» в
любом месте имени). Первый член — лучший рабочий узел по баллу, поэтому без
ручного выбора поведение прежнее. `RH-X-W` / `RH-X-C` остались fallback с
тем же `interval` 600 с; первым членом стала своя «-Ручной», дальше прежний
список, обход последним. Родители `RH-X` (select + ssid-policy) не менялись.
Групп в `proxy-groups` на функцию стало пять вместо трёх; ручная группа идёт
перед своим fallback, как в ST19. Имя (`manualName()`), состав ручной
группы и пару «ручная + fallback» строит новый модуль
`src/clients/stash-manual.js` (`clients/stash.js` уже больше 15 КБ: 19,7 КБ
до правки, 21,2 КБ после); форму членства он получает функциями, без
обратного импорта. Сборщик держит ту же строку имени, сцепку сторожит тест.

ГРАНИЧНЫЙ СЛУЧАЙ. Рабочих узлов нет (только обход или ничего) — ручная группа
не создаётся и в fallback не ставится: `select` без членов Stash не разберёт,
а подмена на DIRECT изменила бы маршрутизацию. Висячей ссылки не возникает —
обе стороны решаются одним условием.

ФОРМА (Б). Ручная группа — `use` + `filter` по рабочим именам; fallback —
`proxies: [<-Ручной>]` рядом с `use` + `filter`. Ставит ли ядро явных членов
раньше членов поставщика — не подтверждено; порядок в форме (Б) остаётся
открытым вопросом стенда. Основная форма — (А), явные имена.

ОТКРЫТО. Кто ставит выбор — пока Диана вручную в интерфейсе Stash; панель,
плитки или автоматика Worker'а — позже. Время реакции обёртки на профиле
(`interval` 600 с против 60 с в ST19) не измерено. Порядок в форме (Б).

Тесты: `clients-stash` 30 → 37, `stash-collect-coupling` 8 → 9, правки в
`bypass` (каскад читается после ручной группы, в ручной группе нет обхода).
Новое: в «-Ручной» нет обхода; её порядок равен порядку рабочих узлов
fallback; она первая в fallback, обход последним; форма (Б); пустой рабочий
набор (только обход; игровой + обход); каждый член каждой группы профиля
существует; порядок в YAML. Мутационная проверка — в записи сборщика v0.2.3.

### S-draft-7 (2026-09-14) — DNS КАК В LOON И АВТОМАТИЧЕСКИЙ FINAL

Пять изменений, все по согласию Дианы.

**`BENCH_TIMEOUT` 3 → 5 с.** Три секунды проект поставил сам, ниже значения из
примера документации (`benchmark-timeout: 5`). Это дефект ST14 в четвёртый
раз: свой короткий порог выдаёт живой узел за мёртвый. У Stash таймаут замера
— ЕДИНСТВЕННЫЙ документированный способ пометить узел нездоровым, значит цена
заниженного порога — ложные переключения. В ST14 Польша упиралась в
3034–3040 мс, то есть промахивалась мимо прежнего порога на тридцать
миллисекунд. Тайм-аут обхода (10 с) не меняется и остаётся длиннее общего.

**`GROUP_INTERVAL` 3600 → 600 с** — возврат к документированному умолчанию
Stash. Прежнее обоснование опиралось на допущение о событийной перепроверке у
`fallback`, которого документация не описывает: там сказано только «Unhealthy
proxies will be skipped». Окно, в течение которого мёртвый `DIRECT` числится
живым, доходило до часа — отсюда полевая жалоба 08.09 («App Store грузил
секунд тридцать, перезашёл — открылось»). Расход на обходных узлах вырос с
примерно 0,4 до примерно 2,3 МБ в сутки; «порядка килобайта на замер» —
оценка, а не измерение, но даже при пятикратном промахе это два порядка ниже
200 МБ, которые тратит активный замер скорости.

**`dns.fake-ip-filter`** — аналога `real-ip` из Loon в профиле не было ВООБЩЕ,
крупнейшее расхождение контуров. Перенесена бесспорная часть: `wpad`,
`+.local`, `*.lan`, `time.*.com`, STUN (пробивка NAT для звонков) и проверка
связности (`msftncsi`, `msftconnecttest`). Apple и iCloud сознательно НЕ
перенесены: гипотеза S-draft-7 в том, что `real-ip` подрывает доменные правила
C-draft-42, и она не проверена ни на Loon, ни на Stash.

**`dns.nameserver-policy`** — возможность, которой у Loon нет вовсе: РФ-зоны
(`+.ru`, `+.su`, `+.xn--p1ai`, `yandex.net`, `vk.com`, `vkuser.net`) уводятся
на системный резолвер. Сверка со списками РКН 08.09: под whitelist из четырёх
наших резолверов живы два — `system` и 77.88.8.8; DoH под whitelist мёртв.
DoH при этом не трогаем: вне whitelist plain-резолвер в РФ отравлен.
`geosite:` не используется — база тянется с github при первом обращении.

**`RH-Главный`: `select` → `fallback`.** Убирает ручное переключение под
whitelist — источник ручной работы номер один. НОВЫЙ РИСК, записанный в шапке
`stash-profile.js` и в «Открыто» ADR-04: документация не описывает, как ядро
считает здоровье члена-ГРУППЫ и чем проверяет `DIRECT`. Если `DIRECT`
ошибочно сочтут мёртвым, весь прочий иностранный трафик молча уйдёт на узлы,
включая обходные. Правка живёт только на стенде k3 и в боевой Loon не
переносится.

Обязательное ревью пройдено, одиннадцать поправок применены. Семь мутаций из
семи роняют тесты. Профиль разбирается настоящим YAML-парсером. ADR-04
приведён в соответствие: интервал, порог, расход, новый открытый пункт.

### S-draft-6 (2026-09-04) — СВОЙ ТАЙМ-АУТ ЗАМЕРА ДЛЯ ОБХОДА

**Что показал прогон ST14.** Из трёх проверенных обходных узлов **один
ответил**: `webhook.deploy-assure.ru:443`, `alive: true`, задержка **729 мс**
(по стенке 1128 мс). Два других — `delay: 65535`, `alive: false`, замер
вернул 503. Рабочий узел для сравнения — 70 мс.

**Это закрывает сразу две версии.** Раз хотя бы один обходной узел
поднимается по той же схеме (`tcp+tls+vision`, порт 443, тот же SNI, то же
описание из нашего конвертера), значит **и профиль, и конвертер верны**.
Версия про ключ `sni` вместо `servername` отпадает окончательно: если бы SNI
не передавался, не ответил бы ни один. Отпадает и версия «порт 443» — узел на
443 работает.

**Что осталось и что исправлено.** Обходные узлы кратно медленнее рабочих:
729 мс против 70 мс, то есть в десять раз; полевой замер 11.08 давал обходу
388–505 мс против 89–99 у обычных. При общем `benchmark-timeout: 3` замер
ходит около порога — отсюда жалоба «делаешь ещё раз, и таймаут уже у других
узлов»: попадание в порог случайно. Обходным узлам задан **свой тайм-аут в
10 с**, заведомый запас над наблюдённым максимумом. Цена нулевая: тайм-аут
тратится только на узлах, которые и так молчат, а обычным узлам длинный
тайм-аут не даётся — он задерживал бы проверку всей группы.

**Чего это НЕ объясняет.** Почему два узла из трёх молчат совсем
(`delay: 65535`, а не «долго»). Это может быть и стороной провайдера, и
временной недоступностью — одиночный прогон вывода не даёт. Следующий прогон
ST14 с новым тайм-аутом покажет, сколько узлов было «медленно», а сколько
действительно мертво.

**Тест** сторожит обе стороны: возврат общего тайм-аута для обхода и его
расползание на обычные узлы.

### S-draft-5 (2026-09-03) — ОБХОД СНОВА РАБОТАЕТ: `benchmark-disabled` СНЯТ

**Отказ.** Обходные узлы на Stash не работали ВООБЩЕ: ручной тест задержки не
показывал ничего — даже не таймаут, а пусто, — и интернета через них не было.
Замер Дианы 03.09 сузил причину до предела: узлы выбирались **вручную в
глобальном режиме** и **без whitelist**, то есть ни `fallback`, ни правила,
ни режим сети ни при чём. В Loon те же узлы работали.

**Что проверено и отброшено.** Конвертер: срез подписки (D1, `sub_cache`)
показал, что из 16 обходных узлов 8 идут `type=ws` (`path=//`,
`host=cdn.deploy-assure.ru`), а 8 — `tcp+tls+vision`, и `ws` в подписке
больше нигде не встречается — все обычные узлы `tcp`. Это выглядело
идеальным подозреваемым: сломанная ветка `ws` задела бы ровно обход. Но
прогон `parseNodeLink` на обеих формах дал корректный результат (`path`
раскодирован в `//`, `ws-opts` с заголовком `Host` на месте), а обходной
`tcp`-узел выходит **структурно идентичным** рабочему обычному — различаются
только `sni` и `fp`. При этом 52 обычных узла меряются и качают.

**Осталось одно, и оно наше.** Единственное, чем профиль отличал обходной
узел от рабочего, — `benchmark-disabled: true`, поставленный в S-draft-3.
Ключ документирован только для `relay`-групп, а применён на узле: ровно тот
случай, про который у проекта есть правило «недокументированные параметры не
в боевой контур без проверки». Снят. Вместе с ним снят `lazy` у `RH-Обход` —
он вводился вторым рубежом под то же правило, и разбирать, какой из двух
ключей виноват, имеет смысл только после того, как обход заработает.

**Правило 1 теперь исполняется интервалом, а не отключением замера.** Прежняя
тревога («2 400 запросов в сутки через обход», вывод 23) считала ЗАПРОСЫ, а
не БАЙТЫ. Замер ядра — HTTP HEAD на `generate_204`, порядка килобайта:
у `fallback`-групп задан `interval: 3600`, и это около **0,4 МБ в сутки** на
все 16 обходных узлов вместе. Для сравнения, наш активный замер скорости
тратит около 200 МБ в сутки на обычных узлах. Обход был выключен ради
экономии, которой там нет, ценой всего whitelist-сценария.

**Что сторожат тесты.** Прежние проверки кодировали старое решение и потому
падали — переписаны вслед за замером, а не вместо него: `benchmark-disabled`
не должен появиться ни у одного узла, адрес теста обязан быть у ВСЕХ, включая
обходные, `lazy` не должен вернуться ни у одной группы, а у каждой
`fallback`-группы обязан стоять часовой интервал.

**Чего эта правка НЕ доказывает.** Что виноват именно `benchmark-disabled`, а
не `lazy` и не что-то третье. Это ведущая версия, снятая методом исключения;
подтвердит её только прогон на устройстве.

### S-draft-2 (2026-08-29) — УДАЛЁННЫЕ НАБОРЫ ПРАВИЛ

Коммит `0e1da32`. Секция `[Remote Rule]` боевого конфига перенесена: двенадцать
наборов стали `rule-providers` плюс строки `RULE-SET,<набор>,<политика>`,
вставленные ниже локальных правил и выше `MATCH` — тот же порядок, что в
`routehub.conf`, где `[Rule]` идёт целиком до `[Remote Rule]`. Разбор
соответствия — `docs/ЭТАП_K_STASH_ПРАВИЛА.md`.

Две находки при выборе `behavior`: `whitelist-ips.list` содержит строки с
префиксом типа (`IP-CIDR,…,no-resolve`), то есть это `classical`, а не
`ipcidr`; у blackmatrix7 реклама разнесена на два файла, потому что 39 897
доменов лежат голым списком и берутся как `behavior: domain`.

Сняты ключи `benchmark-url` и `benchmark-timeout` у поставщика прокси: они
были придуманы по аналогии с Clash, а вики Stash перечисляет только `url`,
`path`, `interval`, `filter`, `headers`.

Проверено на стенде 2026-08-30: профиль 26 206 байт, разбирается PyYAML,
12 групп, 36 правил, последнее `MATCH,RH-Главный`, 12 `RULE-SET` при 12
`rule-providers`. Применение наборов ядром **не проверено** — это вопрос
пробы ST8, на устройстве не прогонявшейся.

### S-draft-1 (2026-08-29) — КАРКАС

Коммит `3cb3a67`. Первый профиль: `mode`, `log-level`, `dns`,
`proxy-providers` (интервал 600), `proxy-groups`, локальные `rules`.

Узлы приезжают ПОСТАВЩИКОМ, а не в теле профиля: сам профиль Stash
обновляет раз в 12 часов и только пока приложение на переднем плане
(`docs/СВЕРКА_STASH_ИНТЕРФЕЙС.md`), а порядок узлов по композитному баллу
должен обновляться чаще.

Группы — пары `-W`/`-C` плюс родитель `select` с
`ssid-policy: {cellular, default}`; `routehub-netwatch.js` в контур Stash
не переносится, переключением занимается сам клиент. Метка узла несёт обе
метрики (вариант 1 из ADR-02), вынесена отдельной функцией.

Перенесены все 24 локальных правила `[Rule]`, `FINAL` стал `MATCH`.
`PROTOCOL,STUN` подтверждён документацией.

Известное ограничение среза, не закрытое до сих пор: **сортировки по
композитному баллу на Stash нет**. Балл кормит
`scripts/routehub-speedtest.js`, а он стоит на трёх опорах Loon, из которых
`$config` у Stash отсутствует принципиально (ST2). Чем заменяются
остальные две — вопрос пробы **ST9**.

---

## routehub.conf

### C-draft-41 (2026-08-15) — СНЯТ СКРИПТ RH-RKN

`routehub-rkn.js` пробивал маяки через ИМЕНА ГРУПП (`RH-Проба-VPN`, `RH-Обход`),
а `$httpClient` понимает только имена УЗЛОВ — имена групп игнорирует молча
(тест T6 от 2026-06-13). Скрипт с самого начала ходил не туда, куда думал:
детект whitelist в Этапе F срабатывал случайно, телефон будился каждые три
минуты впустую.

Замена — ручная проба **L10** (`routehub-probe-context.js`): снимает сеть,
детект whitelist по пяти иностранным маякам через `node:'DIRECT'` и живость
каждого выбранного узла через `node:<имя узла>`. Обходные узлы пропускает —
платный трафик. Запускается кнопкой, не по таймеру.

Сохранены: группа `RH-Проба-VPN`, эндпоинт Worker `POST /rkn`, история
`rkn_hist` в D1. История версий вынесена сюда: шапка занимала 11 КБ из 29 КБ.

---
