# CHANGELOG — RouteHub

История версий Worker'а и главного конфига. Вынесена из шапок файлов
2026-08-15: в `routehub-worker.js` она занимала 18 КБ (четверть файла),
в `routehub.conf` — 11 КБ из 29 КБ. Сами файлы от этого стали легче, а история
никуда не делась и читается здесь целиком.

Порядок — от новых к старым, как было в шапках.

---

## Worker

### v1.9.9 (2026-08-16) — ПУТЬ СКРИПТА С ПАПКОЙ

Подготовка к раскладке репозитория по папкам. Подстановка базового адреса в
`script-path` понимала только имя файла в корне; regex расширен до
`(?:[\w.-]+/)*routehub-…`, то есть работает и с `routehub-speedtest.js`,
и с `scripts/routehub-speedtest.js`.

Сделано ДО переезда файлов намеренно: между коммитом с перемещением и
обновлением конфига на устройстве нет окна, в котором Worker отдал бы
неправильную ссылку. Правка в `src/clients/loon.js`, плюс тест на обе
раскладки.

### v1.9.8 (2026-08-16) — ПРОВЕРКА ДЕПЛОЯ И ЧЕСТНЫЙ ДЖИТТЕР

`GET /version` отдаёт `{"worker":"vX.Y.Z"}` — до этого убедиться, что
Workers Builds собрал именно последний коммит, можно было только косвенно.
Ответ не требует авторизации и не читает D1.

В дашборде `jit` больше не превращается в `0` при `null`: сбойный замер
(v1.9.7) показывался как идеальный джиттер, что читалось ровно наоборот.
Теперь `null` доходит до панели как `null`.

Парная правка — `routehub-speedtest.js` v0.6.3: тестовый адрес переведён на
`connectivitycheck.gstatic.com/generate_204`, тот же, по которому Loon
проверяет узлы в группах. Раньше скрипт и группы мерили разные цели, и
верхушка нашего списка не обязана была совпадать с выбором Loon. Сравнение
медиан `rtt` до и после — через сутки после выката.

### v1.9.7 (2026-08-16) — ОТСЕЧЕНИЕ СБОЙНЫХ ЗАМЕРОВ

Разбор среза `metrics:k1` (`ЗАМЕРЫ_И_ВЕСА.md`) показал: `jit` и `bl` дают
45-50 % различий между узлами, при этом в данных встречались `jit = 23 726 мс`
и `bl = 8039 мс`. Одиночная испорченная проба отбрасывала быстрый узел на
10-23 позиции вниз — а порядок узлов и есть всё, чем Worker влияет на выбор.

`metricOf` теперь отдаёт `null` вместо значений выше предела разумного
(`JIT_BAD = 1000`, `BL_BAD = 2000`). В `scoreOf` такой компонент нейтрален,
а не худший из возможных; `voiceOk` при `jit = null` по-прежнему отказывает —
обещать голос по сбойному замеру нельзя. Реальный плохой джиттер в сотни
миллисекунд порог не превышает и наказывает узел как раньше.

Проверка на срезе 16.08: 🇦🇹 Австрия (`jit` 1028 при `down` 43) поднимается с
40-го места на 11-е, польская локация с `jit` 23 726 — с 50-го на 40-е.
Набор Wi-Fi не изменился вовсе: там абсурдных значений не было.

Парная правка — `routehub-speedtest.js` v0.6.2: устойчивый джиттер на
устройстве. Worker чинит последствия, скрипт — причину. Старые записи в D1
перезаписываются при следующем свипе, то есть эффект появляется в течение
20 минут после деплоя, а не мгновенно.

**Веса композитного балла НЕ трогали.** Проверка показала, что они не рычаг:
любые разумные наборы дают ранговую корреляцию ρ ≥ 0.985.

Заодно тесты разложены по файлам (`harness.js` + `routehub-worker.test.js`,
`clients-loon.test.js`, `metrics.test.js`) и версия перестала хардкодиться в
тестах: правка одного набора больше не требует перезаливки 34 КБ через API,
а релиз — правки тестов. Тестов 62.

### v1.9.6 (2026-08-16) — КЛИЕНТСКИЙ СЛОЙ

По ADR-01 код делится не только на модули, но и на два уровня: ядро `src/*.js`
считает и хранит, `src/clients/*.js` рендерит под конкретный клиент. Первый
клиентский модуль — `clients/loon.js`, туда перенесены три вещи, которые знали
про синтаксис Loon и лежали не на своём месте:

* `aiBlocks` — из `ai.js`. В `ai.js` остался расчёт тиеров (`buildAiTiers`),
  то есть то, что от клиента не зависит.
* `subParamsFromConf` — из `util.js`. Разбор строки `[Remote Proxy]` — формат
  Loon, в утилитах общего назначения ему делать нечего.
* Подстановки плейсхолдеров из `handleConfig` — теперь функция `renderConfig`.
  `api.js` собирает контекст (ключ, база с токеном, флаги устройства,
  посчитанные блоки) и отдаёт его клиентскому слою.

**Логика не менялась.** Проверено побайтовым сравнением: старая цепочка
`replace` и новый `renderConfig` на боевом `routehub.conf` дают идентичный
результат (18 749 байт).

Что это даёт: `clients/stash.js` встанет рядом и будет собирать YAML, не
трогая ядро. Раньше для этого пришлось бы ветвить `api.js` и `ai.js`.

### v1.9.5 (2026-08-15) — РАЗДЕЛЕНИЕ НА МОДУЛИ

Файл вырос до 76 КБ и перестал передаваться инструментами целиком: `git push`
из облачной сессии блокирует прокси (баг claude-code #76248), а передача
текстом рисковала исказить суррогатные пары эмодзи (вывод 9 проекта — на
проверке не подтвердился, все 49 констант прошли round-trip побайтово).
Разложен на восемь модулей в `src/`, каждый меньше 15 КБ и правится по
отдельности. Точка входа — только роутинг и `scheduled`, 6.9 КБ.

Граф импортов ацикличен: `const` ← `util` ← `store` ← `sub` ← {`ai`, `api`,
`dash`, `admin`}. `ADMIN_HTML` импортируется точкой входа и передаётся в
`handleAdminPage` параметром — правило Text в `wrangler.toml` действует только
на статический импорт.

**Логика не менялась**, тесты те же 58 и все зелёные. Временный файл теста
переехал в корень репозитория, иначе не разрешаются относительные импорты.

### v1.9.4 (2026-08-15) — РЕГИОНАЛЬНЫЕ ОСТАТКИ AIeu / AIam

В AI-тиеры попадали только страны с двумя и более узлами, поэтому одноузловые
европейские (на дату: AT, BE, CH, CZ, DK, ES, FR, GB, IE, NO) падали в общий
`AIrest` — ниже тира 🇹🇷 Турции с её двумя узлами. Наблюдение 15.08: `RH-AI-C`
выбрала Турцию, тогда как `RH-АВТО-C` со своим региональным каскадом взяла
Великобританию. Для ИИ гео важнее числа узлов. Новый порядок каскада:
DE → тиры Европы → AIeu → тиры Америки → AIam → тиры прочих → AIrest → Обход.
СНГ исключён везде, как и раньше.

---

## Скрипты устройства

### routehub-speedtest.js v0.6.2 (2026-08-16) — УСТОЙЧИВЫЙ ДЖИТТЕР

`RTT_SAMPLES` 3 → 5, а `jit` считается **усечённым размахом**: из
отсортированных проб отбрасываются минимум и максимум. Раньше метрикой был
полный размах `max − min` по трём пробам — одна проба, попавшая в таймаут или
в паузу планировщика iOS, определяла результат целиком.

На синтетике: пробы `[50, 52, 55, 58, 3000]` дают 6 мс вместо 2950. При числе
проб меньше пяти отбрасывать нечего, поведение прежнее.

Правка парная с Worker v1.9.7: скрипт чинит причину, Worker страхует от
остатков и от чужих данных.

---

## Worker — история до разделения (дословно из шапки)

RouteHub Worker (Cloudflare) — единая точка выдачи конфига и узлов.
VERSION: worker v1.9.3 (2026-08-12) — ТЕСТ-АДРЕС ГРУПП В ШАБЛОНЕ AI.
  Тот же адрес, что и в C-draft-40: cp.cloudflare.com отвергает часть
  плавающих выходных адресов кодом 400, и для fallback это не таймаут, а
  отказ — живой узел считается мёртвым. AI-группы собираются ЗДЕСЬ
  (в конфиге их текста нет), поэтому правка парная: routehub.conf C-draft-40
  плюс этот файл. По отдельности бесполезны.
VERSION: worker v1.9.2 (2026-08-11) — КЛАССИФИКАЦИЯ УЗЛА ПО СЛОВУ В ТЕГЕ.
  tagOf больше не ищет точную подстроку '[VPN]': провайдер выпустил локации
  «[🌀 VPN]» (прямые зарубежные, не обход), и они не попадали никуда.
  Критерий — слово в скобочном теге, порядок проверки обход → игры → VPN.
  Парная правка с routehub.conf C-draft-39 (фильтры Loon).
VERSION: worker v1.9.1 (2026-08-10) — СЕССИЯ АДМИН-ПАНЕЛИ.
  Cookie rh_adm = '<exp>.<подпись>', подпись HMAC-SHA256 по 'rh-admin-v1|<exp>'
  с ключом ADMIN_KEY. Ничего не хранится на сервере: проверка пересчётом.
  Смена ADMIN_KEY обнуляет все выданные сессии автоматически.
  Path=/admin — на эндпоинты устройств cookie не уходит. SameSite=Strict.
VERSION: worker v1.9.0 (2026-08-05) — D1 ВМЕСТО KV, АДМИН-ПАНЕЛЬ.
  Миграция завершена: биндинг RH_KV убран, все чтения и записи в D1
  (таблица kv: key/value/updated_at). Модель данных ПРЕЖНЯЯ: один ключ =
  один JSON-блок (metrics:<kN> — все узлы одним объектом). Лимит D1 —
  100 000 строк/сутки против 1000 записей/сутки у KV.
  Панель /admin: состояние, устройства, флаги, токены, личный список,
  хранилище. HTML вбирается в бандл (правило Text в wrangler.toml).
  FRESH_MS 10 → 60 мин: Loon ходит за /config раз в 15–20 мин, при пороге
  10 мин кэш почти всегда протухал и запрос синхронно ждал Lastdep.
  Отсечение мёртвых метрик: ключи, которых нет в текущем sub_cache.
  Предохранитель — подписка недоступна или дала 0 строк, чистка пропускается.
VERSION: worker v1.8.1 (2026-08-04) — РЕГИОН ВАЖНЕЕ ЧИСЛА УЗЛОВ.
  buildAiTiers: сначала регион (Европа → Америка → СНГ → прочие), потом
  число узлов страны. Азиатские и ближневосточные узлы часто не открывают
  европейские сайты. Парная правка с C-draft-36 (региональный каскад RH-АВТО).
VERSION: worker v1.8.0 (2026-08-03) — ТОКЕНЫ УСТРОЙСТВ, ФАЗА 1.
  Токен 32 символа без похожих (0/O/l/1) в пути: /t/<token>/config?key=kN.
  Фаза 1 — токен не передан, пропускаем; передан и не совпал — отказ всегда.
  Фаза 2 (token_required=true) включена 2026-08-06 тумблером панели.
VERSION: worker v1.7.3 (2026-06-13) — ДАШБОРД: CORS, /mylist, /addrule,
  /delrule, rkn_hist, /dashboard, SVG-иконка. Парная правка с C-draft-26.
VERSION: worker v1.7.0 (2026-06-12) — исправлен дефект CORS.
VERSION: worker v1.6.0 (2026-06-12) — МОДЕЛЬ «СЛАБЫЙ DIRECT».
  Все fallback-группы: max-timeout=2000, interval=120, проба — зарубежный
  generate_204. Парная правка с C-draft-23.
VERSION: worker v1.5.x (2026-06-09) — ОДНА ПОДПИСКА, ДВА НАБОРА.
  /nodes отдаёт блок 🛜 (сортировка по wifi-скорости) и 📱 (по cell), обход
  разово в конце. Разделение меткой в имени, фильтры RH-Filter-W-* / -C-*.
  AI-тиеры строит Worker через плейсхолдеры # __RH_AI_FILTERS__ и
  # __RH_AI_GROUPS__.
VERSION: worker v1.0–v1.4 (2026-06-04…08) — становление контура.
  Личные подписки на ключ (?key=kN), nonce устройства, статусы free/bound/
  conflict, приём метрик POST /speed, кэш подписки sub_cache, cron раз в 2 ч,
  композитный балл узла, метка «блок скорости + процент + ☎» в имени.
  Хранилище — сначала секретный gist, затем KV, затем D1.

Секреты Cloudflare (тип Secret, не Text — plaintext-переменные стираются
каждым деплоем): SUBSCRIPTION_URL, SUB_HWID, ADMIN_KEY.
Переменная CONFIG_URL — обычная, в wrangler.toml.

---

## routehub.conf

### C-draft-41 (2026-08-15) — СНЯТ СКРИПТ RH-RKN

`routehub-rkn.js` пробивал маяки через ИМЕНА ГРУПП (`RH-Проба-VPN`, `RH-Обход`),
а `$httpClient` понимает только имена УЗЛОВ — имена групп игнорирует молча
(тест T6 от 2026-06-13). Скрипт с самого начала ходил не туда, куда думал:
детект whitelist в Этапе F срабатывал случайно, телефон будился каждые три
минуты впустую.

Замена — ручная проба **L10** (`routehub-probe-context.js`): снимает сеть,
детект whitelist по пяти иностранным маякам через `node:'DIRECT'` и живость
каждого выбранного узла через `node:<имя узла>`. Обходные узлы пропускает —
платный трафик. Запускается кнопкой, не по таймеру.

Сохранены: группа `RH-Проба-VPN`, эндпоинт Worker `POST /rkn`, история
`rkn_hist` в D1. История версий вынесена сюда: шапка занимала 11 КБ из 29 КБ.

---

## routehub.conf — история до C-draft-41 (дословно из шапки)

C-draft-40 ОТНОСИТЕЛЬНО C-draft-39 — СМЕНА ТЕСТ-АДРЕСА ГРУПП.
  ПРИЧИНА: cp.cloudflare.com отвергает часть ПЛАВАЮЩИХ выходных адресов узлов
  кодом 400. Для fallback это не таймаут, а отказ — живой узел считается
  мёртвым, перебор проваливается до конца списка, где стоит RH-Filter-Обход.
  Отсюда наблюдаемое 2026-08-11: пять групп сидели на платном обходе, тогда
  как обычные узлы отвечали за 89–99 мс и давали вчетверо большую скорость.
  ЗАМЕР (k4-13, четыре выхода: direct / RU-узел / DE-узел / группа):
    connectivitycheck.gstatic.com — 57 / 64 / 98 / 74 мс, 204 и пустое тело 3/3;
    cp.cloudflare.com — 65 / 147 / 99 / 71 мс.
  Адрес проверен и под активным whitelist РКН (даёт таймаут напрямую — это
  штатное поведение детектора, маяк из того же набора).
  ЧТО ИЗМЕНЕНО: девять мест — proxy-test-url и восемь групп (RH-Обход,
  RH-Проба-VPN, RH-RU, RH-Главный, RH-АВТО-W, RH-АВТО-C, RH-Звонки-W,
  RH-Звонки-C). internet-test-url НЕ трогали: это проверка наличия интернета,
  а не тест узлов.
  ПАРНАЯ ПРАВКА: routehub-worker.js v1.9.3 — тот же адрес в шаблоне AI-групп
  (RH-AI-W / RH-AI-C собираются Worker'ом, в конфиге их текста нет).
  Обе части обязательны вместе, иначе AI-группы останутся на Cloudflare.
  ПОРЯДОК УЗЛОВ НЕ МЕНЯЛСЯ СОЗНАТЕЛЬНО: залипание на обходе — вероятное
  следствие ложных отказов, две правки сразу сделали бы причину неразличимой.

C-draft-39 ОТНОСИТЕЛЬНО C-draft-38 — VPN-ФИЛЬТРЫ НЕ ЗАВИСЯТ ОТ ЗНАЧКОВ.
  ПРИЧИНА: фильтры требовали точную подстроку \[VPN\]. Провайдер выпустил
  быстрые локации с именем «[🌀 VPN]» (🌀 = прямая зарубежная локация, НЕ
  обход; трафик не платный) — 4 узла: 🇩🇪, 🇳🇱, 🇫🇷, 🇵🇱. Ни один фильтр их не
  ловил, ни одна группа не использовала: узлы висели в списке мёртвым грузом.
  Провайдер значки меняет часто, поэтому привязка к содержимому скобок хрупка.
  РЕШЕНИЕ: критерий — слово в скобочном теге: «VPN]» вместо «\[VPN\]».
  Скобка «]» вне класса символов литеральна, обратный слэш не нужен — заодно
  снят риск удвоения слэша при коммите (см. СТАРТ.md, вывод 14).
  Отрицательный lookahead «^(?!.*Обход)» — страховка: если провайдер когда-
  нибудь назовёт обходной узел со словом VPN, тот НЕ попадёт в регионный
  каскад и не начнёт жечь платный трафик.
  ПАРНАЯ ПРАВКА: routehub-worker.js v1.9.2, tagOf (порядок обход → игры → VPN).
  Обе части обязательны вместе — иначе Worker и Loon разойдутся в классификации.
  ОЖИДАЕМО ПОСЛЕ ПРИМЕНЕНИЯ: RH-Filter-W-VPN 43 → 47, RH-Filter-W-EU 22 → 26.

C-draft-38 ОТНОСИТЕЛЬНО C-draft-37 — ЗАГЛУШКА В СТРОКЕ Lastdep (фаза 2 токенов).
  ПРИЧИНА: репозиторий публичный, а в строке лежал рабочий адрес
  подписки (.../nodes?key=k1). После включения фазы 2 (token_required=true,
  2026-08-06) он уже бесполезен — без токена Worker отвечает 403, — но висеть
  в публичном репо ему незачем.
  ВАЖНО: URL в этой строке ничего не решает — handleConfig целиком
  переписывает строку своим origin со встроенным токеном устройства. А вот
  ХВОСТ ПАРАМЕТРОВ после первой запятой Worker берёт ИМЕННО ОТСЮДА
  (subParamsFromConf) — его терять НЕЛЬЗЯ, иначе Loon поставит свои умолчания
  (в том числе block-quic=true). Параметры подписки правятся ЗДЕСЬ.

C-draft-37 ОТНОСИТЕЛЬНО C-draft-36 — ЗАКРЫТИЕ НЕЗАВЕРШЁННОГО ТЕСТА MITM.
  ПРИЧИНА: тест из C-draft-33 ([MITM] enable=true, hostname=*.wikipedia.org)
  так и не был проведён, а MITM всё это время оставался включён вместе с
  постоянно активным RouteHub-FailLog.plugin. Держать расшифровку трафика
  ради непроведённой проверки — лишний риск и лишняя сущность.
  РЕШЕНИЕ: [MITM] enable=false; RouteHub-FailLog.plugin -> enabled=false.
  Строки hostname и заготовка «ловить всё» ОСТАВЛЕНЫ в комментариях — тест
  можно вернуть отдельной задачей, ничего заново не восстанавливая.
  На маршрутизацию правка НЕ влияет: MITM и faillog в выборе узла не участвуют.

C-draft-36 ОТНОСИТЕЛЬНО C-draft-35 — РЕГИОНАЛЬНЫЙ ПРИОРИТЕТ ОБЫЧНОГО ТРАФИКА.
  ПРИЧИНА: азиатские/индийские/ближневосточные узлы часто не открывают
  европейские сайты (геоблок, иная выдача). Нужен порядок: Европа -> Америка ->
  Россия/СНГ -> прочие.
  КАК: 8 новых фильтров по регионам (4 региона x 2 сети) + каскад в RH-АВТО-W/-C.
  Внутри каждого региона порядок ПРЕЖНИЙ — по композитному баллу от Worker.
  ВАЖНО: подписка — ОДИН упорядоченный список, все фильтры сохраняют его порядок.
  Поэтому региональность сделана каскадом fallback, а НЕ пересортировкой узлов —
  так звонки (RH-Звонки) и проба РКН (RH-Проба-VPN) остаются на чистом балле:
  для голоса важнее jit/потери, чем регион.
  Новая страна в подписке, не перечисленная ниже, автоматически попадает
  в RH-Filter-*-Прочие (отрицательный lookahead) — конфиг не ломается.

C-draft-35 ОТНОСИТЕЛЬНО C-draft-34: block-quic=true -> false (откат).
  Проверено: QUIC НЕ был причиной поломок загрузки. Блокировка QUIC не помогла,
  поэтому вернули block-quic=false (QUIC через узлы разрешён). Worker снова
  доступен (блокировка workers.dev снялась) — конфиг подтягивается штатно.

C-draft-34: пробовали block-quic=true (QUIC через узлы). Откатано в C-draft-35.

C-draft-33 ОТНОСИТЕЛЬНО C-draft-32 — ТЕСТ: MITM В ГЛАВНОМ КОНФИГЕ (не в плагине).
  ПРИЧИНА: история показала — example.com ЛОВИЛСЯ (14.06 ~22:51) при C-draft-30,
  где [MITM] hostname=example.com был В ГЛАВНОМ КОНФИГЕ. Перенос MITM в плагин
  (C-draft-32) ловлю сломал — плагинный MITM на устройстве НЕ пробрасывает hostname
  к скрипту (проверено httpbin/wikipedia: «*» появлялась в MITM-странице, но faillog
  домены не получал). ВЫВОД: рабочая связка — MITM в ГЛАВНОМ КОНФИГЕ + faillog-плагин.
  ТЕСТ НЕ ПРОВЕДЁН, снят в C-draft-37.

--- История (сжато) ---
C-draft-32: попытка MITM в плагине (НЕ сработало — плагин MITM не пробрасывает).
C-draft-31: откат теста MITM. C-draft-30: MITM example.com в конфиге — ЛОВИЛО.
C-draft-29: faillog оформлен плагином (conf-скрипт не выбирается в UI-фильтре).
C-draft-28: + routehub-faillog.js. C-draft-27: RKN cron 10->3 мин; дашборд v0.4.2.
C-draft-26: домен дашборда routehub.io -> rh.box (FakeIP); личный список RH-RU
  (/mylist) -> RH-RU выше whitelist; RouteHub-Dash.plugin (визитка homepage).
  T1 (k1): $httpClient ИГНОРИРУЕТ node для ГРУПП; T6: РАБОТАЕТ для имён УЗЛОВ.
C-draft-25: личные исключения в [Rule] (whoosh.bike) — бьют remote whitelist.
C-draft-24: category-ru -> RH-RU (часть не в whitelist РКН).
C-draft-23 — МОДЕЛЬ «СЛАБЫЙ DIRECT» (Этап F: Loon пробивает DIRECT в fallback):
  RH-Главный = fallback DIRECT->RH-АВТО (FINAL); RH-RU = fallback DIRECT->RH-Обход.
  forg-lib whitelist -> ЖЁСТКИЙ DIRECT (обход вреден, анти-VPN). Банки/Apple -> RH-RU.
  Все fallback: max-timeout=2000, interval=120. url пробы — зарубежный generate_204.
C-draft-22..18: дашборд (FakeIP), RH-DashCache/RH-RKN/RH-Проба-VPN, звонки по ☎,
  слой forg-lib + банки ЦБ, hijack-dns=*:53, domain-reject-mode=DNS, Яндекс резерв.
