# ЭТАП DASH — дашборд-инструмент (прогресс)

> Дизайн согласован Дианой 2026-06-12. Этот файл — накопитель решений и
> чек-лист реализации дашборд-этапа.

## ПРИНЯТЫЕ РЕШЕНИЯ (дизайн)

1. **Единый список наблюдения** (вместо двух списков «чекер» + «личный RH-RU»):
   запись = домен + тумблер «через обход». Чекер проверяет ВСЕ записи; в remote-
   список RH-RU (Worker `/mylist?key=kN`) попадают только записи с включённым
   тумблером. «Удаление ожившего» = выключение тумблера ПО КНОПКЕ ДИАНЫ после
   показа результатов проверки (НЕ авто). Домен остаётся под наблюдением.
   Хранение: store `rh_watch` (источник правды) ↔ KV `mylist:kN` (зеркало,
   только route=on). Стартовое наполнение: ТОЛЬКО whoosh.bike (вкл) —
   контрольные НЕ добавляем (решение Дианы).
2. **Каналы данных**: удалённые — прямой fetch `/dashboard` (live);
   локальные (список, pending, runlog, срез getConfig) — маршрут диспетчера
   `GET rh.box/local`; аварийные — кэш rh_dash + localStorage (как v0.1.0).
3. **Маршруты диспетчера routehub-dash.js v0.2.0**: `/` (HTML), `/local`,
   `/add?d=`, `/del?d=`, `/toggle?d=`, `/sync`, `/recheck?d=`.
   ВАЖНО (T1): проверка доменов — HTML-fetch под нужным режимом, НЕ DIRECT-проба
   скриптом (node игнорируется — см. ниже).
4. **Мутации списка — только через диспетчер** (store = источник правды,
   $httpClient без CORS). Чтения /dashboard — напрямую с Worker (быстрее).
5. **Применение списка в Loon** (мгновенность): KV — мгновенно; Loon тянет
   Remote Rule по `update-interval` (целевое 60 с — тест T3) + кнопка
   «Применить в Loon» = универсальная ссылка nsloon.com/openloon/update?sub=all
   (тест T2: тянет ли Remote Rule). API записи правил / перезагрузки ресурсов
   в Script API НЕТ — мгновеннее нельзя.
6. **Вход-«приложение»**: (а) PWA standalone — меты apple-mobile-web-app-capable
   + apple-touch-icon (Worker /apple-touch-icon.png); иконка на домашнем экране
   открывает дашборд полноэкранно; вёрстка с env(safe-area-inset-*);
   (б) плагин-«визитка» RouteHub-Dash.plugin: метаданные + `#!homepage=
   http://rh.box` + `#!icon=` Worker /icon.png; скриптов и правил НЕТ.
7. **История РКН — на Worker** (rkn_hist:<kN>, 50/смена; /dashboard отдаёт 20).
   **Runlog — только локально** (rh_runlog, 100 записей; через /local).
8. **CORS-дефект Worker** исправлен в v1.7.0 (ACAO:* + OPTIONS).
9. **Вкладки HTML v0.2.0**: Обзор / Узлы / Домены / История / Система.
   Live-таймер 15 с (10/15/30/пауза) + Page Visibility. Индикатор источника.
   Вкладка «Система»: реестр скриптов (описания вшиты), возрасты кэшей,
   переходы loon://LogLists и loon://requestLists.
10. **Пуши с deep-link**: openUrl=http://rh.box/#вкладка.
11. **НЕВОЗМОЖНОЕ**: $done({node}) для HTTPS требует MITM; пассивный лог
    неудач — нет; **$httpClient НЕ маршрутизирует по node** (T1, см. ниже).
12. recheck блокируется в UI при rkn.mode=whitelist.
13. Авто-триггеры по опросу Worker — ОТЛОЖЕНЫ.

## РЕЗУЛЬТАТЫ ШАГА 0 (k1, 2026-06-12)

- **T1 — node ИГНОРИРУЕТСЯ.** 5 запросов через RH-Обход / RH-AI / DIRECT /
  фейк-группа / без node — ОДИН IP на Wi-Fi (45.145.56.110) и ОДИН на сотовой
  (.113). Фейк-группа не дала ошибки. Сеть на результат не влияет (IP-ы —
  соседние в одной подсети обходного узла). Вывод: `$httpClient` в Loon 3.3.9
  на k1 НЕ маршрутизирует по параметру `node`.
  → DIRECT-проба скриптом НЕВОЗМОЖНА в обеих формах (node:"DIRECT" и
  RH-ПробаDIRECT). Группа RH-ПробаDIRECT и скрипт-сторож ИСКЛЮЧЕНЫ.
  → Проверка доменов: HTML-fetch дашборда под нужным режимом
  (whitelist — «нужен обход»; норма — «ожил»). Решение — кнопкой Дианы.
- **T5 (CORS)** — проверить попутно (горит ли «живой» зелёным после v1.7.x).

## СДЕЛАНО

- **worker v1.7.3** (деплой OK): CORS; /mylist, /addrule, /delrule; rkn_hist;
  /dashboard +rkn_hist +mylist; /icon.svg + /icon.png + /apple-touch-icon.png
  → SVG-иконка приложения (тёмное поле, хаб + 6 лучей к узлам, бирюзовый акцент;
  обновлена в v1.7.3). ВСЁ проверено живыми запросами.
- **Иконка приложения** отрисована (cairosvg, согласована Дианой): хаб + 6 прямых
  лучей, #05352C/#5DCAA5/#9FE1CB/#7FE0C0. PNG НЕ коммитим — Worker отдаёт SVG.
- **conf C-draft-26** (деплой OK, подстановка mylist проверена): домен rh.box;
  личный список mylist в [Remote Rule] ВЫШЕ forg-lib (Worker подставляет URL —
  проверено: `…/mylist?key=k1, policy=RH-RU, update-interval=60`); строка
  [Plugin] для визитки; [Host] rh.box=198.18.0.1.
- **RouteHub-Dash.plugin v0.1.0**: визитка, #!homepage=http://rh.box,
  #!icon=Worker /icon.png.
- **routehub-test-step0.js v0.2.0** — тест T1 (выполнен, можно удалить из Loon).

## УРОКИ (зафиксировано)

- **Бинарники (PNG) через текстовые MCP-инструменты заливать НЕЛЬЗЯ** — base64
  искажается/обрывается (iconResp на PNG падал с atob-ошибкой). Иконки —
  только SVG (текст, передаётся надёжно), отдаёт Worker.
- **LITERAL-СУРРОГАТЫ ЭМОДЗИ (`\uD83C\uDDxx`) ИСКАЖАЮТСЯ ПРИ ПЕРЕДАЧЕ КОДА ТЕКСТОМ.**
  Дважды подряд при коммите worker строка флага BY `\uD83C\uDDFE` пришла как
  `\uD83C\uDDYE` (F→Y, потеря half) — Cloudflare ОТКЛОНЯЛ автодеплой, Worker висел
  на старой версии. Поймано node --check ПОСЛЕ коммита. Правило: эмодзи-escape
  выносить в ИМЕНОВАННЫЕ константы (const BY=...) и переиспользовать через
  computed-ключ [BY], НЕ дублировать literal'ом в таблицах. После КАЖДОГО коммита
  кода с эмодзи — node --check свежей версии из main (раньше деплоя CF).
  Серьёзнее заметки про PNG: бьётся даже ASCII-escape эмодзи, не только бинарь.
- **replace в Worker берёт ПЕРВОЕ вхождение** — дублирующий плейсхолдер
  `# __RH_MYLIST_URL__` в комментарии истории конфига перехватывал подстановку.
  Урок: плейсхолдеры НЕ повторять в комментариях; сверять /config после коммита.
- raw GitHub .conf кэшируется CDN до 5 мин даже с ?cb — ждать обновления.

## ОСТАЛОСЬ (чек-лист реализации)

- [x] routehub-dash.js v0.2.1 — диспетчер (маршруты /local /add /del /toggle
      /sync /check) + вшитый HTML, 5 вкладок. ФИКС: страница читала w.metrics,
      Worker отдаёт w.nodes:{wifi,cell} — иначе вкладки узлов пусты. apple-touch-
      icon -> Worker. Проверка доменов — HTML-fetch (НЕ node-проба).
- [x] routehub-dash.html — встроен в диспетчер (один файл): 5 вкладок, live-
      таймер 10/15/30/пауза + Page Visibility, чекер, список наблюдения с
      тумблерами, PWA-меты, safe-area, inline-SVG иконография.
- [x] runlog: speedtest+netwatch УЖЕ писали rh_runlog; добавлено в
      rkn v0.2.1 (смена режима {s:'rkn'}) и dashcache v0.2.0 (сбой {s:'dash'}).
      dashcache: кэш теперь в обёртке {ts,data} (возраст на вкладке Система).
- [ ] **ТЕСТЫ НА УСТРОЙСТВЕ (только Диана):**
      T2 — тянет ли nsloon.com/openloon/update?sub=all Remote Rule (mylist).
      T3 — минимальный рабочий update-interval Remote Rule (целевое 60 с).
      T4 — порядок remote-списков: бьёт ли mylist (выше) forg-lib whitelist-
           DIRECT (добавить контрольный домен через /addrule, проверить).
      T5 — CORS: горит ли точка «live» зелёным (страница читает /dashboard).
      T6 (НОВЫЙ, КРИТИЧНО) — см. ОТКРЫТЫЕ ВОПРОСЫ ниже: работает ли node в
           $httpClient для спидтеста/rkn (T1 показал, что НЕТ).
- [ ] Полевой прогон: добавить домен -> применить -> проверить под РКН ->
      выключить тумблер кнопкой.
- [ ] Обновить СТАРТ.md (Диана переложит в Файлы проекта).

## ОТКРЫТЫЕ ВОПРОСЫ (критично, требуют устройства)

- **T6 — противоречие T1 и рабочих скриптов.** T1 доказал: $httpClient
  ИГНОРИРУЕТ node (5 запросов через разные группы -> один IP). НО на node
  построены ТРИ боевых скрипта:
    - routehub-speedtest.js: rttSamples/rateOf с node:<имя узла> — замер
      скорости КАЖДОГО узла. Если node не работает -> мерится ОДИН узел под
      всеми именами (объяснило бы близкие баллы узлов — проверить).
    - routehub-rkn.js: пробы node:"RH-Проба-VPN" / "RH-Обход" — детект
      whitelist. В полевом тесте Этапа F детект РАБОТАЛ.
    - routehub-netwatch.js: probeDirect node:"DIRECT" — детект типа сети.
  Гипотезы: (а) node в $httpClient.get работает, а в сценарии T1 все маршруты
  всё равно шли через обход (обход активен -> один выход); (б) node работает
  для ИМЁН ГРУПП-ПОЛИТИК, но не для DIRECT/несуществующих; (в) не работает
  нигде, а спидтест давал «разные» цифры из-за джиттера одного узла.
  ДЕЙСТВИЕ: тест на k1 — $httpClient.get с node=<конкретный медленный узел>
  vs node=<быстрый узел>, сравнить реальную скорость/IP. От результата
  зависит достоверность ВСЕГО спидтеста и rkn-детекта. НЕ переписывать
  скрипты до ответа — они работают по полевым данным.
- **Чекер доменов (routeCheck) тоже зависит от T6** частично: он НЕ
  использует node (обычный fetch по правилам конфига) — это верно при любом
  исходе T6. Но если node не работает, «DIRECT-проба» точно мертва (уже учтено).

## ДИЗАЙН-СИСТЕМА (зафиксировано)

Язык «хаб и маршруты», нативный iOS: системный шрифт, нижний таб-бар (5 вкладок,
свои контурные иконки — единое семейство, штрих 1.75 pt, скруглённые концы,
inline-SVG); балл узла — кольцевой индикатор (>=70 зелёная зона, 40-69 янтарная,
<40 красная, мёртвый — серое кольцо); режим РКН — баннер под шапкой; индикатор
источника данных — цветная точка (live/кэш/localStorage); тёмная/светлая тема —
prefers-color-scheme; иконка приложения — тёмное поле, три+ маршрутные линии
сходятся в точку-хаб, бирюзовый акцент (#04342C / #5DCAA5 / #9FE1CB / #7FE0C0);
скелетоны при загрузке; scale(.98) на нажатии; никаких внешних шрифтов и CDN.
Иконка приложения — SVG, отдаёт Worker (/icon.svg, /icon.png, /apple-touch-icon.png).
